# 세션 인계 기록 — 2026-08-02

> **목적**: 다음 세션에서 맥락 재설명 없이 이어서 작업하기 위한 기록.
> 이전 인계본(`세션인계_2026-07-31.md`)은 유일 미해결이던 PR #4가 해소돼 폐기했습니다.

## 0. 한 줄 요약

24일간 막혀 있던 **PR #4를 해소**하고 원인(GITHUB_TOKEN PR의 승인 대기)을 구조적으로
고쳤으며, **gg-trusted-registry와의 연동 설계 협상을 종료**했습니다.
남은 일은 합의된 4개 구현 항목입니다.

---

## 1. 현재 상태

| 항목 | 값 |
|---|---|
| 저장소(로컬) | `C:\Users\first\vibecode-checker` |
| 저장소(원격) | `github.com/Lex6won/vibecode-checker` |
| main HEAD | `8a5ab3e` 이후 — 아래 §5 참조 |
| 엔진 버전 | 0.3.0 |
| 테스트 | **661 통과** (+1 skip) |
| CI | ✅ 초록 (pytest 3.11 · 3.12 · packaging) |
| 라이선스 | **PolyForm Noncommercial 1.0.0 + 수급사업자 사용 추가 허가** |
| 실행 방법 | `PYTHONPATH=src ~/miniconda3/python.exe -m gvskb.cli ...` |

> **환경 주의**: 시스템 `python`은 Windows Store 스텁(가짜)입니다.
> 반드시 `~/miniconda3/python.exe`를 직접 호출하세요.

---

## 2. 다음에 할 일 (우선순위)

| # | 작업 | 규모 | 선행 조건 |
|---|---|---|---|
| **1** | **락파일 파싱(P1-4)** | 큼 | 없음 — 바로 착수 |
| 2 | 인텔 캐시 낡음 배너 | 중 | 없음 |
| 3 | verdict 2종 + 3분기 규칙 | 중 | 합의 확정됨 |
| 4 | 레지스트리 플러그인 본체 | 큼 | 위 3개 |

### 1번 — 락파일 파싱

현재 `audit_manifest()`는 락파일을 정직하게 거절합니다(`verdict="unparsed"`,
`check_package.py:468`). 그런데 **실무 취약점은 대부분 전이 의존성에 있고, 전이
의존성은 매니페스트에 없고 락파일에만 있습니다.**

- 우선순위: `package-lock.json` → `uv.lock` → `poetry.lock` → `pnpm-lock.yaml` → `yarn.lock`
- **`limit` 재설계 필요** — 현재 기본 20·상한 100(`check_package.py:457,480`).
  락파일은 수백~수천 건
- **내부 인터페이스를 처음부터 배치 형태로** 설계할 것. 레지스트리 배치 API
  (`check-bulk`, 200건)가 나중에 드롭인으로 붙어야 합니다. 단건 반복 구조로 만들면
  나중에 갈아엎게 됩니다
- `_detect_lockfile` 결과로 `source_scope`를 `lockfile`/`manifest`로 갈라야 함

### 2번 — 인텔 캐시 낡음 배너

3분기 규칙(합의서 §4-2)의 절반이며 레지스트리 없이도 유용합니다.
캐시가 낡았을 때 **패키지별 `requires_review`로 올리지 말고 보고서 배너 1개**로
표시합니다. 30일 초과 시에만 승격.

### 3번·4번

`docs/연동합의_gg-trusted-registry.md` §4·§5·§6이 완전한 명세입니다.

---

## 3. gg-trusted-registry 연동 — 협상 종료

**단일 원천: `docs/연동합의_gg-trusted-registry.md`**
관계 정의·경계·API 계약·결정 연혁이 전부 여기 있습니다. 라운드별 문서 6종은 근거
기록이며, 충돌 시 합의서가 우선합니다.

핵심만:

1. **의존은 단방향** — 레지스트리가 없어도·죽어도 gvskb는 온전히 동작. 환경변수 옵트인
2. **관측과 판정을 섞지 않음** — gvskb는 승인 권한 없음, 레지스트리는 코드 안 봄
3. **fail-open, 침묵 금지** — 조회 실패가 검사를 막지 않되 반드시 표시
4. **로컬 탐지가 최종 안전망** — 기관 승인도 로컬 악성·KEV 대조를 건너뛰지 못함
5. **알림 단위는 조치 단위를 따름** — 시스템 열화는 배너 1개, 패키지 판정만 패키지별로

연동을 막는 미결 항목은 없습니다.

---

## 4. 검증 대기 중인 것 ⚠

**PAT로 만든 PR이 승인 없이 CI를 돌리고 자동 병합되는지는 아직 검증되지 않았습니다.**

| 항목 | 상태 |
|---|---|
| 인텔 수집 → 번들 배포 | ✅ 검증됨 (2026-07-31 정기 실행 초록) |
| PAT 유효성(존재 + 실제 조회 성공) | ✅ 검증됨 (2026-08-01 수동 실행, `state=ok`) |
| **PAT로 만든 PR의 CI·자동 병합** | ❌ **미검증** — 새 KEV 등재일 필요 |

새 KEV가 나오는 날 자동으로 검증됩니다. 그날 워크플로가 초록이고 PR이 자동 병합되면
끝, 실패하면 §6의 진단 절차를 따르세요.

토큰 상태는 이제 **매 실행 점검**되므로 만료는 하루 안에 드러납니다.

---

## 5. 이번 세션 작업 이력

| 커밋 | 내용 |
|---|---|
| `33f1a8f` | **PR #4 근본 원인 수정** — 인텔 룰 PR을 PAT로 생성 |
| `10611f8` | 워크플로 회귀 테스트를 PAT 설계에 맞춰 교정 |
| `1d3d119` | 외부연결 카탈로그 보강 + 문서 문맥이 구체적 경고를 덮어쓰던 결함 |
| `31a32df` | 라이선스 MIT → PolyForm Noncommercial 1.0.0 |
| `28db33e` | 라이선스 수급사업자 사용 추가 허가 부기 |
| `8f17885` | `sk_` 키 탐지·마스킹 누락 + 비상업 라이선스 분류 |
| `134a7d6` | 패키지 판정 감사 기록 추가 |
| `8a5ab3e` | PR 토큰 상시 점검(만료 조기 발견) |

세부 내용은 `CHANGELOG.md`의 Unreleased 절에 정리돼 있습니다.

### PR #4가 24일간 막혔던 이유 (재발 시 참고)

체크런 목록만 보면 전부 성공이었지만, **체크 스위트** 단위로 보니 `pull_request`
이벤트 런 20건이 전부 `action_required`(사람 승인 대기)였습니다.
`statusCheckRollup`이 `null`이라 GitHub 입장에서는 필수 체크가 "보고되지 않음"이었고,
이름·SHA·앱ID가 일치하는 성공 체크가 있어도 BLOCKED이 유지됐습니다.

`workflow_dispatch`로 같은 이름의 체크를 만들어 붙이는 우회책은 **구조적으로 이
게이트를 통과시키지 못합니다.** 지금은 PAT로 PR을 만들어 정상 경로를 탑니다.
이 교훈은 `tests/test_workflows.py`에 가드로 고정돼 있습니다.

---

## 6. 진단 명령 모음

```bash
cd /c/Users/first/vibecode-checker

# 테스트 · 린트
PYTHONPATH=src ~/miniconda3/python.exe -m pytest -q
~/miniconda3/python.exe -m ruff check src tests

# 룰 검증
PYTHONPATH=src ~/miniconda3/python.exe -m gvskb.cli validate-rules

# 인텔 워크플로 상태
gh run list --workflow=update-intel.yml --limit 5
gh run view <RUN_ID> --json jobs -q '.jobs[0].steps[] | "\(.conclusion)\t\(.name)"'

# PR 이 BLOCKED 일 때 — 체크런이 아니라 체크 스위트를 볼 것
SHA=$(gh pr view <N> --json headRefOid -q .headRefOid)
gh api repos/Lex6won/vibecode-checker/commits/$SHA/check-suites \
  --jq '.check_suites[] | {app:.app.slug, status, conclusion}'
# action_required 가 보이면 승인 대기 상태다:
gh api -X POST repos/Lex6won/vibecode-checker/actions/runs/<RUN_ID>/approve
```

---

## 7. 작업 원칙 (계속 유지)

1. **도구의 주장을 그대로 옮기지 않는다** — 원본 소스에서 확인한 것만 판정으로 기록.
   상대 저장소처럼 접근할 수 없는 곳의 보고는 "그쪽 보고"로 표기한다.
2. **우리 도구의 오탐도 상용 도구와 같은 기준으로 기록한다.**
3. **과잉 교정을 경계한다** — 문맥 분류가 구체적 경고를 덮어쓴 결함(`1d3d119`)이 실례.
4. **실패는 올바른 곳에서 나게 한다** — 조용한 초록불이 빨간불보다 위험하다.
5. **커밋·푸시는 명시적으로 요청할 때만.**
6. **경고 피로는 탐지력 저하다** — 같은 사유로 수백 줄이 뜨면 담당자는 무시하게 되고,
   그러면 그 사이의 진짜 위험도 함께 묻힌다. 조치 단위가 다르면 알림 단위도 다르게.

### 이번 세션에서 얻은 것

- **변이 검사**: 새로 만든 가드가 실제로 실패하는지 확인한다. 세 번 썼고 세 번 다
  유효했다. 실패하지 않는 테스트는 초록불을 하나 더 만들 뿐이다.
- **커밋 전 파일에 `git checkout`을 쓰지 않는다** — 변이 검사 되돌리다 미커밋 작업을
  날린 적 있음. 백업 사본을 쓸 것.
- **시크릿을 예시에 쓸 때 실제 벤더 접두사를 피한다** — `sk_live_`를 룰 예시에 넣었다가
  GitHub 푸시 보호에 막혔다. 우회 URL은 쓰지 않고 가상 접두사로 바꿨다.
