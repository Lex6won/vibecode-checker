# Changelog

이 프로젝트의 주요 변경 사항을 기록합니다. 형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/)를 따르고,
버전은 [유의적 버전(SemVer)](https://semver.org/lang/ko/)을 따릅니다.

## [Unreleased]

### 14차 — 같은 줄·같은 유형은 "1개 문제, 근거 룰 2개" (S-8, 개선요청 #34 C) — 2026-08-30

자기검사 때 보류한 S-8 을 포털 요청으로 착수했다(포털은 `rule_id` 를 소비하지 않음을
확인). JSON 계약 변화는 **추가 필드 `Finding.also_matched`** 와 건수 감소 두 가지다.

- `dedup_group` 선언 7묶음 15룰: py-code-exec(GOV-CODE-EXEC-001·KISA-PY-INPUT-02) ·
  py-cmd-injection · py-sql-injection · js-code-exec(KISA-JS-API-02·INPUT-02) ·
  hardcoded-credential(GOV-SECRET-APIKEY-001·KISA-PY-SEC-06·KISA-JS-SEC-06) ·
  password-hashing(SEC-04·SEC-14) · html-dom-xss(GOV-HTML-DOM-XSS-001·KISA-JS-INPUT-04).
- 대표 선정 순위를 (심각도, 결정, rule_id) 에서 **(엔진 정밀도, 근거, 심각도, 결정,
  GOV 우선, rule_id)** 로 — 예전엔 동점이면 문자열 비교로 regex KISA 가 AST-confirmed
  GOV 를 밀어냈다. 패자의 rule_id 는 `also_matched`, references 는 합집합.
- 예외 파일(suppressions)·프로파일 decision_overrides 는 `also_matched` 의 id 로도
  매칭한다(여러 개면 가장 엄격한 쪽). 보고서 카드·표·MD 는 `A (+B)` 로 표기.
  벤치마크 매칭도 also_matched 를 인정.
- 룰별 지표(`collapse_duplicates=False`)는 불변. RULESET.lock 2026.08.30.
- 자기 검사 재측정: 603 → **466건**(병합 169, 차단 48 → 41). 벤치마크 recall 100%·FP 0.
- 곁가지: 12차 fail-closed 캐시가 상태 파일 `.autopull-state.json` 에 매 검사마다
  경고를 냈다 — 캐시 항목 열거에서 점 파일 제외.

테스트 **1838 passed**.

### 13차 — HTML 인라인 <script> 는 JavaScript 다 (개선요청 #34 D3) — 2026-08-30

포털 자체 점검(개선요청 #34)이 "my-scans.html 의 innerHTML XSS 를 체커가 못 잡았다"고
했다. 재현하니 `.js` 면 잡히고 `.html` 이면 0건 — JS 룰 36개가 전부
`languages: [javascript, typescript]` 라 HTML 파일에서는 한 줄도 돌지 않았다. 라운드 18의
"typescript 누락"과 같은 계열의 언어 스코프 결함이다.

- 룰에 html 을 더하지 않고 **스크립트 블록만 남긴 사본**을 javascript 로 한 번 더
  검사한다 — 마크업 설명문의 `eval()` 에 JS 룰이 걸리지 않게. 줄 번호 보존,
  `type="application/json"`·`text/template` 블록 제외, `type="module"` 포함.
  js-taint 엔진도 같은 사본에 돈다.
- 첫 시안의 `type` 판별 정규식이 `["']?` 의 되물림으로 `type="module"` 을 비 JS 로
  오판 — 값을 꺼내 파이썬에서 비교하도록 바꿨다.
- 포털 HTML 749개 실측: 새 발견 26건 중 9건이 `el.innerHTML = ""`·고정 문구 리터럴
  이었다(`.js` 에서도 원래 잡히던 **기존 오탐**). `html_sink_context` 에 "상수 리터럴
  대입" 관찰을 더해 내린다 — `${…}`·`+`·식별자·닫히지 않은 템플릿은 그대로 차단.
  잔여 19건은 전부 동적 값(실제 `my-scans.html:118` 포함).

테스트 **1829 passed**(+12), 벤치마크 recall 100%·FP 0.

### 자기검사 12차 — 체커 자신의 공격면(서버 운영 관점) (Hardening) — 2026-08-30

룰 검사는 제품 코드 0건이었지만 룰은 정규식이다. "서버에 설치해 운영해도 되는가"를
직접 찔러 확인했다 — HTML 보고서 XSS(증거·파일명·대상 경로 전부 이스케이프 확인),
ReDoS(396 패턴 × 병리 입력 13종, 50ms 초과 0), 전송 TLS·타임아웃, 압축 반입, 임시
파일, 심볼릭 링크, 자기 의존성.

- **zip-slip(높음, 실측 재현)** — 인텔 번들 `manifest.json` 의 파일명을 그대로 써
  `../../x` 가 캐시 밖에 실제로 기록됐다. 파일명은 단일 세그먼트만 허용하고 최종
  경로가 캐시 디렉터리 안인지 확인, 멤버 크기 상한(200MB).
- **예측 가능한 임시 파일(중간)** — 자동 갱신이 `/tmp/gvskb-autopull-bundle.zip` 고정
  이름에 썼다(공용 서버 심볼릭 링크 선점). `mkstemp` 배타 생성 + 다운로드 상한(300MB,
  스트리밍).
- **서명 없는 캐시 통과(중간)** — sha256 이 비어 있는 인텔 캐시를 "검증 불가 — 통과"
  시켰다. 캐시 폴더에 쓸 수 있으면 악성 패키지 목록을 바꿀 수 있었다. 거부로 전환
  (`gvskb update-intel` 로 재수신).
- **심볼릭 링크 추적(중간·Linux 서버)** — 파일 링크를 따라가 트리 밖 파일(다른
  프로젝트의 `.env` 등)의 내용이 증거로 보고서에 실릴 수 있었다. 링크는 건너뛰고
  생략 사유로 남긴다.
- **pyproject.toml 미지원(높음·자기 맹점)** — `--check-deps` 가 `requirements*.txt`·
  `package.json` 만 읽어 체커가 **자기 의존성을 한 번도 검사하지 못했다.** PEP 621·
  poetry 파서 추가. 첫 검사에서 하한 fastmcp 2.0.0(CRITICAL 포함 14건)·pydantic 2.0
  이 차단으로 나와 하한을 취약하지 않은 첫 버전으로 올렸다(fastmcp 3.2.0 · pydantic
  2.4.0 · pytest 9.0.3).
- **버전 존재 비교 오탐(높음 등급 오판)** — `httpx>=0.27` 의 `0.27` 이 releases 의
  `0.27.0` 과 문자열이 달라 "요청 버전 없음(high)"이 됐다. PEP 440 정규화 비교.
  또 하한(경계값)이 실재하지 않는 경우는 오타·자리차지 신호가 아니므로 `unknown·low`
  (판정 불가, 초록불 아님)로 갈랐다. `==` 고정 버전 미존재는 그대로 high.
- 마크다운 증거의 백틱 탈출 — 더 긴 펜스로 감싼다.
- **푸시 전 검토에서 잡은 자기 회귀** — 의존성 선언이 없는 pyproject(메타데이터 전용,
  매우 흔함)가 "파싱 실패·검토 필요"로 올라왔다. 선언 0건은 `ok`, `dynamic` 선언이면
  "다른 파일에 있다"고 적고 검토 필요. OneDrive 재분석점이 링크로 오인되지 않음·기존
  캐시 4종 sha256 보유·캐시 파일명 4종 통과를 실측으로 확인.

**개발 환경 참고**: 이 PC 의 miniconda 에는 `mcp 1.27.2`·`starlette 1.2.0`·
`cryptography 48.0.0`(HIGH)이 있으나 **체커 의존 트리 밖**(streamlit·Authlib 이 끌어온
것). 체커 자기 pyproject 감사는 새 하한에서 6/6 clean.

재측정: 테스트 **1807 passed**, 벤치마크 recall 100%·FP 0. 게이트는 여전히 blocked —
잔여 차단은 100% eval_corpus.

### 자기검사 11차 — "이제 배포 가능한가"를 다시 재어 보다 (Recheck) — 2026-08-29

10단계를 닫고 같은 조건(포털 dev-quick · --check-deps)으로 저장소를 다시 검사했다.
결과: **`src/`·`config/`·`rules/`·`tests/` 차단 0**, 잔여 차단 48건 + 패키지 3종은 전부
`eval_corpus/`(고의 취약 샘플·`f_dependencies` 의 pyyaml 5.3.1·reqeusts·minimist).
게이트 판정은 여전히 **blocked** 이며 이것은 도구가 옳게 동작한 결과다 — 저장소에
취약 코퍼스가 실려 있다.

재점검이 낸 것 4건:

- **주석 안의 코드 모양** — 4차 테스트의 설명 주석 `# 바깥의 exec( 가 진짜` 가 문자열
  감쇄 재매치에서 잡혀 `tests/` 에 차단 2건이 남아 있었다(자기참조 세 번째). 문자열
  블랭킹이 **주석 토큰도 지우도록** 바꿨다 — 주석은 실행되지 않는다. 같은 줄의 실제
  `eval(v)` 호출은 그대로 차단(테스트로 고정).
- **실행환경 등급 출처 오표기(회귀)** — 9차의 `cooldown.env_grade` 폴백이 `--env` 를
  주지 않은 실행을 "검사 실행 시 지정(`--env E1`)"으로 적었다. 명시값과 적용값을
  갈라 폴백은 "지정 없음 · 기본값 적용"으로 되돌렸다.
- **confidence 빈 값 라벨 회귀** — 6차에서 `_confidence_label` 을 엔진별로 나누며
  None 의 기본이 '패턴 일치만'에서 "—" 로 떨어졌다. 복원.
- 옛 테스트 2건이 새 문구(악성 0이면 '악성' 생략)·전용 룰(PEM → PRIVATEKEY-001)을
  몰랐다 — 탐지 소실이 아님을 확인하고 기대값만 갱신.

재측정: 테스트 **1790 passed**, 벤치마크 recall 100%·FP 0.

### 자기검사 10차 — 코퍼스·벤치마크가 실제 호출 조건을 말하게 (Corpus) — 2026-08-29

- 에이전트 권한 룰(GOV-AGENT-EXCESSIVE-AUTHORITY-001)의 정밀도·재현율을 코퍼스가 한 번도
  재지 않았다. `d_llm_chatbot/agent_tools.py` 신설 — D-07(`agent.delete_file(path)` 양성)
  · D-08(`tools.delete(name)` 집합 연산, 음성).
- 벤치마크가 기본 프로파일 recall 1.0 만 보고해, 하네스가 실제로 부르는 `dev-quick`
  (severity_min=high)에서 medium 룰이 조용히 빠지는 사실을 숨겼다. `vuln_recall_dev_quick`
  과 빠지는 케이스 목록(A-11 KISA-PY-ERR-03 · C-01 혼합 콘텐츠)을 함께 적는다.
- 재실행: recall **40/40**, 음성 대조군 FP 0, dev-quick 0.95(A-11·C-01).

**자기검사 10단계 종합** — 493건/차단 297 → 425건/차단 47(잔여 전부 eval_corpus 고의
샘플, 신규 테스트 픽스처 제외). `src/`·`config/`·`rules/`·`tests/` 차단 0. 테스트 1028 →
1770+. 수정 명세 44항목 중 41항목 적용, 보류 3(S-8 dedup 병합·R-11·P-13).

### 자기검사 9차 — 보고서가 자기 결과를 정직하게 말하게 (Report) — 2026-08-29

보고서 감사 결과 **수치는 전부 맞았다**. 결함은 의미와 문구였다.

- **재현 명령을 결과에 새긴다** (`ScanReport.reproduce_command`). `gvskb report <json>`
  으로 다시 렌더하면 폴백 `gvskb scan <path> --profile X` 가 `--check-deps`·`--max-files`
  를 잃어, 재현자가 의존성 차단이 사라진 **더 깨끗한 결과**를 받았다. 이제 JSON 의
  명령을 우선 쓰고, 없으면(구버전) "재현 명령 미기록"을 명시하며 의존성 감사가 있었으면
  최소 `--check-deps` 를 붙인다.
- **HTML 은 자기 경로를 말한다.** "이 보고서 위치"가 HTML 안에서도 `.md` 였다. 렌더러마다
  자기 경로 + "같은 이름으로 함께 저장: <쌍둥이>".
- **"비밀값 노출 129건 — 반드시 재발급"** 중 98건은 도구가 스스로 낮춘(테스트 경로·
  안내문·문자열·룰 문서) 것이었다. `_exposure_counts` 가 살아 있는 것과 낮춘 것을 갈라
  세고(`secret_live`/`secret_attenuated` — 기존 키는 그대로), 결론 문구는 살아 있는
  건수로 재발급을 요구하고 낮춘 건수는 별도로 적는다.
- 조치 순서 1단 힌트 "사유 기록으로 넘길 수 없습니다" → "보안담당자 승인 없이는 우회할
  수 없습니다" — 같은 문서의 기준표("소스 발견 → 조건부 승인")와 모순이었다.
- 같은 룰이 '필수'와 '나머지'에 나뉠 때 목록에 **낮춘 사유**를 바로 적는다.
- 수정 프롬프트의 위치 목록을 파일당 8줄에서 자르지 않는다("외 6건"은 AI 가 고칠 수 없다).
- 판정 기준 칸에 룰셋 **지문을 항상 병기**(버전 문자열은 같은 값으로 룰이 바뀔 수 있다).
- "총 조치 대상 499건(소스 493건 · 패키지 6종)" — 단위가 다른 수를 더하지 않는다.
- "취약·악성 패키지 N종" — 악성 0 이면 '악성'을 말하지 않는다.
- 실행환경 등급을 audit 최상위에 없으면 각 check 의 `cooldown.env_grade` 에서 읽는다.
- 테스트 `tests/test_selfscan_round9_report.py`(9케이스). `test_report_readability` 의
  "8건" 합산 기대를 새 문구로 갱신.

### 자기검사 8차 — 외부 연결 인벤토리가 '호출'과 '표·주석·문서·테스트'를 가른다 (Fixed) — 2026-08-29

자기검사의 외부 연결 111행 중 제품 코드의 **실제 아웃바운드는 10건**이었다. 나머지는
스캐너 자기 카탈로그의 문자열 표, 출력용 상수, 문서 URL, 테스트 리터럴. 카탈로그 소스의
**주석 한 줄**이 "서버 공인 IP 노출 ⚠"로, `docs.python.org` 참고 링크가 "Python 설치본
다운로드 · 개인정보 인접"으로 올라왔고, 개인정보 인접 13건 중 정당한 것은 0건이었다.
보안팀에 넘기는 목록이 grep 결과가 되면 신호가 노이즈에 묻힌다.

- `ExternalConnection.context` 에 `test`·`comment`·`data-table` 추가(기존 값 유지).
  줄 문맥으로 판정: 주석 줄 → comment, 창 안에 호출식(requests/httpx/fetch/axios…)이
  있으면 runtime, URL 이 3개 이상이거나 표 원소 모양이면 data-table, 테스트 경로면 test.
  한 호스트가 한 파일에서 호출식과 표 양쪽에 나오면 호출식이 이긴다(과소 판정 방지).
- 개인정보 인접 신호는 **URL 본문·주석을 뺀** 코드에서, runtime 맥락일 때만.
- 호스트 카탈로그는 접미 매칭(`python.org` 가 `docs.python.org` 를 삼키지 않음),
  `docs.`/`wiki.`/`blog.`/`help.` 는 "문서 사이트(운영 중 전송 아님)".
- URL 호스트의 마지막 라벨은 알파벳 TLD — `git+https://…@v0.2.1` 의 `v0.2.1` 가짜 호스트.
- 한눈에 보기의 국외·⚠ 는 **고유 호스트** 기준, runtime 맥락만(행 기준으로는
  api.openai.com 하나가 "국외 11"이었다).
- ipify 문구를 단정형 마크다운 강조에서 가능성형 평문으로.
- 테스트 `tests/test_selfscan_round8_external.py`(9케이스).

### 자기검사 7차 — 전수 분석이 드러낸 미탐 4종 (Detection) — 2026-08-29

오탐을 줄이는 것만으로는 반쪽이다. 493건 전수 대조와 적대적 실측에서 **잡았어야
하는데 못 잡은 것**들이 함께 나왔다.

- **프롬프트 주입(AST)** — `SYSTEM_PROMPT + user_input`(리터럴이 없어 "리터럴+동적"
  판정을 비껴감), `"요약: %s" % user_input`(`%` 포맷 미추적 — SQL 조립에도 같은
  구멍이었다), LangChain `chain.invoke(p)`·`llm.predict(p)`(sink 목록에 없음). `%` 는
  `_expr_builds_dynamic_sql` 에 넣어 SQL·프롬프트가 함께 잡는다. LangChain sink 는
  `db.invoke`·`queue.stream` 오탐을 막으려고 수신자를 llm/chain/model/chat/agent 로 한정.
- **판정 근거 규약 통일** — 프롬프트 주입도 SQL 과 같이 "동적 부분이 매개변수뿐이면
  likely, 스코프 안에서 조립을 따라갔으면 confirmed". 예전엔 무조건 confirmed 였다.
  대문자 지시문 상수(`SYSTEM_PROMPT`)는 동적 이름으로 세지 않는다.
- **`sh -c <오염값>`** — `subprocess.run(["sudo","-u","root","sh","-c",cmd])` 는
  shell=True 가 없어도 셸을 띄운다. 코퍼스 s04_privesc.py:13 이 미탐이었다. argv 에
  sh/bash/cmd/powershell + `-c`/`/c` + 비상수 원소가 있으면 KISA-PY-INPUT-05.
- **`yaml.load` 안전 Loader 없음** — 코퍼스 c_deser.py:8 이 미탐. `Loader=` 가
  Safe 계열이 아니거나 없으면 KISA-PY-CODE-03(AST 정본 + regex 보조).
- 테스트 `tests/test_selfscan_round7_misses.py`(19케이스). 룰셋 잠금 2026.08.29d.

### 자기검사 6차 — 룰 정밀도 8종 (Rules) — 2026-08-29

- **GOV-LLM-OUTPUT-HANDLING-001**: sink 가 **호출 `(` 또는 대입 `=`** 형태여야 발화.
  토큰 공존만 요구하던 패턴은 케이스 ID(`llm-eval-in-evaluate-json`)·산문·룰 린터의
  정규식 문자열·음성 픽스처까지 높음·차단(11건). `subprocess.run(model_output)` 미탐 보강.
- **GOV-LLM-PII-PROMPT-001**: 값 신호(f-string 보간 `{rrn}`·`+`/`%` 결합·주민번호/휴대폰
  리터럴·`주민번호:`) 요구. `prompt = "민원 챗봇입니다"` 가 치명이었다. `*_masked` 제외.
- **GOV-AGENT-EXCESSIVE-AUTHORITY-001**: 동사 뒤 `(?!(?-i:[a-z]))` — `dropdown`·
  `approved`·`transferable`·`deletion_log` 는 안 잡고 `sendEmail`·`dropTable`·`DELETE` 는
  잡는다. `(?i)` 아래 `[a-z]` 가 대문자를 먹는 함정은 `(?-i:)` 로 되살렸다.
- **KISA-PY-INPUT-13**: `redirect(request.*)`·`HttpResponseRedirect(request.*)` 패턴 삭제
  — 오픈 리다이렉트(INPUT-07)의 영역. 헤더·쿠키 직접 주입만 남김.
- **KISA-PY-ERR-01**: `app.run(debug=True)` 패턴 삭제 — GOV-FLASK-DEBUG-001 과 글자
  단위 동일. Django `DEBUG = True`·`str(e)` 반환은 유지.
- **KISA-PY-SEC-06**: 픽스처 가드(dummy/fake/test/example…)를 형제 룰 APIKEY-001 과
  맞춤 — 형제가 negative 로 확정한 `dummy_password_1` 을 이 룰이 되살렸다.
- **GOV-SECRET-APIKEY-001**: PEM 패턴 삭제 — PRIVATEKEY-001 이 더 넓게 잡는다(9줄 중복).
- 보고서 판정 근거 라벨: regex 룰의 `confirmed` 는 "확인됨(패턴 자체가 확증)". 예전엔
  "데이터 흐름 추적"이라 적어 하지 않은 일을 한 것처럼 읽혔다.
- 코퍼스: E-02 휴대폰 시료를 `010-2345-6789` 로(예전 값은 PHONE 룰의 예시 제외번호라
  구조적으로 못 잡았다), D-02 기대에서 발화 불가 룰(baseline OWASP-LLM-2025-01·
  OUTPUT-HANDLING) 제거, I-05 기대에서 INPUT-13 제거, H-05/06/08 을 탐지 기대로 승격.
- 보류: KISA-PY-INPUT-01 f-string/`+` 확장은 dedup 병합(S-8)과 함께 — 지금 넣으면
  GOV-SQL-INJECTION-001 과 같은 줄 2건만 늘어난다.
- 테스트 `tests/test_selfscan_round6_rules.py`(52케이스). 룰셋 잠금 2026.08.29c.

### 자기검사 5차 — 게이트 판정을 JSON 에 싣는다 (Added · 계약 비파괴) — 2026-08-29

사람용 보고서의 결론은 `gate_status()`(의존성이 게이트, 소스는 보조)인데 JSON 에는
그 결과가 없었다. JSON 만 읽는 포털은 `summary.blocked`(소스 기준·legacy)로 폴백해
**같은 검사에서 문서와 기계가 다른 답**을 냈다. 기존 필드는 그대로 두고 추가만 한다.

- `ScanReport.gate` — 저장·반환 직전 `gate.attach_gate()` 로 새긴 판정 스냅샷. CLI
  `scan`(의존성 감사가 붙은 뒤)·MCP `scan_code`/`scan_path`/`detect_secrets_and_pii`/
  `save_report` 전부.
- `ScanSummary.location_count`·`block_location_count` — 고유 (파일, 줄) 수. 같은 줄에
  GOV·KISA 두 룰이면 건수 2, 위치 1. `summary.blocked` 는 docstring 에 legacy 명시.
- 구버전 JSON(`gate` 없음)도 그대로 파싱된다(회귀 테스트).
- 같은 줄 두 룰을 **한 건으로 병합**하는 `dedup_group` 활성화는 건수 의미가 바뀌어
  포털·하네스 확인 뒤로 미룬다(명세 S-8).

### 자기검사 4차 — 경로가 아니라 구조로 낮추기 (Fixed) — 2026-08-29

**① 문자열 리터럴 안의 코드 모양.** 자기검사에서 코드 실행·명령 주입 발견 216건 중
tests/*.py 의 159건이 **전부** `scan_code("eval(x)")` 처럼 문자열 안에 있었다(tokenize
로 159/159 확인). 실행되는 코드가 아니다. `fixtures/`·`corpus/` 를 테스트 경로에 추가
하자는 안은 **거부**했다 — Django fixtures 는 실제 개인정보 덤프가 가장 흔히 놓이는
자리라, 경로로 낮추면 정확히 가장 위험한 곳에서 게이트가 열린다. 대신 구조로 판단한다:
그 줄에서 문자열 내용을 지운 뒤 룰 패턴이 하나도 안 맞으면 매치는 문자열 안이었다.
Python·regex 엔진·비노출 룰에만, 그리고 **지우지 않고 낮춘다**(코드 생성기가 취약
코드를 문자열로 써내는 사례). `exec("os.system('id')")` 의 바깥 `exec(` 는 그대로 차단.

**② 룰 정의 문서.** 파일명에 secret/password 가 든 룰 문서 6개가 '비밀 파일 특례'로
스캔돼 룰의 **예시 코드**가 치명 35건이었다. `rules/` 경로를 빼면 진짜 프로젝트의
`rules/` 업무 코드가 사각지대가 되므로 **구조 마커**(frontmatter `id:`+`severity:`+
`detection:/patterns:`, semgrep `rules:`+`id`+`pattern*`, 벤치마크 `cases:`+`sink:`+
`expected_rule_ids`)로 인식하고, 제외가 아니라 감쇄한다. 비밀 자재 검사(`secret-file`)
는 룰 문서라도 낮추지 않는다 — 예시에 진짜 키를 붙여 넣는 사고 대비.

**③ 값 기반인데 카테고리가 달라 감쇄에서 빠진 룰.** `GOV-LLM-PII-PROMPT-001`(category
llm-appsec)은 적혀 있는 것만으로 위험한 값 기반 룰인데 테스트 경로 감쇄가 카테고리로만
걸려 tests/ 픽스처 5건이 게이트를 막았다. `schema.VALUE_BASED_RULE_IDS` 로 룰 단위
opt-in(카테고리를 바꾸면 프로파일·리포트 분류가 흔들린다).

**④ 금지문 정규식.** `이렇게 쓰지 마세요: eval(x)` 는 안 낮아지고(`하지 마` 만 알았다),
`never_cache = eval(x)`·`deprecated_eval(y)` 는 식별자 조각이 안내문으로 읽혀 실코드가
낮아졌다. 동사+`지 마세요` 일반형 추가, 영문 토큰 양쪽 ``.

- 테스트 `tests/test_selfscan_round4.py` 신규(22케이스).

### 자기검사 3차 — 증거 마스킹이 못 가리던 일곱 모양 (Security) — 2026-08-29

보고서에 **원문이 그대로 실리던** 값들(실측): `SECRET_KEY`·`SIGNING_KEY`·
`JWT_SECRET_KEY`(접미 `_KEY` 를 키 어휘가 몰랐다), `비밀번호 =`(한국어 키), `**password**
=`(마크다운 강조 — 탐지 룰은 알고 마스킹은 몰랐다), `postgres://admin:p4ss@…`(URL
자격증명), JWT 3세그먼트, 카드번호 16자리. 국면 ⅩⅩⅢ("탐지해 놓고 유출본을 한 벌 더
만들었다")과 같은 계열이다 — 탐지 룰의 어휘가 넓어질 때 마스킹 어휘는 따라오지 않았다.

- 인용값 규칙의 키 어휘를 탐지 룰과 같게(`secret(?:[_-]?key)?`·`signing_key`·
  `private_key`·`access_key`·`auth_token`·`비밀번호`·`암호`, `**` 강조 허용).
- URL 자격증명은 통째로, JWT 는 헤더 앞 4자만, 카드번호는 앞 4·뒤 4 만 남긴다.
- `_MASKED_SHAPE_RE` 가 카드 모양을 알아 `evidence_is_masked` 가 참을 돌려준다.
- 가리면 안 되는 것 고정: 경로 안의 `token` 단어, 커밋 해시, `PUBLIC_KEY`, 타임스탬프,
  카드 모양이 아닌 번호. 주민번호·휴대전화 모양은 그대로.
- 테스트 `tests/test_selfscan_round3_masking.py` 신규(19케이스).

### 자기검사 2차 — 존재하는 스쿼팅 패키지가 통과하던 게이트 구멍 (Fixed · Rules) — 2026-08-29

**① `expresss 0.0.1` 이 "이상 없음"이었다.** 평가 코퍼스의 고의 오타 패키지인데,
npm 에 `expresss` 는 **실제로 존재**한다(2016년 등록된 자리차지 패키지, latest 0.0.0).
판정 사다리는 `malicious > vulnerable > cooldown_hold > checked_clean` 네 단뿐이라
이름 휴리스틱(`typosquat_warning`)이 어디에도 반영되지 않았고, 이름 신호를 가르는
것은 **레지스트리 실재 여부뿐**이었다 — `reqeusts`(미존재)는 차단, `expresss`(존재)는
통과. 스쿼터가 이름을 등록해 두면 휴리스틱이 정확히 그 상황에서 무력화된다.
요청한 버전 0.0.1 은 존재하지도 않았는데 발행일 None → `cooldown.ok=None` 으로
흡수돼 "판정 불가"조차 표시되지 않았다.

- `PackageRegistryMetadata.version_exists` 신설(npm `versions`·PyPI `releases` 대조).
- 판정 `version_not_found`(high)·`suspicious_name`(편집거리 1, high) 추가 — **실재
  여부와 독립**으로 적용. `requires_review` 에 `cooldown.ok is None`·편집거리 2 포함.
- 게이트 조건부 기준에 두 판정 추가. 보고서는 **판정 칸**에 "⚠ 이름 의심(express와
  1자 차이)"를 적는다 — 비고 칸의 경고는 판정 칸이 초록이면 읽히지 않았다.
- CVSS CRITICAL 패키지를 보고서 집계에서 '치명'으로 — 결론은 "CRITICAL 3종 차단"
  인데 집계는 "치명 1"이던 불일치(HIGH 와 한 등급으로 묶여 있었다).

**② KISA-PY-SEC-10 이 known-answer 테스트를 차단했다.** `assert _sha256_bytes(b"abc")
== hashlib.sha256(b"abc").hexdigest()` — 자체 헬퍼가 표준 라이브러리와 같은 값을
내는지 확인하는 줄. `== hashlib.sha256(` 만 보고 발화했다. `exclude_patterns` 로
assert 문·리터럴 인자를 제외하되, 같은 줄에 요청·헤더·토큰 등 **외부값 토큰이
있으면 제외를 취소**한다 — `if hashlib.sha256(b"admin").hexdigest() ==
request.args["h"]` 는 계속 잡힌다.

**③ KISA-PY-SEC-05 가 사설망 http 를 평문 전송으로 잡았다.** `requests.post(
'http://10.0.0.5/api')`·`http://localhost:3000` — 룰은 CWE-319 민감정보 평문 전송인데
호스트도 페이로드도 보지 않았다. 망분리 기관의 내부 API 호출 전반에 경고가
뿌려진다. 공인망 http 는 그대로, 사설망·루프백은 민감 토큰이 동반될 때만.
`http://10.0.0.5.evil.com/` 은 경계 `[:/'"]` 요구로 여전히 잡힌다.

- 회귀·적대 테스트 `tests/test_selfscan_round2.py` 신규(28케이스).
- 룰셋 잠금 → **2026.08.29b**.

### 자기검사 1차 — 제품 코드 오탐 세 원인 제거 (Fixed · Rules) — 2026-08-29

포털로 **이 저장소 자체**를 검사해 493건이 나왔다. 493건을 하나씩 소스와 대조한
결과 `src/` 실제 취약은 0건이었고, 제품 코드·설정을 차단으로 올린 원인은 셋이었다.
(전수 판정과 수정 명세 44항목은 별도 문서 — 이 커밋은 그중 1단계.)

**① 데이터 파일에 언어 필터가 꺼져 있었다.** `_EXT_TO_LANG` 에 .yaml/.json/.toml 이
없어 언어가 "미상"이 됐고, 언어 필터는 *미상이면 통과* 였다. 그래서 Python/JS 전용
룰이 YAML 문자열(`sink: "eval(x)"`)·JSON 케이스 ID·semgrep 룰 패턴에 그대로 걸렸다.
정작 .html 은 언어가 있어 필터됐으니 **알 수 없는 파일에서만 필터가 꺼지는** 역전.
.yaml/.yml/.json/.toml → `data`, .ini/.cfg/.conf/.env/.properties → `config` 를
부여했다. 값 기반 룰(시크릿·PII·내부망, `languages` 미선언)은 영향 없이 계속 본다.
데이터 파일에 실행 코드가 실리는 룰(package.json 스크립트·CI `run:`)은
GOV-CODE-EXEC-001·GOV-CMD-INJECTION-001·KISA-PY/JS-INPUT-05 가 `data` 를 **명시
opt-in** 했다 — 기존 테스트 `test_real_execution_risk_still_blocks` 가 지키는 자리.

**② 벤더 접두사 패턴에 좌측 경계가 없었다.** `sk-[A-Za-z0-9_-]{20,}` 가 NIST 공개
URL `ai-risk-management-framework` 의 단어 중간(`ri` 뒤)에서 정확히 20자를 잡아
**제품 설정 파일 `config/security_sources.yaml` 이 치명·배포 차단**으로 올라왔다.
`desk-`·`task-`·`mask_token…`·`NAKIA…`·`laughp_…` 도 같은 결함. 접두 패턴 10개
전부에 `(?<![A-Za-z0-9…])` 를 걸었다(`` 는 `_sk-` 를 통과시키므로 lookbehind).
`Bearer sk-…`·`="sk-…"`·`=sk-…`(.env)·`?key=sk-…`·`{"key":"sk-…"}` 는 계속 잡힌다.
증거 마스킹의 `sk[-_]` 규칙도 같은 결함이라 같은 경계를 걸었다 — 공개 URL 을
`ai-risk-ma[마스킹]rk` 로 가려 담당자가 무엇이 잡혔는지 알 수 없게 하고 있었다.

**③ `GOV-CODE-EXEC-001` 이 메서드 호출을 잡았다.** `(eval|exec)\s*\(` 는 `.` 뒤를
막지 못해 `engine.eval(`·`pattern.exec(`·`cp.exec(` 를 치명으로 올렸다(13줄). 실제
Python 에서는 SQLModel 의 `session.exec(select(...))` 가 매우 흔해 실사용 오탐이
확실한 자리. KISA-PY-INPUT-02 와 같은 경계 `(?<![A-Za-z0-9_.])` 를 쓰고
`builtins.exec` 우회만 명시적으로 되살렸다.

**같이 배운 것.** 수정 주석에 `` `sk-management-framework` `` 를 그대로 적자 새
패턴이 **그 주석을 잡았다** — 백틱 뒤는 경계 밖이고 시크릿 룰은 주석도 본다. 도구가
자기 결함 설명문을 결함으로 잡는 자기참조는 국면 ⅩⅨ와 같은 계열이라 문구를 바꿨다.

- 회귀·적대 테스트 `tests/test_selfscan_round1.py` 신규(양방향 45케이스).
- `test_security_guidance_prose_is_attenuated_not_blocked` 는 YAML 산문이 아예
  매치되지 않게 되어 "발견 존재" 전제를 풀었고, 감쇄 메커니즘 검증은 코드+반성문
  케이스(`test_prohibition_note_attenuates_but_keeps_finding`)로 옮겼다.
- 룰셋 잠금 2026.08.9 → **2026.08.29**.
- 자기검사 재측정(dev-quick, 신규 테스트 파일 제외): 493/차단 297 → **445/247**,
  `src/`·`config/` 발견 **0**. 남은 대부분은 tests/ 문자열 리터럴(2단계 대상).

### git 없는 PC 설치 경로 추가 · 퀵스타트를 현재 도구에 맞춤 (Docs) — 2026-08-13

**① git 없는 PC 설치 경로가 문서에 없었다.** 안내한 설치 명령이
`pip install git+https://…` 하나뿐이었는데, 기관 표준 PC에는 git 이 없는 경우가
많다. zip 주소 직접 설치(`pip install https://…/archive/refs/heads/main.zip`)가
동작하는지 깨끗한 venv 로 실측 확인하고 추가했다. AI 코딩 도구에 설치를
맡기는 방법(터미널 쓰는 도구 vs 아닌 도구)도 함께 적었다.

**② 퀵스타트가 도구보다 뒤처져 있었다.** 사용자가 따라 하면 실패하거나
현재와 다른 결과를 보는 자리들:

- MCP 설정이 `"command": "python"` — Windows 에서 Microsoft Store 스텁으로
  잡히면 서버가 조용히 뜨지 않는다. `gvskb-server` 로 교체하고 "저장 후 도구
  재시작" 안내를 붙였다(리드미는 이미 고쳤는데 퀵스타트만 남아 있었다).
- 결과 읽는 법이 `block`/`warn`/`allow` 3색이었다 — 지금 보고서는 **결론 박스**
  (승인 가능/미승인/보류)가 먼저 나오고 등급 이름도 '필수 조치'다. 결론 박스
  중심으로 재작성하고 "패턴 일치만" 항목 확인 안내를 추가했다.
- 망분리 절차가 "캐시 폴더를 옮기라"는 옛 방식이었다 — 현재는 검증 가능한
  번들(`intel-bundle import`, 해시 불일치 시 전체 거부)이다. 공식 번들 URL 로 교체.
- **없는 기능을 안내하고 있었다** — *"Claude·Cursor에서는 `/보안점검` 명령으로도
  실행할 수 있습니다"*. 저장소에 그런 슬래시 명령은 없다. 삭제했다.

### README 전면 개편 — 낡은 수치 갱신과 구조 재편 (Docs) — 2026-08-13

문서가 실제보다 뒤처져 있었다: 테스트 배지 1,552(실제 1,589), 벤치마크 30시드
(실제 37), 룰 "219개"(실제 326 = 탐지 99 + 참조 227), 그리고 인텔 자동 동기화
(autopull·`intel-sync`·`GVSKB_INTEL_DIR`)는 기능이 있는데 문서에 없었다.

- 수치를 전부 실측값으로 갱신하고, 룰 수는 탐지/참조를 나눠 표기(총계를 탐지
  능력으로 읽지 않게 — 도구가 이미 나눠 보고하는 것을 문서도 따라감).
- **보고서 스크린샷·텍스트 예시 재생성** — 기존 스크린샷(08-03) 이후 보고서가
  구조적으로 바뀌었다: 배포 판정 3단계(차단은 악성·KEV·CRITICAL 등 5종만),
  결론 박스의 해소 방안·판정 기준표, 발견 등급 '차단'→'필수 조치', 판정 근거
  확신 수준(확인됨·유력함·패턴 일치만), 보고서 자기 위치 표기. eval_corpus 의
  민원 앱 시드 + 취약 의존성 샘플로 실제 스캔을 돌려 현재 화면을 다시 찍었다
  (`report-*-2026-08-13.png` — jsDelivr @main 캐시를 피하려고 파일명 변경).
- 구조를 독자 흐름대로 재편: 하는 일 → 차별점 → 보고서 화면 → 설치(3단계) →
  사용 방법 → 결과 읽는 법 → 탐지 범위 → 성능·한계 → 오탐 → 망분리 → CI.
- 성능 표에 known gap 15건·경계 프로브 미탐 5건을 명시(한계 공개 원칙 유지).
- MCP 연결 실패 유형 3가지를 "자주 겪는 문제"로 정리.
- 회귀 테스트 수정: README MCP 스니펫 검사가 개수(`>= 3`)를 고정하고 있어
  중복 스니펫 정리(3→2)를 오탐으로 막았다(CI에서 발견). 지키려던 불변식대로
  재작성 — 스니펫이 최소 1개 존재하고, 존재하는 **모든** `"command"` 가
  gvskb-server 인지 검사한다(개수 무관).
- 2차 보완(같은 날): README 는 사용자(공무원)가 알아야 할 내용만 담는다는
  기준으로 내부 서술을 제거 — 측정 방법론 해설("한계를 숨길 유인"), 룰 정밀화
  절차, 인텔 초안 카드 승격 파이프라인, 벤치마크 재현 명령어 등은 사용자
  문서가 아니라 개발 문서의 몫. '성능과 한계'는 결과와 한계만, '오탐'은
  사용자가 할 일 3단계(확인→예외 등록→제보)로 재구성.

### 판정은 맞았는데 안내가 담당자를 엉뚱한 곳으로 보냈다 (Fixed) — 2026-08-13

인텔 캐시가 실제로 쌓이고 판정에 반영되는지 실측으로 훑다가 나왔다. 캐시 자체는
건전했다 — 4개 출처(KEV 1,656 · OSV-malicious 11,582 · NVD 2,000 · EPSS 657) 모두
`sha256` 재계산이 기록값과 일치하고 `item_count` 도 실제 건수와 맞았다. 양성 대조군
(`ascii2text` → `malicious`), 음성 대조군(`requests` → CVE 미확인이라 `unknown`),
커버리지 공백(npm → `unknown`) 세 갈래도 의도대로 갈렸다. 변조 시험 — 사본 캐시에서
악성 항목 1건만 지우고 `sha256` 필드는 그대로 뒀더니 `load()` 가 캐시를 거부하고
판정이 `malicious → unknown` 으로 떨어졌다. **'깨끗함'으로 조용히 새지 않았다.**

**문제는 그 다음 문장이었다.** 커버리지 공백 안내가 원인과 무관하게 언제나
*"npm까지 받으려면 `GVSKB_OSV_INCLUDE_NPM=1` 을 설정하세요"* 였다. 그런데 위 변조
시험에서 **PyPI 를 물어본 결과에 npm 을 켜라는 안내가 붙었다**. PyPI 는 항상 기본
수집 대상이라 그 변수와 아무 관계가 없고, 실제 원인은 osv 캐시가 없거나 무결성
검증에 걸려 버려진 것이다. 담당자가 안내대로 npm 을 켜고 다시 받아도 **공백은 그대로
남는다.** 판정이 맞아도 조치로 이어지지 않으면 그 판정은 쓰이지 않는다.

- `_coverage_gap_note()` 로 분리하고 원인별 세 갈래로 갈랐다.
  ① osv 캐시 없음/무결성 실패 → `update-intel` + `intel-bundle` 반입 안내
  (파일이 있으면 *"무결성 검증에 실패해 무시되었습니다"* 로 사유를 구분한다 —
  파일이 눈에 보이는데 "캐시가 없습니다"라고만 하면 담당자가 안내를 신뢰하지 않는다).
  ② opt-in 생태계(npm) → 해당 환경변수 안내 유지.
  ③ 기본 수집 대상인데 안 담김 → 캐시 불완전, 갱신 후 `doctor` 로 커버리지 재확인.
- `osv.py` 의 `DEFAULT_ECOSYSTEMS`·`OPT_IN_ECOSYSTEMS` 를 공개했다. 어느 생태계가
  opt-in 인지 소비자가 알 수 없어서 문구에 `npm` 을 박아 둔 것이 원인이었다.

**적대적 검증** — 반대 방향도 함께 고정했다. npm 조회에서 `GVSKB_OSV_INCLUDE_NPM=1`
안내가 **사라지지 않는지**(과교정), 무결성 실패 캐시로 악성 판정이 **살아나지 않는지**
(캐시를 버렸으므로 `unknown` 이어야 한다) 검사한다. 회귀 테스트 3건 추가
(1,586 → 1,589 통과).

### 파일 단위로 고친 실패를 디렉터리 단위에서 그대로 반복했다 (Fixed) — 2026-08-10

공공 바이브코딩 보안 포털을 검사하다, 이 도구가 **유효한 와일드카드 TLS 개인키
6사본을 한 건도 보고하지 않은 것**을 발견했다. 능력이 없어서가 아니다.
`.pem`·`.key` 는 이미 검사 대상 확장자였고(`DEFAULT_INCLUDE_EXTS`), 확장자 목록
밖의 키 파일을 건지는 `_is_secret_filename` 우회까지 있었다. 그 장치는 국면ⅩⅤ에서
*"ssl/ 디렉터리의 개인키가 스캔·제외 어디에도 없이 사라졌다"* 를 겪고 넣은 것이다.

**그런데 똑같은 `ssl/` 개인키가 똑같이 사라졌다 — 한 단계 위에서.**
`os.walk` 는 `dirnames` 를 **먼저** 쳐낸다. 제외된 디렉터리 안의 파일은 그
안전장치가 있는 지점에 **도달조차 하지 못한다**. 파일 단위 방어가 디렉터리 단위
프루닝을 이기지 못했다.

**① `tmp` 를 빌드 산출물에서 분리했다.** `tmp`·`.tmp` 가 `.turbo`·`.parcel-cache` 와
함께 `_BUILD_OUTPUT_DIR_NAMES` 에 묶여 *"빌드 산출물(압축/번들) — 원본 소스 아님"*
으로 기록되고 있었다. 그러나 일반명 `tmp/` 는 빌드 캐시가 아니라 **파일 업로드를
받는 앱이 남의 데이터를 쌓아 두는** 관례적 위치다. 실측한 포털에서 `tmp/scan-targets/`
는 정확히 업로드 격리 폴더였고, 그 안에 타 기관 프로젝트 3벌(8,522파일)과 개인키
6사본·세션 서명키 3사본이 있었다. 즉 이 도구는 **가장 위험한 자재가 쌓이는 곳을
골라서** 보지 않았다.

- `_DATA_STAGING_DIR_NAMES` 신설. 소스 룰 검사에서는 계속 제외하되,
  `_sweep_excluded_dir()` 가 그 안을 **열지 않고** 걸어 개인키·키스토어를 건져 검사한다.
- 과잉 교정 방지 — 중첩된 `.venv`·`node_modules` 안은 **세기만 하고** 자재 후보로
  보지 않는다. `certifi/cacert.pem` 같은 공개 CA 번들이 프로젝트마다 딸려 와
  진짜 신호를 덮기 때문이다. `_SECRET_MATERIAL_EXTS` 도 개인키·키스토어로 좁혔다
  (`.crt`·`.cer`·`.der` 는 공개 인증서라 제외).

**② 제외 디렉터리가 규모를 숨기고 있었다.** 파일 단위로는 *"검사 제외 953건"* 을
사유별로 정직하게 분해하면서, 디렉터리는 **몇 천 건이든 1건으로** 세고 있었다.
읽는 사람은 `1,005 검사 + 953 제외` 를 실제 1만여 파일과 대조할 방법이 없었다.

```
전: tmp/ → 빌드 산출물(압축/번들) — 원본 소스 아님
후: tmp/ → 임시·업로드 디렉터리 — 8522개 파일 미검사 · 비밀 자재 21건은 검사했습니다
```

**③ 자기 참조 가드가 이름 하나에만 걸려 있었다.** 보고서에는 증거 문구(PEM 헤더 등)가
인용돼 있어 재검사하면 발견이 증식한다. 이미 알고 `.check-reports` 를 제외해 뒀지만
**디렉터리 이름 기준**이었다. 산출물을 `reports/` 에 쓰는 포털에서는 그대로 우회돼,
전체 발견 49건 중 **24건(49%)이 과거 보고서에서 나온 에코**였고 그중 CRITICAL 이 16건이었다.
최고 심각도 판정의 절반이 자기 출력물이었다는 뜻이다.

- `_looks_like_gvskb_report()` — 폴더 이름이 아니라 **내용**으로 판단한다
  (JSON: `ruleset_digest`+`engine_version` 조합 / HTML: 문서 제목).
- 첫 구현은 앞 4,000자만 봤고, 그래서 **가장 큰 보고서 하나를 놓쳤다** — 필드 순서상
  거대한 `findings` 배열이 먼저 나와 마커가 72,070자 지점에 있었다(실측 131KB 파일).
  에코 15건이 그대로 남았다. 전체 텍스트 검색으로 고치고 회귀 테스트로 고정했다.
  HTML 은 제목이 구조상 앞에 오므로 윈도를 유지한다 — 본문 전체를 뒤지면 그 문구를
  **화면에 띄우는 UI 소스**까지 보고서로 오인해 진짜 사각지대가 생긴다.

**적대적 검증** — 같은 소스를 수정 전/후 체커로 각각 검사해 대조했다.
제품 코드 발견 **9 → 9(신규 오탐 0 · 소실 0)**, 사라진 것은 `reports/` 에코 24건,
새로 생긴 것은 `tmp/` 실제 유출 자재 33건. 검사 파일은 1,041 → 109건으로 줄어 더 빨라졌다.
회귀 테스트 6건 추가(1,580 → 1,586 통과).

### 룰 수 표기가 능력을 과장했다 · 외부 지침 대비 공백을 사실로 고정 (Changed) — 2026-08-10

외부 보안 지침(국가 기관 발간, 2026.8)과 항목 단위로 대조하다 두 가지가 나왔다.

**① `total_rules` 만 보여 주고 있었다.** 룰 326개 중 **탐지 패턴을 가진 것은 99개**다.
나머지 227개(인텔 106 · MOIS-49 50 · NIS-AI 45 · OWASP 20 · 기타 6)는 다른 룰의
`references` 로 인용되거나 카탈로그 역할이며, 실제 점검 보고서에 발행된 이력이 **0건**이다
(외부 저장소 보고서 2건 교차 확인). 총계를 탐지 능력으로 읽으면 과장이다.

- `runtime_detection_rules`(99)와 `reference_only_rules`(227)를 나눠 보고하고,
  `rule_count_note` 로 *"총계를 탐지 능력으로 읽지 마세요"* 를 함께 싣는다.
- 회귀 테스트 `test_rule_count_does_not_overstate_detection_capability` 로 고정.

**② 지침이 요구하는 취약점 유형 22종 중 7종만 탐지된다.** 나머지 15종을 실측하고
`eval_corpus/projects/i_guide_types/`(22개 파일)로 편입했다.

- 탐지되는 7건 → `kind: vulnerability`(회귀 방지)
- 미탐 15건 → **`kind: known_gap`** — 못 잡는 것을 **사실로 고정**한다.
  탐지율(recall)에 넣지 않는다(못 잡는 것이 사실이므로 탐지율을 왜곡한다).
  대신 벤치마크가 `known_gap=N/15` 로 매번 보고하고, **탐지되기 시작하면 알린다** —
  ground truth 를 갱신해야 한다는 신호다.
- 드라이버에 **알 수 없는 `kind` 경고**를 추가했다. 예전 구조에서는 모르는 종류가
  집계에서 조용히 빠졌다.

미탐 15종을 유지하기로 한 판단과 근거는 별도 문서에 남겼다(로컬 보관).
요지: 지침의 절반은 *외부 AI 에 코드를 보내는 전제*라 로컬 도구에 해당하지 않고,
남은 항목 중 8종은 지침 자신이 **동적 점검 영역**으로 분류했으며, 나머지는 실측에서
새로 발견되는 취약점이 0건인 반면 오탐 원인은 확정적으로 존재했다.

테스트 1577 → **1580**.

### 비밀 파일의 값이 보고서에 통째로 실렸다 (Security) — 2026-08-10

외부 저장소(지자체 재난대응 웹앱) 실측에서 나왔다. `.secret_key` 의
Flask 세션 서명키 64자가 보고서 증거란에 **가려지지 않고 그대로** 실렸다.

```
- **증거**: `<64자 hex 세션 서명키 원문 그대로>`
```

그 값이면 세션 위조로 관리자 로그인을 우회할 수 있다. 보고서는
`.check-reports/` 에 저장되고 결재로 올라간다 — **탐지해 놓고 유출본을 한 벌 더
만든 셈**이다. 마스킹 안내문이 바로 그러지 않겠다고 약속하는 자리이기도 하다.

원인은 마스킹의 단서가 전부 **문맥**이라는 점이다. `redact_evidence` 는
`키 = "값"` 꼴, `sk-`·`AKIA` 접두사, 주민번호·전화번호 모양을 본다. 그런데 비밀
파일의 내용은 **접두사도 변수명도 없는 맨 토큰**이라 어느 단서에도 걸리지 않는다.
결과적으로 *맨 비밀값을 찾으라고 만든 룰*(`GOV-SECRET-KEYFILE-001`)의 증거만
유일하게 안 가려졌다.

- `redact_secret_material()` 신설 — 줄 전체를 값으로 보고 `_partial()` 로 가린다.
  `키=값` 이면 **키는 남긴다**(한 파일에 값이 여럿일 때 어느 것을 폐기할지는 키
  이름이 말해 준다).
- **전역에 넣지 않았다.** 룰 정의가 이미 적어 둔 그대로다 — 줄만 봐서는 64자 hex 가
  해시인지 키인지 구분할 수 없다. 전역 적용하면 커밋 해시·체크섬·무결성 값까지
  가려 '늘 켜진 마스킹'이 되고, 그러면 딱지가 정보이기를 멈춘다. 호출부가
  `_is_secret_filename` 과 값 모양을 이미 확인한 **그 자리에서만** 적용한다.
- 회귀 테스트 3건 — 가려지는가 · 키 이름은 남는가 · **일반 증거는 안 가려지는가**.
  마지막 것이 과잉 마스킹 방지 축이다(SHA-256 해시·커밋 해시·PEM 배너로 검증).

변이 검사: 호출을 `_redact_evidence` 로 되돌리면 테스트 2건이 즉시 실패한다.
실측 재검사에서 `.md`·`.html` 양쪽 모두 원문이 사라지고
`77fec856[마스킹]cfac` 로 바뀐 것을 확인했다.

### SBOM 에 검사 PC 의 전체 경로가 실렸다 (Security) — 2026-08-09

`lexdiff` SBOM 을 열어 보니 `metadata.component` 가 이랬다.

```json
"name": "C:\\Users\\<사용자명>\\AppData\\Local\\Temp\\claude\\<세션ID>\\scratchpad\\lexdiff",
"bom-ref": "root:C:\\Users\\<사용자명>\\AppData\\Local\\..."
```

사용자명·세션 ID·디렉터리 구조가 그대로 나간다. **SBOM 은 조달처·발주처로 나가는
반출 문서**이고, 여러 기관 것이 한 곳에 모이면 **남의 기관 내부 구조**까지 함께
모인다.

방어의 방향이 뒤집혀 있었다. 반입 번들은 코드 조각·파일 경로를 허용목록으로
철저히 막는데(`tools/registry_bundle.py`), **더 멀리 나가는 SBOM 쪽이 더 헐거웠다.**

- `sbom.project_name(target, explicit)` 신설 — 경로의 **마지막 구간만** 남긴다.
  드라이브 루트(`C:\`)·빈 값은 `project` 로 떨어진다.
- 막는 자리를 CLI 가 아니라 **문서 생성기(`to_cyclonedx`)** 로 잡았다. 호출자가
  늘어도(MCP·웹사이트) 방어가 따라온다 — 값을 고르는 책임은 소비자가 아니라
  생산자에게 있다(반입 번들과 같은 원칙).
- `--project-name` 추가 — 기관 정식 명칭이 따로 있을 때 쓴다. 사용자가 직접 고른
  이름이므로 쪼개지 않는다(`경기도/법령비교` 같은 값이 상하지 않게).
- `name` 과 `bom-ref` **둘 다** 회귀 테스트로 고정했다. 한쪽만 고치고 다른 쪽으로
  새던 결함이 이 저장소에 이미 있었다.

E2E 로 확인: `AppData`·`scratchpad`·세션 ID·`Users` 가 문서 전체에서 사라졌고,
`--project-name "경기도 법령비교 서비스"` 가 그대로 실린다.

### 보고서 문구 — 자리표시자 `N건`, 중복 경고, 단위 혼용 (Fixed) — 2026-08-09

사용자가 `lexdiff` 보고서를 읽다가 **"'사람이 지은 값'이 뭐냐"** 고 물었다.
문구를 훑어보니 한 곳이 아니라 네 종류였다.

**① 자리표시자가 그대로 나갔다.** 의존성 절 머리의 단위 설명이 이랬다.

```
숫자의 단위 — `취약·악성 N건` 은 패키지 수이고, 표의 `개별 취약점 N건` 은 …
```

`N` 은 라벨 형식을 가리키는 변수였지만, **실제 숫자로 가득한 보고서에서 `N` 만
남아 있으면 치환에 실패한 템플릿으로 읽힌다.** 도구를 설명하려던 문장이 도리어
도구를 못 믿게 만든다. `_DEP_UNIT_NOTE`(상수) → `_dep_unit_note(vuln, components)`
로 바꿔 **이 보고서의 실제 숫자**를 넣는다. 예시는 취약점이 가장 많이 붙은
컴포넌트에서 뽑는다 — 두 단위의 차이가 가장 크게 벌어지는 자리라 한 번에 이해된다.

```
숫자의 단위 — 위의 `취약·악성 37종` 은 패키지 수(조치 단위: 업그레이드할 대상)이고,
아래 표의 `개별 취약점 31건`(`next`) 은 그 패키지 하나에 붙은 보안 권고 수입니다.
```

**② 패키지를 `종`과 `건`으로 번갈아 셌다.** 한 문장 안에서 같은 수가 두 번
세어졌다 — 게이트가 `패키지 48종`, 뒤에 이어 붙는 조각이 `판정 불가 48건`.
보고서 전체에서 **패키지는 `종`, 취약점·발견은 `건`** 으로 통일했다(13곳).
단위 자체가 구분을 담게 되어 ①의 설명 문장이 거의 자명해진다.

**③ 같은 경고를 한 문단에서 두 번 했다.** `_dep_verdict_clause` 가
`판정 불가 N건(‘안전’이 아닙니다)` 을 붙였는데, 게이트는 **차단·경고 두 분기
모두** 에서 이미 그 말을 하고 있었다 — 항상 잉여였다. 곡선 따옴표(`‘ ’`)가
섞여 있던 자리이기도 하다(문서 내 직선 3 : 곡선 1). 괄호째 걷어냈다.

**④ 비유가 뜻을 가렸다.** 마스킹 안내의 `사람이 지은 값`·`유출본을 한 벌 더`·
`직접 여세요`. `짓다` 는 이름·집·시와 붙지 값과는 붙지 않아, 전달하려던
**"기계가 만든 키 vs 사람이 만든 비밀번호"** 대비가 통째로 날아갔다.
`앞 몇 자가 추측을 도와주고` 는 주체가 빠져 **도움받는 쪽이 공격자라는 게 안
보였다** — 문장만 보면 의미가 정반대로 읽힌다. 그리고 "그러니 가려도 확인에는
지장 없다"는 결론이 아예 적혀 있지 않아 문단이 허공에서 끊겼다. 셋 다 고쳤다.

`gate.py` 의 `사유 기록으로 넘어갈 수 없는 등급` 도 다른 곳의
`사유 기록으로 넘길 수 없습니다` 에 맞췄다 — 같은 정책이 두 표현이면 다른
얘기로 읽힌다.

문구를 문자열째 고정하던 테스트 7곳을 함께 갱신했다(1572 통과). 실측 `lexdiff`
감사 데이터로 재렌더해 네 항목을 눈으로 확인했다.

### 판정 문구가 게이트를 따르게 했다 (Fixed) — 2026-08-09

바로 앞 변경(판정 사다리)에서 `gate.py` 만 고치고 **본문 문구를 두고 왔다.**
`koica-reg-mcp` 보고서가 한 화면 안에서 두 말을 했다.

```
15행  차단 사유는 없습니다 … (조건부 승인)
68행  소스 코드만 고치면 배포 차단이 풀리지 않습니다
```

판정을 세 군데서 따로 계산하고 있었다 — `gate_status`(새 기준),
`summary.blocked`(발견 단위 등급), 그리고 **의존성 감사의 옛 `blocked`
플래그**(사다리 이전 기준: 취약하면 곧 차단). 뒤의 둘이 옛말을 하고 있었다.

판정별로 렌더해 '차단'이라 말하는 줄을 기계로 뽑아 **어긋난 다섯 자리**를 찾았다.

| 자리 | 예전 | 지금(조건부일 때) |
|---|---|---|
| 조치 순서 · MD/HTML | 소스만 고치면 **배포 차단이 풀리지 않습니다** | 소스만 고쳐서는 **조치가 끝나지 않습니다** |
| 조치 순서 단 이름표 | (배포가 차단됩니다) | (사유 기록으로 넘길 수 없는 등급) |
| 수정 프롬프트 경고 | **패키지 블록**을 빠뜨리지 마세요 | **패키지**를 빠뜨리지 마세요 |
| 수정 프롬프트 머리표 | `[차단]` | `[조치 필요]` |
| 의존성 절 요약 | **차단 권고** | **업그레이드 필요(조건부 승인)** |

배포 결과를 말하는 문장은 이제 `_deploy_blocked()` **하나만** 거친다. MD·HTML 이
각자 적어 두었던 문장은 한 함수로 합쳤다 — 각자 적어 둔 것이 한쪽만 따라간
원인이었다.

**완화가 침묵이 되지 않게** 조치 가이드 리드를 셋으로 갈랐다: 차단(배포 금지) ·
**필수 조치**(`decision=block` 등급이 있으나 배포는 조건부) · 권고.

죽은 코드 `_verdict_line`·`_verdict_css_color` 삭제 — 호출되지 않으면서 사다리
이전 기준을 담고 있었다.

#### 후속 — 화면에 마크다운이 그대로 나오고 있었다

위 스윕이 `lead`·`depwarn` **두 클래스만** 봤다. 실제 보고서에는 별표 6개·백틱
4개가 화면에 찍혀 있었고, 하나는 **판정 상자의 해소 방안**이었다.

```
<li>`mcp 1.8` → **1.28.1 이상**으로 올리세요</li>
```

원인은 HTML 쪽 관행이 `_esc(text).replace("**", "")` — 강조를 **지우는** 방식.
지우기를 잊으면 새고, 지우면 같은 문장이 MD 와 HTML 에서 달라진다.
`_md_inline_to_html`(강조 + 백틱→`<code>`)로 **일곱 자리**를 옮겼다: 판정 상자
본문 · 해소 방안 목록 · 조치순서 안내 ×2 · 심각도 기준표 · 의존성 단위 안내 ·
실행환경 등급 · 생략 파일 표.

스윕은 복사용 프롬프트 블록(AI 에 붙여넣는 평문)만 걷어내고 **문서 전체**를
훑는다. 픽스처에 제외 파일을 넣고, 실제 `scan_path` 결과를 렌더해 훑는 케이스도
추가했다 — 합성 픽스처는 아는 문장만 만든다.

변이 **21/21 검출**(누적) · 테스트 1552 → **1568** · 벤치마크 recall 100%·FP 0
불변 · 실측 화면 별표 0·백틱 0.

#### 후속² — 발견 등급을 '차단'이라 부르지 않는다 (Changed)

조건부 승인 보고서 요약에 `차단(block): 1건` 이 찍혀 있었다. 판정 상자는
"조건부 승인" 인데 그 아래 "차단 1건" 을 읽으면 막힌 건지 알 수 없다.

| 자리 | 예전 | 지금 |
|---|---|---|
| 요약 줄 | 차단(block): N건 | **필수 조치: N건** |
| HTML 통계 | 차단(block) | **필수 조치** |
| 발견 뱃지 | 치명·차단 | **치명·필수 조치** |
| 수정 프롬프트 | `[치명/차단]` | **`[치명/필수 조치]`** |
| LLM 처리 순서 | (차단 → …) | **(필수 조치 → …)** |

**기계 값은 그대로다** — `decision: "block"` · `by_decision["block"]` ·
`--fail-on block` · SARIF. 하네스·레지스트리가 읽는 계약이다.

`_hero_line`(세 번째 죽은 판정 함수) 삭제. 같은 결함을 세 번 만났으므로 이제
**`report.py` 안에서 `.blocked` 를 읽는 곳이 하나도 없어야 한다**는 것을 AST 로
검사한다 — 죽은 코드는 화면에 안 나오므로 출력 검사로는 못 잡는다.

변이 **30/30 검출**(누적) · 테스트 1568 → **1572** · 벤치마크 불변.

### 배포 판정을 셋으로 나눴다 (Changed) — 2026-08-09

**BREAKING** — `blocked` 의 뜻이 좁아졌습니다. 아래 '필드 계약' 참조.

예전 기준은 **CVSS HIGH 하나라도 있으면 차단**이었다. 실측 4개 저장소가 전부
차단됐고, 차단 근거는 **100% `high` 등급 하나** — 악성 0건, KEV 0건,
CRITICAL 0건. 가장 강한 신호 셋은 한 번도 발동한 적이 없는데 가장 흔한 신호와
결과가 같았다.

> **차단이 예외가 아니라 기본값이 되면, 그것은 더 이상 신호가 아니다.**

게다가 `noti.kpetro` 의 `xlsx 0.18.5` 는 **수정본이 없다.** 담당자가 게이트를
만족시킬 방법이 없었다 — 만족시킬 수 없는 게이트는 우회되거나 꺼진다.

| 판정 | 조건 |
|---|---|
| **차단** | 악성 패키지 · 기관 레지스트리 거부 · 레지스트리에 없는 이름 · **CISA KEV 등재** · **CVSS CRITICAL** |
| **조건부 승인** | CVSS HIGH 이하 · 발행 직후 버전 · 판정 불가 · **소스 발견 전부** |
| **승인** | 조치할 항목 없음 |

실측: 차단 **4 → 1**. 남은 하나는 `lexdiff` 의 `protobufjs`·`vitest`(진짜 CRITICAL).

#### 소스는 왜 차단하지 않는가

라운드 13의 원칙 — **소스는 보조, 의존성은 게이트**. 의존성은 *"이 버전에 이
CVE 가 있다"* 는 사실 조회라 오탐이 구조적으로 없고, 소스 룰은 추론이라 맥락을
모르면 틀린다. 의존성 차단을 좁히면서 소스를 그대로 두면 **원칙이 뒤집힌다**.

**대신 비밀값·개인정보 노출은 결론 박스 앞으로 끌어냈다.** 조건부라고 조용히
넘어가면 안 되는 것이 있다 — 노출된 키는 코드에서 지우는 것만으로 끝나지 않고
Git 이력에 남으므로 **반드시 재발급**해야 한다.

#### 결론 박스가 해소 방안과 판정 기준을 함께 말한다

예전 박스는 *"차단 기준에 걸린 패키지가 있습니다"* 라고만 적었다. **어떤
기준인지 알 수 없었고**, 무엇을 하면 되는지도 없었다.

```
> ### 조건부 승인 (검토 후 배포)
>
> **배포 판정** · 차단 사유는 없습니다. 확인이 필요한 항목이 있습니다…
>
> **해소 방안**
> - `mcp 1.8` → **1.28.1 이상**으로 올리세요
>
> **이 판정의 기준**
> | 차단 | 악성 · 기관 거부 · 미존재 · CISA KEV · CVSS CRITICAL |
> | **조건부 승인** | CVSS HIGH 이하 · 발행 직후 · 판정 불가 · 소스 발견 ← **이 보고서** |
> | 승인 | 조치할 항목 없음 |
```

#### 필드 계약

`blocked` 만 읽던 소비자(연동 하네스·레지스트리)는 예전에 막던 것을 이제
통과시킨다. 조용히 느슨해지지 않도록 둘을 둔다.

- **`verdict`** — `blocked` | `conditional` | `approved` | `undetermined`
- **`requires_action`** — 차단이든 조건부든 **할 일이 남았는가**.
  예전 `blocked` 의 "뭔가 조치가 필요하다"는 뜻을 그대로 옮겨 담는다.

CLI 기본값 `--fail-on warn` 은 그대로라 **CI 는 여전히 발견이 있으면 실패**한다.
느슨해지는 것은 `--fail-on block` 을 명시한 경우뿐이다.

`max_cve` 와 `advisories[].severity` 를 **둘 다** 본다 — 한쪽 필드에만 걸면
advisory 목록이 비거나 잘린 경로에서 CRITICAL 이 조용히 통과한다.

검증: 변이 **15/15 검출**(차단 기준 다섯 개를 하나씩 지워 전부 검출) ·
테스트 1538 → **1552** · ruff OK.

### 보고서를 어디에 모을지 사용자가 정한다 (Added) — 2026-08-09

> *"점검 파일을 다운 받는 위치에 저장하는거지? 근데 찾기가 너무 어려워.
> 별도로 폴더를 지정하게 해주면 좋겠어."*

기능이 **있기는 했다** — `GVSKB_REPORT_DIR` 환경변수. 그런데 Windows 에서
환경변수를 설정하려면 시스템 속성 창을 열어야 한다. 공무원이 쓸 수 있는
방법이 아니었으니 **사실상 없는 기능**이었다.

```bash
gvskb config --report-dir "D:\보안점검"       # 한 번만 지정하면 계속 적용
gvskb config                                   # 지금 어디에 저장되는지 + 그 근거
gvskb config --clear-report-dir                # 기본값으로 되돌리기
gvskb scan <경로> --report-dir "D:\임시"       # 이번 한 번만
```

우선순위: `-o` → `--report-dir` → `GVSKB_REPORT_DIR` → 설정 파일 → 기본값.
`gvskb config` 는 **아무것도 바꾸지 않아도 실제로 쓰일 경로와 그 근거를 찍는다** —
설정만 바꾸고 결과를 안 보여주면 사용자는 또 어디 저장되는지 모른다.

#### 공용 폴더로 모으면 파일명에 사업 이름이 붙는다

지정 기능만 붙였으면 곧바로 다음 문제를 만들 뻔했다. 파일명이
`2026-08-09_1745_보안점검` 뿐이라 **여러 부서가 한 폴더에 쌓는 순간 어느 사업
보고서인지 알 수 없다.** 공용 폴더일 때만 `2026-08-09_1745_민원챗봇_보안점검` 로
바꾼다(프로젝트 옆에 둘 때는 폴더가 이미 맥락이므로 기존 이름 유지).

`gvskb doctor` 에도 저장 위치 항목을 넣었다 — *"내 보고서가 어디로 가지?"* 는
검사하기 **전에** 물어볼 수 있어야 하는 질문이다.

문서 결함도 함께 고쳤다: README 가 *"`-o` 를 빼면 결과가 화면에만 출력됩니다"*
라고 적고 있었는데, 이미 자동 저장으로 바뀐 지 오래였다.

검증: 변이 **11/11 검출** · 테스트 1524 → **1538** · ruff OK.

### 보고서가 자기 위치를 말한다 (Added) — 2026-08-09

사용자 지적:

> *"항상 정확한 파일 위치를 알려줘. 사용자들에게 마찬가지야. 최종 점검 후에는
> 파일위치 풀 경로를 제공해줘야해 **사용자들은 몰라. 기억을 못해.**"*

저장 경로는 CLI 의 stderr 한 줄과 MCP 응답의 `saved` 필드로만 나갔다. 그 줄을
놓치거나, 나중에 파일만 전달받거나, 며칠 뒤 다시 찾는 사람은 **원본이 어디
있는지 알 방법이 없었다.** 결재로 올라가고 감사에 인용되는 문서가 자기 출처를
말하지 않고 있었다.

머리말 표에 한 줄을 넣는다 — Markdown·HTML 양쪽, CLI·MCP 양쪽 모두.

```
| 이 보고서 위치 | C:\...\프로젝트\.check-reports6-08-09_1731_보안점검.md |
```

화면으로만 흘려보낼 때(`--stdout`)는 저장 경로가 없으므로 **적지 않는다** —
없는 것을 있다고 쓰지 않는다.

테스트는 함수 서명이 아니라 **실제로 저장된 파일을 다시 열어** 경로가 들어
있는지 본다(렌더러만 고치고 호출부를 안 고치는 실수를 잡는다).

검증: 변이 **5/5 검출** · 테스트 1519 → **1524** · ruff OK.

### 남의 저장소 하나가 오탐 셋을 찾아냈다 (Fixed) — 2026-08-09

공공기관 규정 MCP 서버(`koica-reg-mcp`, 176파일)를 검사했더니 소스 발견 3건이
나왔고 **셋 다 오탐**이었다. 그중 둘은 `치명·차단` 이었다.

#### SQL 식별자 조립 — 스코프 안만 봐서는 알 수 없는 것

```python
def _bump(conn, table, key_col, key, now):
    conn.execute(f"INSERT INTO {table}({key_col}, count) VALUES(?, ?)", (key, now))
...
_bump(conn, "tool_usage", "tool", tool, now)      # 호출부 넷 전부 리터럴
```

값은 전부 `?` 바인딩이고 f-string 이 끼워 넣는 것은 **테이블·컬럼 이름뿐**이다.
SQL 식별자는 파라미터로 넘길 수 없으니 이 조립은 정당하다.

상수 판정이 스코프 안만 봐서 매개변수의 출처를 알 수 없었다. 이제 모듈 전체를
먼저 훑어 **모든 호출부가 리터럴을 넘기는 매개변수**를 증명한다. 증명 조건을
좁게 잡았다 — 사설 함수(`_`)·정의 하나·이름이 값으로 새지 않음·호출부 1곳 이상·
언팩(`*args`) 없음. **하나라도 못 채우면 증명 실패로 두고 예전처럼 잡는다.**

#### 추적하지 않은 것을 "추적했다"고 말하고 있었다

같은 발견의 판정 근거가 `confirmed` — 보고서에는 **"확인됨(데이터 흐름 추적)"**
으로 찍힌다. 매개변수의 값이 어디서 오는지는 추적한 적이 없다.

> **오탐보다 이쪽이 무겁다.** 틀린 판정은 검토로 걸러지지만,
> 틀린 확신은 검토를 건너뛰게 만든다.

동적 부분이 매개변수뿐이면 `likely`("유력함 · 문맥 확인 권장"), 같은 스코프에서
대입을 따라간 것은 `confirmed` 로 나눈다. DDL 룰이 `likely` 인 것과 같은 기준이다
— **'공격자 통제 증명'이 아니라 '흐름을 따라갔는가'**.

#### 자원을 넘겨주는 자리에는 `with` 를 요구할 수 없다

```python
def _connect(path):
    conn = sqlite3.connect(path, timeout=5.0)
    return conn                  # ← 주인이 호출자로 넘어간다
```

생성 함수는 애초에 `with` 를 쓸 수 없다 — 블록을 벗어나면 닫혀서 반환값이
죽는다. **해제 책임이 있는 곳에 요구할 규칙을 책임이 없는 곳에 요구**하고
있었다. 자원을 만들어 그대로 반환하면 `low` 로 낮추고 **호출부를 보라**는 사유를
남긴다(삭제하지 않는다 — 호출자가 정말 닫는지는 이 자리에서 알 수 없다).

검증: 변이 **11/11 검출** · 적대적 13건 · 실측 3개 저장소 **차단 3건 불변 ·
신규 0 · 소실 0 · 판정 변경 0** · 정확도 벤치마크 recall 100% · FP 0 **불변** ·
테스트 1498 → **1519** · ruff OK.

### 도구가 문서를 고치게 만들지 않는다 (Fixed) — 2026-08-09

실사용 제보로 시작했다.

> *"적대적 검증 중 체커가 새 YAML 문구를 LLM 출력 실행 위험으로 오탐 차단했는데,
> 문장형 설명을 구조화 필드로 바꿔 제거했습니다."*

**진짜 피해는 차단이 아니라 그다음이다** — 담당자가 도구를 통과시키려고 문서를
고쳤다. 확인해 보니 결함이 하나가 아니었고, **제보되지 않은 쪽이 더 무거웠다.**

#### 주민등록번호가 TypeScript 에서 미탐이었다

`GOV-PII-RRN-001` · `GOV-SECRET-APIKEY-001` · `GOV-INTERNAL-NET-001` 이
`languages` 목록에 typescript 가 없어 `.ts`·`.tsx`·`.go`·`.cs`·`.php`·`.rs`
에서 **한 번도 돌지 않았다.** 공공 웹앱의 주력 언어가 TypeScript 다.

노출 위험은 언어를 가리지 않는다 — 주민등록번호는 Go 로 적으나 Rust 로 적으나
주민등록번호다. **값의 모양만 보는 룰에 언어 제한을 단 것 자체가 잘못된 설계**라
셋 다 해제했다. 같은 구멍이 다섯 번 났으므로 린터를 붙였다: **javascript 는
있는데 typescript 가 없는 실행형 룰이 있으면 CI 가 선다**(TS 는 JS 의 상위집합
이라 'JS 에는 맞고 TS 에는 안 맞는' 룰은 존재할 수 없다).

#### 노출 카테고리 집합이 두 곳에 복제돼 둘 다 같은 구멍

주석 건너뛰기 예외와 테스트 경로 감쇄 대상이 각각 따로 적혀 있었고, **둘 다**
`public-sector-internal` 이 빠져 있었다. 위 언어 제한을 풀자 테스트 픽스처의
사설 IP 48건이 **차단**으로 올라왔다.

`schema.EXPOSURE_CATEGORIES` 한 곳에서 정의하고 세 자리가 그것을 쓴다.

#### "이렇게 하지 마세요"라고 말하는 줄

보안 가이드·룰 설명·인수인계 문서는 자기가 금지한 토큰을 문장 안에 담는다.
`description: LLM 응답을 그대로 실행(execute)하지 말고 검증하세요` 가 '치명·차단'
이었다 — **도구가 자기가 하라고 쓴 문장을 막은 것이다.**

금지·주의 문구가 같은 줄에 있으면 실행형 발견을 `low·warn` 으로 **낮춘다
(삭제하지 않는다).** 두 가지를 지켰다.

- **노출 위험에는 적용하지 않는다.** "비밀번호를 하드코딩하지 마세요:
  password = hunter2" 는 하지 말라고 적혀 있어도 비밀번호가 그대로 노출돼 있다.
  의도가 위험을 지우지 못한다.
- **데이터 파일이라고 실행형 룰을 끄지 않는다.** 처음엔 그렇게 설계하려다
  실측에서 걸렸다 — `package.json` 의 npm 라이프사이클 스크립트와 CI 워크플로의
  `run:` 은 **데이터 파일에 담긴 실행 코드**다.

#### TypeScript 를 열자 드러난 자리 둘

- `access_token: accessToken` (TS 객체 리터럴)이 시크릿으로 차단됐다.
  값이 키 이름 그 자체면 변수 참조다 — 실제 비밀값이 자기 키 이름과 같을 수는
  없으므로 이 제외가 진짜를 가릴 위험이 없다.
- `tools.delete(basic)` 이 '에이전트 과잉 권한'으로 차단됐다. `tools` 는
  `Set<string>` 이다. 룰이 이미 DOM API 를 빼고 있었는데 JS 표준 **컬렉션**
  API 는 빠져 있었다 — 같은 부류의 형제 자리.

검증: 변이 **12/12 검출** · 적대적 16건(우회 0 · 소실 0 · 경계 오판 0) ·
실측 3개 저장소 **차단 3건 불변 · 신규 차단 0 · 소실 0 · 판정 변경 0**
(발견 55 → 114 는 전부 TS 파일에서 새로 보이게 된 것, 테스트 경로라 `low·warn`) ·
테스트 1393 → **1498** · ruff OK · 룰셋 2026.08.6 → **2026.08.9**.

### 담당자가 조치할 수 있는 보고서 (Changed) — 2026-08-09

사용자가 물었다. *"보고서 증거를 마스킹해서 보여주면 담당자는 뭐가 노출됐는지
어떻게 아나? 어차피 담당자잖아."* 세 질문에서 **결함 여섯**이 나왔고,
그중 둘은 사용자가 쓰는 경로가 조용히 절단되고 있던 것이었다.

#### 증거를 알아볼 수 있게 — 부분 마스킹

`***REDACTED***` 로 통째로 가리면 **한 파일에 키가 여러 개일 때 어느 것인지
구분할 수 없다.** 조치로 이어지지 않는 증거는 증거가 아니다. 그렇다고 원문을
실으면 이 보고서가 유출본을 한 벌 더 만든다 — `.check-reports/` 에 파일로
저장되고 결재로 올라가고 감사로그에 남는 문서다.

```
전: OPENAI_API_KEY = "***REDACTED***"
후: OPENAI_API_KEY = "sk-proj-[마스킹]wxyz"     ← 식별은 되고 사용은 불가
```

- 노출 상한은 **앞 ¼(최대 8자) + 뒤 ⅛(최대 4자)** — 어떤 길이에서도 ⅜을 넘지 않는다.
  짧은 값은 비율로 따져도 남는 부분이 커지므로 통째로 가린다.
- **비밀번호만 예외로 통째로 가린다.** API 키는 기계가 만든 고엔트로피 값이라
  앞 몇 자로 나머지를 좁힐 수 없지만, 비밀번호는 사람이 지어 저엔트로피다 —
  `P@ss…` 넉 자가 추측을 실질적으로 돕는다. 어느 비밀번호인지는 변수명이
  이미 말해 주므로 부분 노출로 얻는 것도 없다.
- 주민등록번호(`900101-1******`)·휴대전화(`010-****-5432`)는 **국내 관행 그대로**
  둔다. 눈에 익은 모양이어야 읽힌다.
- **가린 것이 있을 때만 '가렸다'고 말한다.** 예전에는 모든 증거에 '자동
  마스킹됨' 딱지가 붙었다 — `eval(x)` 옆의 그 딱지를 본 담당자는 무엇이
  가려졌는지 찾다가 지치고, 그러면 진짜 가려진 자리에서도 딱지를 읽지 않는다.
  마스킹 규칙 자체도 보고서에 한 번 설명한다.

#### 파일 상한 — 고쳤다고 믿었지만 사용자에겐 그대로였다 (Fixed)

지난 국면에서 `DEFAULT_MAX_FILES` 를 500 → 20,000 으로 올렸는데,
**CLI 와 MCP 에 따로 적어 둔 500 을 그대로 뒀다.** 라이브러리만 검사하던
테스트는 전부 통과했고, 실제로는 570개짜리 저장소에서 70개가 조용히 빠졌다.

이번에는 상수를 재선언하지 않고 라이브러리 값을 **가져다 쓴다.** 같은 숫자를
두 곳에 적으면 언젠가 어긋난다. 회귀 테스트도 상수가 아니라 **진입점의
기본값과 실제 501번째 파일**을 본다.

#### 내가 만든 중복 룰 (Fixed)

`GOV-PII-PHONE-001` 이 최초 커밋부터 있는 줄 모르고 `GOV-PII-CONTACT-001` 을
새로 만들었다. 같은 전화번호 한 줄에 **두 건이 발행**됐다. 회귀 코퍼스가 못
잡은 이유는 기존 룰의 `languages` 에 typescript 가 없어 `.ts` 코퍼스에서
애초에 겹치지 않았기 때문이다 — **진짜 결함은 그 언어 구멍이었고 새 룰이
아니라 그것을 고쳤어야 했다.**

새 룰의 개선(점·숫자 경계 가드, 더미 제외 패턴, 언어 무제한)을 기존 룰이
흡수하고 그쪽을 지웠다. 재발은 **룰 린터의 중복 커버리지 검사**가 막는다 —
같은 카테고리 두 룰이 서로의 positive 예시를 전부 잡으면 CI 가 선다.
326개 룰 전체에서 린터 오탐 0건.

#### `**` — 오탐은 없었고 미탐이 있었다 (Fixed)

*"AI 코딩툴이 강조할 때 쓰는 `**` 가 위험으로 잡히나?"* — **잡히지 않는다.**
27가지를 쳐서 오탐 0건이었다(`2 ** 10`·`**kwargs`·`***` 마스킹 표시 전부 통과).

문제는 반대편이었다. 강조가 키 이름을 감싸면 `키워드 다음 구분자` 모양이 끊겨
**아홉 형태가 통째로 미탐**이었다 — 강조를 떼면 전부 잡히던 것들이다.

```
# **password** = P@ssw0rd2026        전: 미탐 → 후: 탐지
**DB_PASSWORD**=gg2026Secure         전: 미탐 → 후: 탐지
```

짝이 맞을 때만 받아들인다(`(\*\*)?…(?(1)\*\*)`) — 그냥 열어 두면 파이썬
거듭제곱 대입 연산자 `password **= …` 가 걸린다.

**형제 자리를 손으로 훑다 세 번 어긋났다.** 그래서 그만두고 기계에 맡겼다:
모든 비밀값 룰의 positive 예시에 강조를 씌워 판정이 뒤집히는 곳을 찾는 스윕을
테스트로 붙였다. 처음 짠 스윕은 컴파일된 룰을 읽어 **0개를 검사하고 "이상
없음"** 을 냈다 — 그래서 검사 건수 하한 단언을 함께 넣었다.
**검사를 안 하고 통과하는 것이 가장 나쁜 통과다.**

검증: 변이 **14/14 검출** · 적대적 36건(오탐 0·미탐 0) · 실측 3개 저장소
1,004파일에서 **신규 0 · 소실 0 · 판정 변경 0** · 테스트 1302 → **1393** ·
룰셋 2026.08.4 → **2026.08.6** · ruff OK.

### SBOM 을 만들고 읽는다 (Added) — 2026-08-09

우리 룰과 문서는 *"SBOM·의존성 매니페스트로 확인하세요"* 라고 **안내는 하면서
정작 도구는 SBOM 을 만들지도 읽지도 못했다.** 공공 조달이 SBOM 제출을 요구하기
시작한 상황에서 실질적인 공백이었다.

- **만들기** — `gvskb scan --check-deps --sbom <경로>` 로 CycloneDX 1.6 을 저장한다.
  purl · 버전 · 라이선스 · 취약점(OSV 출처·CVSS 벡터·권고 버전)까지 옮긴다.
  실측 `noti.kpetro`: **컴포넌트 540개 · 취약점 41건**.
- **읽기** — `gvskb sbom <파일>` · MCP `scan_sbom`. CycloneDX/SPDX **JSON** 을
  받아 컴포넌트를 그대로 검사한다. 조달·협력사가 SBOM 만 제출하는 경우,
  소스 없이도 취약점을 대조할 수 있다.

**형식 적합성보다 정직성을 먼저 지켰다.** SBOM 은 "무엇이 들어 있나"를 증명하는
문서라, 빠진 것이 곧 "없는 것"으로 읽힌다.

| 규약 | 왜 |
|---|---|
| 판정 못 한 컴포넌트도 **싣고** 사유를 `properties` 에 남긴다 | 빼면 "그 패키지를 안 쓴다"로 읽힌다 — 없는 것과 안 본 것은 다르다 |
| 읽지 못한 항목을 **버리지 않고** `skipped` 에 사유와 함께 돌려준다 | purl 없음·`NOASSERTION` 은 '안전'이 아니라 '보지 못함'이다 |
| 엔진 버전과 **룰셋 버전을 쌍으로** 각인한다 | 한쪽만 있으면 재현 가능한 것처럼 보이는 착시가 생긴다 |
| 파일 해시를 **만들지 않는다** | 우리는 메타데이터만 조회한다. 빈 해시는 "확인했는데 없다"로 읽힌다 |
| serial 을 내용에서 유도해 **결정적**으로 만든다 | 난수 UUID 면 같은 입력에 매번 다른 문서가 나와 두 SBOM 을 비교할 수 없다 |
| 의존성 검사 없이 `--sbom` 만 주면 **쓰지 않고 알린다** | 컴포넌트 0개짜리 SBOM 은 "의존성이 없다"로 읽힌다 |

검증: 변이 **15/15 검출** · 테스트 1269 → **1302** · ruff OK.

### 게이트가 되기 위한 네 가지 (Added/Fixed) — 2026-08-08

오탐을 0으로 만든 것으로는 게이트가 되지 않는다. **어제 통과한 것이 오늘도
통과해야** 하고, **한 번 고친 것이 돌아오지 않아야** 하고, **게이트 신호가
사람이 읽는 결론과 같아야** 한다. 넷 다 없었다.

#### 룰셋 버전 고정 (Added) — 재현성

- **`rules/RULESET.lock`** 신설. 판정에 쓰이는 필드만의 지문(blake2b)과 사람이
  읽는 버전(`2026.08.2`). **문구(safe_fix·설명·제목)를 다듬어도 지문은
  움직이지 않는다** — 흔들리면 아무도 버전을 올리지 않게 되고, 그러면 이 장치
  자체가 무의미해진다.
- **드리프트를 `validate-rules` 가 ERROR 로 막는다.** 별도 명령으로만 두면
  아무도 안 돌린다 — CI 가 이미 부르는 자리에 붙여야 '버전을 안 올리고 룰만
  고치는' 경로가 실제로 닫힌다. (이 작업에서 곧바로 작동했다: 룰 린터가 룰
  2건을 고치게 하자 CI 가 멈췄고 `--bump 2026.08.2` 로 올렸다.)
- **리포트 머리표에 「판정 기준」** 한 줄 — `엔진 0.3.0 · 룰셋 2026.08.2`.
  **쌍으로** 적는다: 엔진 코드가 바뀌어도 판정은 바뀌므로 룰셋만 적으면
  재현 가능한 것처럼 보이는 착시가 생긴다.
- **소비자 핀** `GVSKB_EXPECT_RULESET` — 버전이나 지문(앞자리 8자 이상)으로
  고정하고, 다르면 결론 근처에 배너가 뜬다. `server_status` 도 `pin_ok` 로 답한다.

#### 외부 회귀 코퍼스 (Added) — "고친 것이 돌아오지 않게"

- `tests/corpus/external_regression.json` — 라운드 14에서 **판정이 실제로 뒤집힌
  자리 10건**을 저장소·커밋·경로·줄과 함께 박제. 오탐 재발(`must_not_fire`)과
  진탐 소멸(`must_fire`), 등급 변화(`must_block`/`must_not_block`)를 **양방향으로**
  고정한다 — 한쪽만 지키면 반대편으로 무너진다.
- **10건 전부를 수정 직전 커밋 `4466450` 에서 돌려 차단으로 재현되는 것을
  확인**하고 등록했다. 발췌가 짧아 원래 조건을 재현하지 못하면 테스트는
  통과하지만 아무것도 지키지 않는다(라운드 12에서 실제로 겪은 실패다).
  코퍼스 자체의 위생(빈 기대·출처 없음·재현 미확인)도 테스트로 막는다.

#### 소스/의존성 게이트 분리 (Fixed) — **진짜 결함이 있었다**

`ScanSummary.blocked` 은 **소스 발견만** 본다(의존성 감사는 스캔이 끝난 뒤에
붙는다). 그런데 CLI 종료코드와 MCP `render_report` 가 그 값을 그대로 읽었다:

```
CRITICAL 취약 패키지 있음
  보고서 본문         → "배포 불가 — 차단 기준에 걸린 패키지가 있습니다"
  MCP `blocked`       → False
  CLI --fail-on block → 종료코드 0 (통과)
```

**같은 문서가 사람에게는 막으라고 하고 기계에게는 통과라고 답했다.** 라운드 9에서
남의 하네스에서 찾아낸 *"게이트가 없는 필드를 읽음"* 이 우리 쪽에 있었다.

- `gvskb/gate.py` 신설 — 게이트 판정을 **한 곳에서만** 계산한다.
  `blocked` · `blocked_source` · `blocked_dependency` · 사람이 읽는 `reason`.
- **`--fail-on dependency`** 추가. 라운드 13의 *"소스는 보조, 의존성은 게이트"* 를
  문장이 아니라 **스위치**로 만든 것이다. 소스 오탐 하나 때문에 게이트를 통째로
  끄면 오탐이 거의 없는 의존성 차단까지 함께 꺼진다.
- MCP 가 분해값을 함께 내보낸다 — 연동 상대가 자기 정책을 세울 수 있어야 한다.

#### 룰 린터 (Added) — 오탐 archetype 을 작성 시점에

실측에서 **실제로 뚫린 모양만** 검사한다. 셋 다 ERROR 라 CI 에서 막힌다.

| 검사 | 무엇을 막나 |
|---|---|
| `sink-token-without-boundary` | 경계 없는 `eval`·`exec` 대안 → `evaluate` 속 `eval`(오탐 14건의 단일 원인) |
| `sanitizer-allowlist-substring` | 부분문자열 정화 가드 → `sanitizeMaybe` 가 통과하고, **통과는 곧 발견 삭제** |
| `lowercase-class-under-ignorecase` | `(?i)` 아래 `[a-z]` → 오탐이 아니라 **미탐**을 만든다(그래서 더 조용하다) |

- **린터가 자기 룰의 남은 결함 2건을 찾았다** — `.innerHTML`·`.outerHTML` 의
  부정 전방탐색이 아직 부분문자열 `sanitize` 를 쓰고 있었다. 판단을
  `html_sink_context` 로 옮겨 고쳤다(삭제가 아니라 감쇄).
- **뺀 검사도 기록한다.** "그룹 사이의 무제한 `.*`" 를 넣으려 했으나 324개 룰에서
  유일한 적중이 정당한 줄 전체 전방탐색이었다. **적중 1건이 전부 오탐이면 그
  검사는 경고 피로만 만든다** — 진짜 경고까지 무시되게 한다.
- CI 는 `--fail-on error` 로 돈다. `review_due` 만료처럼 **달력 때문에 언젠가
  반드시 켜지는** 경고로 CI 를 세우면, 어느 날 아무도 코드를 바꾸지 않았는데
  빨간불이 되고 팀은 검사를 통째로 끈다.

검증: 변이 **15/15 검출** · 적대적 우회 17건 전부 차단 · 테스트 1110 → **1191** ·
ruff OK · 실측 3개 저장소 발견 49 · 차단 3(전부 진짜, 불변).

작업 중 **내가 만든 결함 3건**도 커밋 전에 잡았다. ① 린터 1차 시제품이 24건을
경고했는데 전부 `password|token|secret` 같은 **이름 목록**이었다 — 실행 동사로
좁혔다. ② 좁힌 뒤에도 `["'](exec|eval)["']`(파이썬 `compile()` 의 모드 인자)에서
오탐이 났다 — 따옴표를 경계로 인정하게 고쳤다. **린터의 오탐은 린터를 끄게
만든다.** ③ 룰셋 지문 테스트의 픽스처가 `item: 1`(정수) 타입 오류로 **룰 0건**을
로드했다 — 두 지문이 '똑같이 비어' 있어 통과했다. 픽스처 로드를 검증하게 고쳤다.

### 정화 여부는 룰이 아니라 문맥이 판단한다 (Fixed/Added) — 2026-08-08

남은 차단 오탐 8건이 전부 `dangerouslySetInnerHTML` 이었다. 이건 regex 로 풀 수
없다 — `__html: processHtml(x)` 에서 `processHtml` 이 무엇을 하는지 줄 하나로는
알 수 없기 때문이다. 그래서 **판단을 룰에서 빼내** 문맥을 읽는 곳
(`scanners/html_sink_context.py`)으로 옮겼다.

**설계를 가른 실측 한 건**::

    // 법제처 API 법령 본문 = 신뢰 소스. sanitize 생략으로 조당 ~30ms 절감.
    const html = extractArticleText(article, false, lawTitle)
    return <div dangerouslySetInnerHTML={{ __html: html }} />

같은 모양 7건 중 6건은 헬퍼 본문이 정화로 끝나는 정상 방어였지만, 이 1건은
개발자가 **의도적으로 정화를 껐다**. "한 홉 따라가서 함수면 통과"로 만들었다면
이 진짜 위험이 함께 사라졌을 것이다. 그래서 **이름이 아니라 본문**을 본다.

확신의 정도에 따라 결과를 나눈다 — 전부 지우면 위험이 사라지고, 전부 남기면
표준 정상 패턴이 목록을 덮어 아무도 읽지 않는다. 둘 다 실패다.

| 정황 | 결과 | 실측 |
|---|---|---|
| 주입 지점의 정화 호출(*관찰*) | 내린다. 단 그 이름이 이 파일에서 정화하지 않는 지역 함수면 내리지 않는다 | 11 |
| 지역 헬퍼 1홉(*추론*) | medium · warn 으로 낮추고 이유를 남긴다 | 7 |
| `<style>` 요소 | medium · warn (CSS 라 즉시 XSS 는 아니나 `</style>` 탈출 가능) | 1 |
| 그 밖 | **그대로 차단** | 1 |

- **룰의 같은-줄 부정 전방탐색을 걷어냈다** (Changed). 예전에도 정화가 보이면
  지웠지만 근거가 달랐다: 줄에 `sanitize` 라는 **글자**가 있으면 지웠고, 그래서
  적대적 검증에서 `function sanitizeMaybe(h){ return h.trim() }` 에 뚫렸다 —
  **발견이 조용히 사라졌다.** 이제 그 이름이 이 파일에서 정화하지 않는 함수인지
  확인한 뒤에만 내린다. 볼 수 없는 것(import 된 `DOMPurify.sanitize`)은 믿는다 —
  볼 수 없다는 이유로 전부 차단하면 아무도 이 도구를 쓰지 않는다.
- **`GOV-LLM-OUTPUT-HANDLING-001` 이 TypeScript 를 한 번도 검사하지 않았다**
  (Fixed). `languages: [python, javascript, java]` — `.ts`/`.tsx` 는 언어가
  typescript 로 추론되고 언어 필터가 목록에 없는 언어를 걸러 낸다. LLM 을 붙이는
  웹앱은 대부분 Next.js/React = TypeScript 다. 같은 계열 `KISA-JS-*` 는 모두
  `[javascript, typescript]` 인데 여기만 달랐다. **적대적 검증이 아니었으면
  못 찾았다** — 오탐을 세는 것만으로는 절대 드러나지 않는 종류의 미탐이다.
- **`GOV-SECRET-KEYFILE-001` 의 위치가 언제나 1행이었다** (Fixed). 호출부가
  `line_no=1` 을 박고 있었다. 근거 줄은 보여 주면서 위치는 거짓 — 담당자가 1행을
  열면 아무것도 없어 **확인 자체가 되지 않았다.**
- **비밀 파일 판정이 경로와 공개 식별자를 비밀로 봤다** (Fixed). 실측 2건:
  `BACKOFF_FILE="/tmp/claude-token-refresh-backoff"`(경로)와
  `CLIENT_ID="9d1c250a-…"`(OAuth client_id 는 브라우저 URL 에 실려 나가는
  **공개값**). `/` 는 base64 알파벳이라 값 문자만으로는 경로와 구별되지 않아
  머리 모양으로 가른다. `CLIENT_SECRET` 은 계속 잡는다 — 그 경계를 지키는
  테스트를 따로 뒀다.

검증: 변이 **16/16 검출**(감쇄기 11 + 비밀파일 5) · **적대적 우회 시도 17건 전부 차단** ·
테스트 1093 → **1110** · ruff OK · `validate-rules` ERROR 0.

### 차단 오탐 4종 — 룰이 '단어 조각'과 '문맥'을 못 보던 것 (Fixed) — 2026-08-08

전수 스캔 기준 **차단 31건 중 29건이 오탐**이었다. 원인을 하나씩 세지 않고
분해했더니 소수의 archetype 으로 모였다. 네 가지를 근거대로 좁힌다. 넷 다
**규제를 더하는 방향**이라 매치 집합이 진부분집합이 된다 — 새 오탐은 구조적으로
불가능하고, 위험은 오직 **새 미탐**이다. 그래서 모든 테스트를 오탐/진탐 **짝**으로 뒀다.

- **`GOV-LLM-OUTPUT-HANDLING-001` (차단 오탐 14 → 0)**. 14건이 **전부**
  `evaluate`·`evaluator` 안의 `eval` 이었다 — 예외 하나 없이 단일 원인.
  실행 토큰에 식별자 경계를 건다. 핵심은 `(?-i:[a-z])` 로 **대소문자 구분을
  되살린 것** — `(?i)` 아래서 `[a-z]` 는 대문자까지 잡아
  `executeTool(llmResponse)`(에이전트 도구 실행, OWASP ASI05) 같은 **진짜
  위험까지 함께 죽는다**. 트리거 토큰(`llm|response|…`) 쪽에는 경계를 걸지
  **않았다**: 실측 오탐이 그쪽에서 나온 적이 없고, `\b` 를 걸면
  `llmResponse`·`llm_response`(이 룰의 positive 예시)가 통째로 미탐이 된다.
  근거가 요구하는 곳만 좁힌다. 한 줄 안 토큰 거리도 `.*` → `.{0,120}` 으로
  묶었다(산문 JSON 에서 181자 떨어진 토큰이 실제로 걸렸다).
- **`KISA-JS-INPUT-05` (2 → 0)**. **정규식의 `.exec()` 는 명령을 실행하지 않는다.**
  `/^[A-Za-z]+/.exec(...)` 와 `lineRegex.exec(...)` 였다. 정규식 리터럴·
  `new RegExp(…)`·regex/pattern 계열 변수명을 후방탐색으로 제외한다.
- **`KISA-JS-INPUT-04` 의 `.html()` (3 → 0)**. **인자가 없으면 getter**(읽기)라
  주입이 없다. 셋 다 cheerio 의 `$("body").html()` 읽기였다.
- **`KISA-JS-SEC-01` (1 → 0)**. 라우트에 **역할 기반** 인가가 붙은 관리자
  초기화는 제외한다 — 관리자가 *남의* 비밀번호를 초기화하는데 그 사람의 옛
  비밀번호를 요구할 수는 없다. 반면 역할 없는 `requireAuth()`(그냥 로그인)는
  **계속 잡는다**. 그 경계를 지키는 테스트를 따로 뒀다.

한 가지 확인해 둔 것: 같은 archetype 이 다른 룰에도 있는지 324개 룰의 패턴을
전수 훑었다. **없었다** — 다른 룰들은 실행 토큰 뒤에 `\s*\(` 라는 강한 앵커가
붙어 있어 `evaluate` 가 애초에 걸리지 않는다. 경계도 앵커도 없던 것은 이 룰
하나뿐이었다.

검증: 변이 **13/13 검출**(R4 4 + R1·R3·R6 9) · 테스트 1070 → **1093** ·
`validate-rules` ERROR 0 · ruff OK. 실측 차단 **31 → 10**.

작업 중 **내가 만든 결함 2건**을 커밋 전에 잡았다. ① `Regex/regex/RegExp/regexp`
를 손으로 나열했더니 혼합 표기 `urlRegexp` 가 그 사이로 빠져나갔다 — 내가 쓴
테스트가 잡아냈고, 나열 대신 `(?i:)` 로 덮었다. **나열은 끝이 없고 빠진 자리가
조용하다.** ② 변이검사 스크립트를 쉘 heredoc 으로 만들었더니 백슬래시가 소실돼
앵커가 조용히 빗나갔다 — '변이를 못 잡았다'와 '변이를 적용도 못 했다'가 구분되지
않는 상태였다. 앵커 미발견을 실패로 세도록 스크립트를 고쳤다.

### 소스를 다 보지 않고 다 봤다고 하던 두 가지 (Fixed) — 2026-08-08

의존성 쪽을 고치고 소스 룰로 넘어가자마자, **같은 결함이 소스 쪽에도 그대로**
있는 것이 실측에서 드러났다. 룰의 정확도 이전에 **커버리지**가 먼저였다.

- **`.mts` · `.cts` 가 검사 대상이 아니었다** (Fixed). TypeScript 의 ESM/CJS
  명시 확장자다. `lexdiff` 에 15개 파일 **2,271줄**이 있었고, 그 안의 **외부
  연결 11건(국외 5건 포함)** 이 인벤토리에서 통째로 빠져 있었다. 발견 0이
  "위험 없음"으로 읽히는, 가장 위험한 종류의 미탐이다. 네 곳(`scanner` 포함
  확장자 · `regex_scanner` 언어 매핑 · `js_taint` · `semgrep_scanner`)을 모두
  고쳤다. **언어 매핑을 빠뜨리면 미탐이 오탐으로 바뀐다** — 언어를 모르면
  언어 필터가 "통과"로 처리해서 파이썬 전용 룰까지 `.mts` 에서 발화한다.
  `.ts` 와 결과가 같은지로 고정했다.
- **파일 수 상한 500 에서 조용히 잘렸다** (Fixed/Changed). `lexdiff` 는 검사
  대상이 **568개**인데 500에서 멈췄다 — **68개를 열어보지도 않았다.** 더 나쁜
  것은 그 사실이 리포트에 **"최대 파일 수 도달 1건"** 으로 나온 것이다.
  담당자가 읽는 숫자는 1, 실제 미검사는 68. 라운드 13의 의존성 절단과 완전히
  같은 결함이다(그때는 `truncated_count` 를 리포트가 읽지 않았다).
  - 상한 도달 뒤에도 **세는 것은 계속한다**(`os.walk` 는 파일을 열지 않아 거의
    공짜다). 검사 대상 확장자만 센다 — `.png` 까지 세면 규모가 부풀려지고,
    과장된 경고는 무시되기 시작해 진짜 절단도 함께 무시된다.
  - 절단을 **결론 근처의 배너**로 올린다(의존성 배너와 같은 무게). 제외 요약
    줄에도 실제 미검사 파일 수를 적는다.
  - 기본 상한 500 → **20,000**. 전수 22초를 아끼려고 68개를 버리는 교환이었다.
    20,000은 정상 저장소가 닿을 수 없으면서, 잘못 지정한 경로에서 무한정 도는
    것은 막는 안전판이다.

검증: 변이 **11/11 검출**(C1 4 + C2 7) · 테스트 1055 → **1070** · ruff OK.
실측 `lexdiff` 검사 파일 **498 → 568**(발견 수는 50/차단 28로 불변 — 새로 덮은
70개는 실제로 깨끗했다. 커버리지만 정직해졌다).

작업 중 **내가 만든 결함 3건**도 커밋 전에 잡았다. ① `regex_scanner` 주석 판정에
확장자를 더한 것은 **죽은 코드**였다(언어 추론이 이미 앞에서 걸러 도달 불가) —
변이검사로 드러나 지웠다. ② 상한 초과 카운트 테스트가 파일 이름 순서 때문에
검사 대상 아닌 파일을 만나지 못해, 과대계상 변이를 통과시켰다. ③ `"35개"` 가
`"5개"` 를 부분문자열로 포함해 같은 변이가 또 통과했다 — 정규식 완전일치로 바꿨다.

### 31% 를 못 보고 "검사했다"고 하던 의존성 조회 (Fixed) — 2026-08-08

감사에서 나온 의존성 항목 중 마지막. 앞의 다섯과 달리 **동작·성능·네트워크**를 바꾼다.

- **일시 실패를 판정 불가로 굳혔다** (Fixed). `kordoc` 의 패키지 375개 중
  **117개(31%)** 가 `error` 였는데, 그중 **77건이 `getaddrinfo failed`** — DNS
  일시 실패였다. 한 번만 다시 물었으면 대부분 회수됐을 것을 재시도 없이 확정했다.
  일시 실패(연결·타임아웃·5xx·429)만 2회까지 물러났다 다시 묻는다. **4xx 는
  재시도하지 않는다** — 같은 답이 오는데 서버만 괴롭히고 실패를 늦게 알게 된다.
  마지막 시도가 실패한 뒤에는 기다리지 않는다(어차피 포기할 것이라 순수 낭비이고,
  수백 건이면 분 단위로 쌓인다).
- **패키지마다 새 연결을 열었다** (Fixed). 락파일 906개면 TLS 핸드셰이크도 906번.
  느린 것보다 나쁜 것은 **연결이 많을수록 일시 실패가 늘어난다**는 점이다. 감사
  한 번에 `AsyncClient` 하나를 공유한다(오프라인 모드는 열지 않는다).
- **락파일 기본 상한 500 이 실무 규모에 못 미쳤다** (Changed). `lexdiff` 의
  전이 의존성은 **906개**였고 406개가 잘렸다. 방어 상한(`_MAX_PACKAGES=2000`)까지
  올린다. 여전히 잘리면 앞 항목의 배너로 드러난다.
- **진행 표시** (Added). 전수 검사는 시간이 걸린다 — 그 대가를 사용자가 견딜 수
  있어야 한다. `audit_manifest(on_progress=...)` 로 `(완료, 전체, 패키지명)` 을
  흘린다. 콜백이 터져도 검사는 계속된다(곁가지가 본체를 막으면 안 된다).

검증: 변이 **7/7 검출** · 테스트 1048 → **1055** · ruff OK · 실제 락파일 e2e:

```
kordoc  parsed 375 · checked 375 · truncated 0     · error 117 → 0   · vulnerable 0 → 1
lexdiff parsed 906 · checked 890 · truncated 406→0 · error 16(1.8%)  · vulnerable 39
```

**결과가 예상보다 컸다.** `kordoc` 은 `error 117 → 0` 이 되면서 **`vulnerable 0 → 1`**
로 바뀌었다 — 재시도가 통계를 정리한 게 아니라 **조회 실패에 가려져 있던 진짜
취약점을 드러냈다.** "판정 불가는 안전이 아니다"를 문구로 지키는 것은 최소한이고,
**줄일 수 있는 판정 불가를 줄이는 것이 먼저**다.

`lexdiff` 는 재시도 뒤에도 **16건(1.8%)이 남는다** — 전부 회수되지는 않는다.
그 16건은 그대로 '판정 불가'로 표시된다.

- **함정**: 변이 "무한 재시도"가 처음엔 안 잡혔다. `for attempt in range(...)` 의
  루프 상한이 이미 막고 있어 **의미가 거의 같은 변이**였기 때문이다. 그렇다고
  가드가 무용한 것은 아니어서(마지막 실패 뒤 불필요한 대기를 막는다) 그 차이를
  재우는 횟수로 고정했다.
- **함정 2**: 기존 테스트 더블이 새 인자(`client`)를 안 받아 깨졌다. 더블이 먼저
  깨지면 **진짜 회귀를 가린다** — `**_kw` 로 받도록 고쳤다.

### 판정 근거를 확인할 수 없던 의존성 보고서 (Fixed) — 2026-08-08

같은 감사(§ 아래 항목)에서 이어진 넷. **탐지 로직은 건드리지 않는다** — 저장·표시만
바뀐다.

- **검사되지 않은 패키지가 보고서에 없었다** (Fixed). `lexdiff` 의 전이 의존성
  906개 중 락파일 기본 상한 500 때문에 **406개가 잘렸는데**, `report.py` 가
  `truncated_count` 를 **한 번도 읽지 않았다**. 담당자는 "검사됨 500"이라는
  완결돼 보이는 섹션을 그대로 결재에 올린다 — 트리의 45% 를 안 봤다는 사실이
  종이에 없다. 배너 한 줄로 알린다(조치가 "상한을 올려 재검사" 하나인 시스템
  문제이므로 패키지마다 깃발을 꽂지 않는다). 감사 결과의 `disclaimer` 에는 적혀
  있었지만 **그 문자열은 렌더되지 않는 필드**였다.
  `unchecked_count`(검사했으나 판정 못 함)와 구분해 적는다.
- **권고 ID 는 주면서 주소를 안 줬다** (Fixed). 보고서가 `GHSA-4r6h-8v6p-xvw6`
  라는 문자열만 찍어, 담당자가 직접 검색해야 했다 — **결재 문서에 검증할 수 없는
  근거**가 올라갔다. OSV 는 GHSA·CVE·PYSEC·GO 를 한 주소 체계로 서비스하므로
  규칙 하나로 전 생태계가 덮인다. ID 형태가 확실할 때만 만든다(자리표시자·주입
  시도가 그대로 주소가 되면 안 된다). HTML 은 `_esc` 를 거치므로 본문에 URL 을
  박으면 **클릭할 수 없는 글자**가 된다 — `(텍스트, 주소)` 로 나눠 렌더러가 각자
  링크를 만든다.
- **"권고 버전 없음"이 "고칠 방법 없음"으로 읽혔다** (Fixed). `xlsx 0.18.5` 는
  npm 에 수정 버전이 없어 권고가 `None` 인 것이 옳다 — 그런데 **수정본은 존재한다**
  (SheetJS 가 배포를 자체 CDN 으로 옮겼다). 그 안내가 OSV `references` 에 있는데
  이 필드를 저장하지 않아 전달하지 못했다. ADVISORY·FIX 를 우선해 최대 2개를
  싣고, 권고 버전이 없을 때만 보여준다(있으면 노이즈다).
- **`max_cve` 가 `"HIGH"` 같은 라벨이라 임계값 정책을 쓸 수 없었다** (Added).
  게이트가 "CVSS 7.0 이상 차단"을 하려면 숫자가 필요하다. 그렇다고 벡터에서
  점수를 **우리가 산출하면 틀린 CVSS 를 우리 이름으로** 내보내게 된다 — 없는 것보다
  나쁘다. OSV 가 준 **표준 벡터를 그대로 통과**시킨다(`cvss_vector`). 없으면 `None`
  이며 라벨에서 역산하지 않는다.

검증: 변이 **8/8 검출** · 실제 데이터로 렌더 확인(`xlsx` 는 제작사 권고 주소까지,
`multer` 는 2.2.0 권고와 링크 5건) · 테스트 1038 → **1048** · ruff OK.

- **함정**: 테스트를 파일 끝에 덧붙이면서 **이미 있던 `_vuln_check` 헬퍼를 같은
  이름으로 덮어써** 기존 5건이 깨졌다. 이름 충돌은 실패로 드러나서 다행이었지만,
  같은 파일에 덧붙일 때는 기존 헬퍼 이름부터 확인해야 한다.

### 공공기관 운영에 alpha 를 올리라던 권고 (Fixed) — 2026-08-08

외부 저장소 3개(`lexdiff` · `kordoc` · `noti.kpetro`)를 감사하다 발견했다.
취약 패키지 59건의 권고 버전을 훑으니 **16건(27%)이 따르면 안 되는 조언**이었다.

```
@babel/core  7.28.4 → 8.0.0-rc.6      (같은 권고문에 안정판 7.29.6 이 있었다)
multer       2.0.2  → 3.0.0-alpha.2   (안정판 2.2.0 이 있었다)
ajv          6.12.6 → 8.18.0          (6.14.0 이면 끝나는데 파괴적 마이그레이션 요구)
undici       7.16.0 → 8.9.0           (7.29.0 이면 충분)
```

- **원인 1 — 프리릴리스를 거르지 않았다.** `_version_key` 는 `-` 앞만 읽어
  `8.0.0-rc.6` 과 `8.0.0` 을 같은 값으로 본다. 그래서 rc 가 안정판 `7.29.6` 을 이겼다.
- **원인 2 — 브랜치 경계를 버렸다.** `_advisory_fixed_versions` 가 OSV 의
  `ranges` 를 평탄화해 `fixed` 만 남긴다. `[7.29.6, 8.0.0-rc.6]` 이 "7.x 의 수정"과
  "8.x 의 수정"이라는 사실이 사라지고, 그중 **최댓값**을 골랐다.
- docstring 은 *"모두 넘어서는 **최소** 상한"* 이라고 적혀 있었다 — **의도와 구현이
  어긋난 버그**이지 설계 선택이 아니었다.

**오탐보다 나쁜 결과다.** 오탐은 시간을 낭비시키지만, 이 조언은 따르면 사고가 나고,
따르지 않으면 *"메이저 업그레이드 12건은 못 하겠다"* 며 **조치 자체를 포기**하게 된다.
패치 한 줄이면 될 것을 과대 요구해 취약점을 그대로 남긴다.

조치: `(introduced, fixed)` 쌍을 `fixed_ranges` 로 보존하고, 우선순위를
**① 내 구간(`introduced <= 현재 < fixed`) → ② 현재보다 높은 최소 안정판 →
③ 프리릴리스**로 정했다. advisory *사이*에서는 최댓값을 유지한다 — 하나라도 안
넘으면 취약점이 남는다.

**미탐 방향을 같은 커밋에 고정했다.** 무조건 최솟값을 고르면 내 브랜치가 아닌
수정본을 집어 취약점이 그대로 남는다(`[(1.0.0,3.0.0),(2.0.0,2.1.0)]` 에서 현재
1.5.0 이면 2.1.0 이 아니라 3.0.0).

- **함정**: 첫 회귀 테스트 5건 중 **3건이 변이 검사를 빠져나갔다.** 구간 매칭을
  없애도 폴백(현재보다 높은 최소 안정판)이 같은 답을 내서 테스트가 통과했다.
  **겹친 방어층이 서로를 가린 것**이다. `_advisory_target` 을 직접 부르는 단위
  테스트로 층을 분리해 5/5 검출로 만들었다.

검증: 실제 56개 패키지 **before/after 전수 대조** — 동일 40 · 낮아짐 16 ·
**높아짐 0 · 사라짐 0**, 낮아진 16건은 전부 브랜치 구간으로 정당성 확인 ·
프리릴리스 권고 4→0 · 변이 5/5 · 테스트 1028 → **1038** · ruff OK.

### 낡은 프로세스를 스스로 못 알아보던 진단 (Fixed) — 2026-08-08

바로 앞 항목의 과탐 3종 수정을 머지하고 MCP 서버에 반영하려는데, **서버가 계속
옛 룰로 판정**했다. 그런데 `server_status` 는 최신이라고 답했다. 판별용 코드를
따로 짜서야 낡음을 확인할 수 있었다 — 도구가 먼저 말했어야 하는 것이다.

- **`commit_id` 가 디스크의 커밋을 답했다** (Fixed). 호출 시점에 `.git/HEAD` 를
  읽으므로 **저장소가 최신인가**에 답할 뿐, **그 최신이 적용됐는가**에는 답하지
  못했다. gvskb 는 룰과 파이썬 코드를 프로세스 시작 시점에 한 번 읽는데
  (`server.py` 의 `RULES = load_all_rules(...)`), MCP 서버는 한 번 뜨면 며칠씩
  살아 있어 실제로 자주 어긋난다. 이제 **임포트 시점 스냅샷**을 신원으로 삼고,
  디스크의 현재 값은 `install_identity.disk_commit_id` 로 따로 노출한다.
- **`total_rules` 도 디스크를 다시 읽어 셌다** (Fixed). 같은 거짓말의 두 번째
  경로다. 하필 인텔 캐시 룰이 하나 늘어 개수까지 최신처럼 보였다. 이제
  메모리에 올라간 룰을 세고 `rule_count_source` 로 출처를 밝힌다.
- **`runtime_freshness` 추가** (Added). `process_stale` · `reasons` ·
  `remedy` 를 돌려준다. `doctor` 에도 같은 점검을 넣었다. 조치 문구는 '재설치'가
  아니라 **'재시작'** 임을 못 박는다 — 재설치만 반복하다 시간을 버린 일이 있다.
  - **함정**: 처음엔 지문을 `stat` 만으로(파일 수·총 바이트·최신 mtime) 만들었는데
    **탐지에 실패했다**. 수정한 룰이 트리에서 가장 최신이 아니면 `max` 가 그대로고
    크기도 그대로다. 지문이 안 변한 채 '최신'이라 답하는, 고치려던 것과 똑같은
    결함이었다. 내용 해시(blake2b, 375파일 ~50ms)로 바꿨고, 덤으로 **mtime 만
    흔들리는 경우**(OneDrive 동기화, 같은 내용 checkout)에 거짓 경보가 없다.
  - **함정 2**: 첫 회귀 테스트는 tmp 디렉터리에 파일을 하나만 두어, 수정한 파일이
    곧 최신이 되는 바람에 **stat 방식으로 되돌려도 통과**했다. 변이 검사에서
    드러났다. 더 최신인 이웃 파일을 함께 두도록 고쳐 실제 조건을 재현한다.
  - `GVSKB_RULES_DIR` 이 바뀐 경우는 "룰 파일이 수정됐다" 가 아니라 **경로가
    바뀌었다**고 말한다 — 있지도 않은 파일 수정을 찾아 헤매지 않도록.
- **번들 `.mcp.json` 이 이 PC 에서 뜨지 않는 설정이었다** (Fixed). `"command":
  "python"` 에 `PYTHONPATH` 가 없었다. Windows 에서 `python` 은 Microsoft Store
  스텁으로 잡히기 쉽고, 그때 서버는 오류도 없이 그냥 안 뜬다. pip 이 같은 환경에
  만들어 주는 진입점 `gvskb-server` 로 바꿨다 — `gvskb doctor` 가 실행되는
  환경이면 반드시 실행된다. README 세 곳의 스니펫도 함께 고치고, 한 곳만 고쳐
  나머지가 되살아나는 일이 없도록 테스트로 묶었다.

검증: 테스트 1007 → **1028**(신규 21) · **변이 10종 전부 검출** ·
`evaluate` 97룰 TP=289 FN=0 TN=296 FP=0(100%/100%, 탐지 동작 무변화) ·
`validate-rules` ERROR 0 · ruff OK.

### 세 프로젝트 리포트 재분석 — 남은 과탐 3종 (Fixed) — 2026-08-07

세 프로젝트(`semi_fable5` 백테스트 파이프라인 · `wiggle_web` · `ai-idea-pipeline`)
검사 리포트와 외부 정리 문서를 받아 재분석했다. **먼저 확인한 것: 그 리포트들이
직전 수정 이전 산출물이라는 사실**이다. 리포트 발견 25건을 실파일로 재구성해
현재 버전으로 재검사하니 오탐 13건은 이미 해결, 중복 2건은 이미 묶임,
정탐 2건, **남은 오탐은 1종 8건**이었다. 여기에 재분석 중 2종을 더 찾았다.

- **`open(os.path.join(고정, "리터럴"))` 이 '높음·차단'이었다** (Fixed).
  파이썬에서 가장 흔한 *안전한* 관용구다. `open\s*\(\s*os\.path\.join\s*\(` 가
  외부 입력 여부를 전혀 보지 않아, 백테스트 파이프라인 8건이 전부 차단됐다.
  조각이 전부 문자열 리터럴이면 경로가 동적이지 않으므로, 콤마 뒤에 따옴표가
  오지 않는 경우(변수·함수호출·f-string)만 잡는다.
  - **함정**: 처음엔 `,\s*(?!["'])` 로 썼는데 `\s*` 가 0글자로 백트래킹해
    전방탐색이 **공백**을 보고 통과했다 — 아무것도 안 좁혀진 채 '수정 완료'가
    될 뻔했다. 공백을 전방탐색 *안*으로(`,(?!\s*["'])`) 옮겨 해결. 이 함정
    자체를 테스트로 고정했다.
- **`DB_PASSWORD = "${DB_PASSWORD}"` 가 '치명·차단'이었다** (Fixed).
  환경변수·템플릿 주입 표기는 **비밀값을 코드에 두지 않았다는 증거**인데
  모범 사례를 차단하고 있었다. `${…}` · `{{…}}` · `%VAR%` 를 플레이스홀더로 제외.
  - **함정**: `%VAR%` 를 `%[A-Z_][A-Z0-9_]*%` 로 썼더니 패턴 앞의 `(?i)` 가
    전역이라 소문자까지 먹어 **실제 비밀번호 `pa%ss%word12` 가 미탐**이 됐다.
    `(?-i:...)` 로 그 구간만 대소문자를 되살렸다. 맨몸 `$VAR` 는 실제 비밀번호에
    흔한 문자라 제외하지 않는다.
- **실수의 소수부가 주민등록번호로 잡혔다** (Fixed). `\b` 만으로는 부족하다 —
  소수점이 단어 경계라 `12.9001011234568` 의 소수부가 후보가 된다. 실측
  `"avg_win": 294.444152…` 가 지금까지 안 걸린 건 날짜가 '84월'이라 **운이
  좋았을 뿐**이고, 유효 날짜를 심자 즉시 '치명·차단'으로 재현됐다. 하이픈 없는
  형태에만 `(?<![\d.])…(?![\d.])` 를 건다 — 하이픈 형태에 걸면 문장 끝
  마침표에서 미탐이 난다.

**새 오탐이 생길 수 없음을 먼저 증명하고 착수했다.** 세 수정 모두 기존 패턴에
제약을 *추가*하는 형태이므로 매치 집합이 부분집합이어야 한다. 코퍼스
**1,058파일 · 169,775줄**로 대조해 **신규 매치 0건**을 확인했고, 사라지는 매치
7건은 전부 의도한 오탐이며 다른 코드에서 잃는 정탐은 0건이었다.

검증: **1007테스트**(신규 23) · 벤치마크 recall 100%·FP 0 유지 · `evaluate`
97룰 precision 100%·recall 100% · `validate-rules` OK · ruff OK ·
**변이 5종 전부 검출**. 재구성 프로젝트 재검사 **9건 → 2건**(치명 4 → 0,
차단 해제), 남은 2건은 실제 빈 catch. `wiggle_web` 회귀 없음(2건 유지).

채택하지 않은 제안(근거·위험 대비): `Decision.review` 신설(소비 지점 46곳 +
하네스·레지스트리 계약 파괴, 룰 정밀화만으로 같은 결과를 냈으므로 근거 없음) ·
`shell=True` 완화(현장 오탐 근거 0, 위험한 탐지를 약화) · 신규 룰 7종 추가
(오탐 안정화 전에는 새 오탐원) · JSON/YAML 파서와 경로 AST 테인트 도입
(관측 오탐은 정규식 가드로 전부 해결됨).

### 오탐 87.5% — 외부 프로젝트에 처음 겨눠 보고 알았다 (Fixed·Added) — 2026-08-07

남의 저장소(Next.js/TypeScript, 129파일)를 검사했더니 **16건 중 14건이 오탐**이었고,
**critical 12건이 전부 오탐**이라 배포 차단 판정까지 났다. 벤치마크는 계속 recall
100%·FP 0 이었다 — 우리가 만든 코퍼스에서만.

- **13자리 정수의 40%를 주민등록번호로 보고했다** (Fixed). `GOV-PII-RRN-001` 의
  `\b\d{6}-?[1-4]\d{6}\b` 는 하이픈이 선택적인데 **앞 6자리가 날짜인지 보지 않았다.**
  - 실측: 임의 13자리 정수 20만 개 중 **39.96%** 가 매치. Drizzle 마이그레이션의
    Unix 밀리초 타임스탬프 `1784654517497`("17년 **84월** 65일")이 critical·block.
    `Date.now()` 를 쓰는 모든 JS 프로젝트에서 터진다.
  - 날짜 유효성 추가 → **1.51%**(26배↓), 검증식(mod 11)까지 → **0.15%**(267배↓).
  - 검증식은 **하이픈 없는 값에만** 적용한다. 2020.10. 뒷자리 개편 이후 번호에도
    성립한다고 단정할 근거를 확인하지 못했다 — 불확실한 규칙을 미탐 쪽에 쓰지 않는다.
- **테스트 픽스처를 진짜 키로 보고했다** (Fixed). `GOV-SECRET-APIKEY-001` 이
  `token` 을 부분문자열로 잡아 `joinToken: "join_new"` 가 걸리고, 플레이스홀더
  제외목록에 `test`·`dummy`·`fake`·`mock` 이 없어 `apiKey: "test-only-key"` 도 통과했다.
- **`SECRET_KEY` 를 통째로 놓치고 있었다** (Fixed). 같은 룰이 키워드 바로 뒤에 `[:=]`
  를 요구해 Django 에서 **가장 흔히 하드코딩되는 시크릿**이 미탐이었다. 오탐을 고치러
  갔다가 반대 방향 결함을 같이 찾았다.
- **함수 이름 안의 `openai` 를 LLM 호출로 오인했다** (Fixed).
  `requestStructuredOpenAI(...)` 가 매치되고 오류 문구의 "secret" 과 결합해,
  **시크릿이 새지 '않는지' 검증하는 테스트**가 개인정보 전송 critical 로 보고됐다.
  실제 호출·임포트 형태(`openai.` · `from openai` · `new OpenAI(` · `api.openai.com`)만 인정한다.
- **증거가 매치가 아니라 줄 전체였다** (Fixed). 200자 넘는 줄에서 매치와 무관한
  앞부분만 보여, **정탐인데도 오탐으로 읽혔다**(빈 `.catch(() => {})` 가 줄 끝에 있어
  증거에는 `useEffect(() => { fetch(...` 만 찍혔다). 이제 매치 ±60자로 자르고
  잘린 쪽에 `…` 를 붙인다.
- **테스트 코드 경로 감쇄** (Added). 시크릿·PII 오탐 8건이 **전부** `tests/` 밑에
  있었다. 값의 진위가 판정을 좌우하는 계열만 `low`·`warn` 으로 낮추고, **발견은
  지우지 않는다** — 테스트에 진짜 키를 넣는 사고도 흔하다. 낮춘 사유는 리포트에
  `심각도 조정` 으로 함께 찍힌다. 주입·XSS 같은 *코드 모양* 룰은 감쇄하지 않는다.
- **룰 간 중복 묶기** (Added). `detection.dedup_group` — 같은 코드를 다른 각도로 보는
  룰(`KISA-JS-ERR-02`/`-03` 은 둘 다 삼켜진 예외)이 같은 줄에 걸리면 하나만 남긴다.
  그룹을 선언한 룰끼리만 묶으므로 무관한 룰이 임의로 지워지지 않는다.
  룰별 정확도 평가는 `collapse_duplicates=False` 로 묶기를 끈다.
- **값 검증기** (Added). `detection.validators` — 정규식은 *형태*만 보므로,
  형태가 같은 남을 걸러내려면 값을 계산해야 한다. 첫 검증기는 `rrn_checksum`.
  등록되지 않은 이름은 조용히 무시하지 않고 경고한다.
- **examples 없는 실행형 룰을 ERROR 로 막는다** (Added) — *이번 사고의 근본 원인*.
  `gvskb evaluate` 는 75개 룰 전부 precision 100% 를 보고했지만, **`GOV-PII-RRN-001`
  은 examples 가 없어 평가 대상에서 통째로 빠져 있었다**(평가 횟수 0). 40% 오탐 룰이
  품질 게이트에 안 잡히는 구조였다. `validate-rules` 가 positive·negative 둘 다
  요구하고, 예시 없던 21개 룰에 실제 코드 기반 예시를 채웠다 — **97/97(100%)**.
  테스트의 커버리지 하한도 25% → 100% 로 올렸다.

**고치다가 낸 구멍 3건 — 커밋 전 적대적 검증에서 잡았다** (Fixed).
오탐 수정은 거의 항상 반대편에 새 구멍을 낸다. 아래 셋은 전부 *이번 수정이 만든*
결함이고, 커밋 직전 재점검이 없었으면 그대로 나갔다.

- **`SECRET_KEY` 미탐을 고치려다 새 오탐 4종을 만들었다.** 뒷마디를
  `[_-][A-Za-z]+` 로 열었더니 `token_endpoint`(URL) · `secret_name`(이름) ·
  `password_hint`(안내문) · `api_key_header`(헤더명) 가 전부 critical 로 걸렸다.
  **시크릿을 *가리키는* 이름과 *담은* 이름은 다르다.** 뒷마디를 허용목록
  (`key|token|secret|password|passwd|pwd|pass`)으로 좁혔다.
  - 처음 검사에서 URL 두 건이 안 걸린 건 값에 `.example.` 이 들어가 테스트 픽스처
    제외에 우연히 걸린 것이었다. 실제 도메인으로 바꾸자 바로 터졌다 —
    **우연히 통과한 것을 통과했다고 읽으면 안 된다.**
- **감쇄를 넣자 리포트가 감쇄분과 진짜 위험을 한 카드로 합쳤다.**
  `_rule_groups` 가 `rule_id` 만으로 묶어, `tests/` 의 감쇄된 발견과 운영 코드의
  진짜 치명이 같은 카드가 됐다. 카드 머리의 '심각도 조정: critical → low' 가
  운영 시크릿에까지 걸린 것처럼 읽히고, `decision` 은 첫 발견의 것을 쓰므로
  **차단이 경고로 표시**됐다. 묶음 키에 심각도·결정·조정사유를 넣어 분리했다.
- **변이 검사 하네스 자체가 오판했다.** pytest 종료코드 5(수집 0건)를 실패로
  오인해, **아무 테스트도 돌지 않은 변이가 '검출'로 찍혔다.** 선택자 오타 하나로
  검증이 통째로 가짜가 된다. 수집 0건을 '무효'로 따로 처리한다.

검증: **984테스트**(신규 54) · 벤치마크 recall 100%·FP 0 유지 · `validate-rules` OK ·
`evaluate` 97룰 precision 100%·recall 100% · ruff OK · **변이 10종 전부 검출**
(원복 무결성: 추적 파일 diff 바이트 동일) · CI packaging·pytest 3.11·3.12 통과.
같은 외부 프로젝트 재검사 **16건 → 2건**(critical 12 → 0, 차단 해제),
남은 2건은 실제 빈 catch.

### 오프라인 '이상 없음'을 걷어내다 · 결과 필드 계약 노출 (Fixed·Added) — 2026-08-04

연동 하네스 두 곳(`vibe_harness_claude`·`vibe_harness_codex`)을 받아 정적 대조하다
**우리 결함**을 먼저 찾았다.

- **오프라인 경로가 CVE 를 보지 않고 '이상 없음'이라고 말했다** (Fixed).
  악성 피드 대조만 하고 `verdict="checked_clean"` · `requires_review=False` 를
  돌려줬다. 오프라인 캐시에는 악성 피드와 KEV 만 있고 **CVE DB 가 없어** 구조적으로
  취약점을 볼 수 없는데, 판정은 초록으로 나갔다.
  - 실측: 두 하네스가 `.mcp.json` 에 `GVSKB_MODE=offline` 을 박아 둬 온라인 PC 도 이
    경로를 탔다. **취약점 26건짜리 `pillow 12.2.0` 이 '이상 없음'** 으로 통과한다
    (온라인: `vulnerable`/`high`/26건/권고 12.3.0).
  - `verdict="unknown"` · `checked=False` · `requires_review=True` 로 바꾸고, **한 것과
    못 한 것을 함께** 적는다("악성 등록은 없습니다 / 취약점 여부는 확인하지 못했습니다").
    KEV 이름 일치가 있으면 사유에 덧붙인다. 보고서에서는 기존의 정직한 자리
    ("판정 불가 N건 — '안전'이 아닙니다")로 집계된다.
  - **부수 효과(의도함)**: 오프라인 판정은 합의 §5-D 의 미제출 판정이라 반입 번들이
    비게 된다. 그전에는 CVE 를 보지 않은 '이상 없음'이 기관 레지스트리에 쌓였고, 그
    저장소를 쓰는 **다른 기관까지 오염**된다. 관측이 아닌 것을 사실로 보내지 않는다.
  - 우리 원칙("판정 불가는 안전이 아니다")을 **우리 오프라인 경로가 어기고 있었다.**
- **결과 필드 계약을 노출한다** (Added). `server_status().package_result_contract` —
  `fields`(36) · `verdicts`(10) · `decision_fields`(게이트 최소 집합 11).
  - 왜: 한 하네스의 설치 게이트가 `max_cve_severity`·`severity`·`status`·`typosquat`
    를 읽고 있었다. **넷 다 우리 결과에 없는 이름**이라 항상 빈 값이 나왔고, 그 빈 값이
    취약점 심각도 사다리의 입력이었다 — CRITICAL 이 경고로 내려앉는데 **양쪽 어디에도
    예외가 나지 않는다.** 없는 필드를 읽는 것은 예외가 아니라 침묵이다.
  - 대조할 목록을 우리가 내놓지 않으면 상대는 알아낼 방법이 없다. 계약이 실제 스키마와
    어긋나면 테스트가 실패한다(오타를 담은 계약을 배포하면 상대가 그것을 믿는다).
- 회귀 9건 신설. **소스 레벨 변이 5종 전부 검출.** 변이 실행 중 타임아웃으로 마지막
  변이가 원복되지 않은 채 남았던 것을 발견해 복구했다 — 변이 검사도 실패할 수 있다는
  전제로 **끝나고 반드시 원복을 확인**한다.

### 'Top 3' 를 없애고 조치 순서로 · 분야 표의 '치명 10' 오독 (Changed·Fixed) — 2026-08-04

담당자 질문 두 개. 둘 다 **읽는 사람이 화면을 어떻게 붙여 읽는가**의 문제였다.

- **분야 표의 `최고 심각도 | 건수` 가 "치명 10건"으로 읽혔다** (Fixed).
  `비밀값·인증정보 노출 | 치명 | 10` 에서 치명은 **그 분야의 최고 등급**이고 10은
  **분야 전체 건수**(실제로는 치명 2·높음 1·보통 1·낮음 6)인데, 등급 칸과 총계 칸이
  나란히 있으면 사람은 둘을 붙여 읽는다. 열 이름이 맞아도 소용없다.
  - `심각도별 내역` 열을 추가해 분포를 그 자리에서 끊는다. 분야 상세 제목도
    `— 10건 (치명 2 · 높음 1 · 보통 1 · 낮음 6) · 파일 6개` 로 바꿨다.
  - 의존성 행도 같은 형식으로 분포를 적는다.
- **`가장 먼저 할 일 (Top 3)` 을 `조치 순서` 로 바꿨다** (Changed). 질문은
  *"왜 일부만 표시하나, 위험은 모두 조치해야 하는 것 아닌가"* 였고 **맞는 말이다.**
  - Top-N 은 위험 유형 3개만 남기고 나머지를 잘랐다. 잘린 목록은 **"3개만 하면 되나"**
    로 읽히고, 그러면 나머지가 조용히 남는다. 직전 라운드에서 "6개 중 3개"라는 범위
    각주를 붙였지만, **범위를 밝히는 것과 잘라내지 않는 것은 다르다.**
  - Top-N 이 답하려던 질문("무엇부터 손대나")은 여전히 유효하므로 **자르는 대신
    순서로** 답한다: ① 지금 막아야 하는 것(차단) ② 그다음(치명·높음) ③ 나머지(보통·
    낮음 — *순서가 뒤일 뿐 조치 대상입니다*). 세 단을 합치면 전체가 된다.
  - 머리말이 먼저 못박는다: **"소스 16건 · 패키지 3건 전부가 조치 대상입니다. 아래는
    '무엇부터'의 순서일 뿐, 일부만 고르라는 뜻이 아닙니다."**
  - 패키지 업그레이드가 마지막 단계로 들어가 "소스만 고치면 차단이 풀리지 않는다"를
    조치 목록 안에서 말한다.
- 회귀 10건 신설. **소스 레벨 변이 9종 전부 검출** — 그중 하나(분야 표 내역 열)는
  처음에 빠져나갔다. 절 제목에 같은 문구가 있어 **표 칸을 지워도 통과**했다(직전
  라운드와 같은 유형이 또 나왔다). 행 형식까지 확인하도록 좁혔다.

### "26건이 뭔지 알 수가 없다" — 취약점 내역을 되찾다 (Fixed·Added) — 2026-08-04

담당자 질문: *"취약·악성 3건인데 패키지별로 26건·8건·2건이 있다. 뭐가 26건인지
알 수가 없다."* 두 숫자의 단위가 다르다(3=패키지 수·조치 단위, 26=그 패키지에 붙은
개별 권고 수). 단위 차이는 의도한 설계지만, **내역을 한 줄도 보여주지 않은 것**은
결함이었다. 원인이 세 겹이었다.

- **결과에 5건만 남기고 있었다** (Fixed). `vulns[:5]` — 26건을 조회해 놓고 5건만
  저장했다. 숫자는 26이라 적으면서 내역은 5건뿐이니 **나머지 21건은 어디에도 없었다.**
  조용한 절단이다. 상한을 없앴다(실측 재검사: `vulnerability_count=26` ↔ `advisories=26`).
  잘라야 한다면 자른 사실을 값으로 남긴다는 원칙을 표시 계층에도 적용해, 보고서가
  접을 때는 **접은 개수를 적고**, 수집분이 집계보다 적으면 그 사실을 밝힌다.
- **advisory 마다 심각도·고쳐진 버전을 버리고 있었다** (Added). `{id, summary, modified}`
  만 저장해, `max_cve=HIGH` 는 있는데 **26건 중 무엇이 HIGH 인지** 알 수 없었다.
  OSV 응답의 `affected[].ranges[].events[].fixed` 도 통째로 버려 "어느 버전으로
  올려야 하는가"를 말할 수 없었다(직전 라운드에 남긴 한계의 뿌리).
  - 심각도는 패키지 최고값과 **같은 함수**로 뽑는다 — 두 값이 갈라지지 않게.
  - 표기가 없으면 `UNKNOWN`(미상) 이다. 낮음으로 낮춰 적지 않는다.
  - 다른 패키지의 `affected` 항목(전이 의존성)은 건너뛴다 — 남의 버전을 우리 권고로
    내면 안 된다.
- **보고서가 취약점 내역을 렌더하지 않았다** (Added). 패키지마다 `GHSA-ID · 심각도 ·
  해결 버전 · 요약` 목록을 낸다(12건까지 펼치고 나머지는 개수와 함께 접기).
- **목표 버전을 말한다** (Added). `recommended_version` 신설 — 나열된 취약점의 `fixed`
  중 가장 높은 값. 실측: pillow `12.3.0 이상`, pip `26.1.2 이상`.
  - **하나라도 고쳐진 버전을 모르면 `None`** 이다. "올리면 다 해결된다"는 잘못된
    안심을 주지 않는다(실측: xlsx 는 2건 다 fixed 미상 → 목표 버전 미상 + 최신 버전 안내).
  - 버전 비교는 숫자 마디로 한다 — 문자열 비교면 `12.10.0` 이 `12.3.0` 에 진다.
    읽을 수 없는 표기는 추측하지 않고 제외한다.
  - 수정 프롬프트도 `pillow 12.2.0 → 12.3.0 이상으로 업그레이드` 로 바뀌었다.
    예전에는 "공식 배포처를 확인해 정하세요"로 **판단을 사용자에게 넘겼다** — 도구가
    이미 아는 값을 버리고 있었다.
- **숫자에 단위를 붙였다** (Fixed). 판정 칸을 `취약점 26건` → `개별 취약점 26건` 으로,
  의존성 절 머리에 "N건은 패키지 수, 개별 취약점 N건은 그 패키지의 권고 수" 한 줄.
- 회귀 12건 신설(수집 6 · 렌더 6). **소스 레벨 변이 11종 전부 검출** — 그중 하나(판정
  칸 단위 표기)는 처음에 빠져나갔다. 절 제목에 같은 문구가 있어 표 칸을 지워도 통과했다.

### 보고서 판독 결함 6종 (Fixed·Added) — 2026-08-04

실제 결재 보고서를 담당자가 **정독하며 물은 여섯 가지**에서 나왔다. 전부 "숫자는
맞는데 읽는 사람이 알 수 없다" 계열이다 — 탐지가 아니라 **전달**의 결함이고,
전달되지 않은 탐지는 조치로 이어지지 않는다.

- **상세 카드에 파일 경로가 없었다** (Fixed). 카드가 `line 1, 39, 66` 처럼 줄 번호만
  찍었다. 그 인증서 룰은 실제로 **2개 파일 × 3줄**인데 읽는 사람은 3건으로 읽고
  나머지 3건을 찾지 못한다. **위치를 못 찾으면 고칠 수 없다.**
  - 파일 경로를 아는 함수(`_locations_by_file`)가 이미 있었는데 **수정 프롬프트에서만**
    쓰고 사람이 읽는 화면에서는 버리고 있었다. 같은 함수를 카드·Markdown 이 함께 쓴다.
  - 카드가 길어지지 않게 파일 3개까지 펼치고 나머지는 `외 N개 파일`로 접는다
    (전체 목록은 프롬프트 블록에 그대로 있어 정보가 사라지지 않는 자리다).
- **의존성이 집계에서 통째로 빠져 있었다** (Fixed). 보안 분야 개요·심각도 표는 소스
  발견만 셌는데, 결론 박스는 "차단 기준에 걸린 패키지"를 근거로 배포를 막았다 —
  **판정에는 쓰이는데 집계에는 없는** 상태였다.
  - `의존성·공급망(패키지)` 행을 분야 개요에 추가하고, 심각도 표를 `소스 코드 |
    의존성(패키지) | 합계` 세 열로 나눴다. 한 칸에 합치지 않는다 — 정하는 방식이 다르다.
  - 요약에 `총 조치 대상 N건(소스 N · 패키지 N)` 을 추가했다. 실측 재검사: 16건과
    3건이 따로 놀던 것이 **19건**으로 한 줄에 잡힌다.
  - 표시 계층 전용이다. `report.findings`·발견 건수·게이트 판정은 그대로 둔다
    (패키지를 소스 발견으로 둔갑시키지 않는다).
- **심각도 기준이 두 개인데 어디에도 설명이 없었다** (Added). 소스는 룰 문서에 미리
  적힌 고정 등급이고, 패키지는 검사 시점의 취약점·KEV·CVSS 로 계산한다. 같은 '높음'이
  같은 자로 잰 값이 아닌데 보고서가 그 사실을 말하지 않아, **"왜 이게 높음이냐"에
  도구가 답하지 못했다.** 부록에 `심각도 판정 기준` 표를 넣고 매핑을 고정했다
  (악성·레지스트리 차단·미존재=치명 / KEV·HIGH↑=높음 / 그 외 취약·쿨다운=보통 /
  판정 불가=등급 없음).
- **의존성 수정 프롬프트가 없었다** (Added). 배포 차단 사유의 절반이 패키지인데
  프롬프트에는 소스 발견만 있어, 안내대로 AI에게 고치라고 하면 소스만 고쳐지고
  **다시 검사해도 배포 미승인이 유지**된다. 사용자는 왜 안 풀리는지 알 방법이 없다 —
  **도구가 만든 막다른 길**이었다.
  - 패키지·버전·사유(취약점 수·최고 CVSS·KEV)·출처를 담은 블록을 추가하고,
    "패키지 블록을 빠뜨리지 마세요" 경고를 함께 낸다.
  - 소스 발견이 0건이어도 프롬프트 섹션이 나온다(예전에는 섹션 자체가 사라졌다).
  - **권고 버전은 지어내지 않는다** — 검사 결과에 '고쳐진 버전'이 없다. 공식 배포처와
    보안 권고를 확인해 정하라고 지시한다.
- **Top 3 과 분야 개요의 단위 차이를 밝히지 않았다** (Fixed). Top 3 은 위험 유형(룰)
  3개(실측 8건)이고 분야 개요는 전체(16건)인데 라벨이 "Top 3" 뿐이라, 두 숫자를 같은
  것의 요약으로 보고 "왜 안 맞지"에서 멈춘다. `위험 유형 6개 중 3개 · 전체 16건 중
  8건` 을 제목 아래에 적는다.
- **분야별 '파일' 열의 합이 실제 파일 수와 달랐다** (Fixed). 한 파일이 두 분야에
  걸리면(같은 파일의 주석 계정 + 자원 해제) 각 분야에서 한 번씩 센다. 건수에는
  '고유 위치'를 함께 적는 장치가 있었는데 파일 수에는 없었다. 실측 재검사에서
  합 11 · 실제 10 을 각주로 밝힌다.
- **실행환경 등급의 출처를 적지 않았다** (Fixed). 개인 PC 에서 돌린 검사가
  `E2(내부서버 공용)` 로 찍혀 "왜 내 PC 가 내부서버냐"는 질문이 나왔다. 이 도구는
  환경을 **판별하지 않는다** — 부르는 쪽이 넘긴 값이거나 기본값(E1)이다.
  - `검사 실행 시 지정(--env E2)` / `지정 없음 · 기본값 적용` 을 명시하고, 등급이
    **신규 버전 쿨다운 기준일만** 바꾼다는 사실을 그 자리에 적는다.
  - MCP 의존성 도구 4개의 `env_grade` 설명에 "사용자가 배포 환경을 명시하지 않으면
    **비워 두세요**"를 넣었다. 에이전트가 지어 넣은 값이 결재 문서에 사실처럼 실린다.
- 회귀 20건 신설(`tests/test_report_readability.py`). **소스 레벨 변이 11종 전부 검출** —
  그중 하나(Top 3 각주)는 처음에 빠져나갔다. 함수만 테스트하고 **렌더러가 그 함수를
  부르는지**를 보지 않아, 렌더 호출을 지워도 초록불이 유지됐다. 렌더 결과 검증을 추가했다.

### `server_status()` 가 설치 신원을 말한다 (Added) — 2026-08-04

- **`commit_id` 노출** — 이 설치본이 정확히 어느 커밋인지 돌려준다.
  pip 이 남긴 `direct_url.json` 의 `vcs_info.commit_id`(git URL 설치)를 먼저 보고,
  없으면 소스 체크아웃의 `.git/HEAD` 를 읽는다(`git` 프로세스를 띄우지 않고 파일만
  읽는다 — MCP 서버 안에서 도는 코드이고, 기관 PC 는 외부 프로세스 실행이 막혀 있을 수 있다).
  - 왜: **버전 문자열은 신원이 아니다.** `__version__` 이 7/31 이후 고정이라 그 뒤
    변경분이 전부 `0.3.0` 으로 보였고, codex 하네스가 `0.1.0` 설치본으로 도는 것을
    **도구 호출 실패 증상으로 역추적하기 전까지** 아무도 몰랐다.
  - 커밋을 알 수 없으면 조용히 `null` 을 주지 않고, 왜 모르는지와 해결 방법
    (`pip install git+<URL>@<브랜치>` 또는 체크아웃에서 `pip install -e .`)을 함께 적는다.
- **`mcp_tools` · `missing_tools` 노출** — 이 설치본이 실제로 등록한 MCP 도구 목록과,
  매니페스트 대비 빠진 도구. 연동 상대는 필요한 도구 이름을 목록과 대조하면 **한 번의
  호출로** 낡은 설치를 확인할 수 있다.
  - 도구 목록을 프레임워크에 묻지 않는다. fastmcp 의 조회는 비동기라 MCP 도구 안에서
    부를 수 없고, 동기 접근 경로는 버전마다 바뀌는 사설 속성이다(실측: `mcp` SDK 의
    `_tool_manager` 가 fastmcp 3.x 에는 없다). 등록 시점에 이름을 직접 남기고
    (`server.REGISTERED_TOOLS`), **그 사본이 프레임워크 실물과 갈라지면 테스트가 실패**한다.
  - **못 세는 것과 없는 것을 구분한다.** 목록을 읽지 못하면 `missing_tools` 를 빈 목록이
    아니라 **전체 목록**으로 돌려주고 `tool_inventory_ok=false` 를 함께 준다. 빈 목록은
    상대에게 '멀쩡한 설치'로 읽힌다 — 조용한 초록불이 된다.
- `gvskb doctor` 에도 같은 정보가 나온다(`Install commit` · `MCP tools`). 도구가 빠져
  있으면 ERROR, 매니페스트에 없는 도구가 등록돼 있으면 WARN 이다.

### 저장소 위생 가드 — 기관 식별자 유출을 CI 가 막는다 (Added) — 2026-08-04

- **`tests/test_repo_hygiene.py` 신설.** 추적 파일과 커밋 메시지에서 ①기관 도메인
  (`*.go.kr`) ②시·군·구 실명(한글·로마자) ③기본 비밀번호 문자열을 찾으면 실패시킨다.
  - 왜: 직전 세션에 감사 대상의 기본 계정 문자열을 **두 번 놓칠 뻔했고 둘 다 사람 눈으로
    잡았다.** 공개 저장소는 한 번 푸시되면 회수할 수 없다 — 실패를 사람이 아니라 CI 에 둔다.
  - **도메인은 기본 차단 + 명시 허용목록.** 추적 파일의 `go.kr` 42곳은 전부 공개
    참조처(`law.go.kr`·`data.go.kr`)이거나 합성 예제(`example.go.kr`)였다. 전면 금지는
    오탐 42건을 내고 곧 꺼졌을 것이다. 새 호스트는 허용목록에 이름을 적어야 통과한다.
  - 가드 파일 자신도 검사 대상이다. 비밀번호는 조각으로 나눠 원문을 없앴고, 기관명
    목록만은 원문이 필요해 **그 파일 하나, 기관명 검사에 한해** 예외로 뒀다. 예외가
    조용히 늘지 못하도록 목록 크기를 테스트가 고정한다.
  - CI 는 전체 이력을 봐야 하므로 `actions/checkout` 에 `fetch-depth: 0` 을 줬다.
    기본값(1)이면 여러 커밋을 한 번에 푸시할 때 중간 커밋이 검사되지 않는다.
- **가드가 첫 실행에서 실제 유출을 찾았다** — 커밋 `97e6ef7` 의 메시지 본문에 감사 대상
  기관의 개인키 파일명(도메인 포함)이 있다. 이미 푸시돼 `--amend` 로 지울 수 없어
  이력 재작성 대상으로 명시했다(`_PENDING_HISTORY_REWRITE`). 새 커밋에는 예외가 없다.
- 남아 있던 기본 계정 문자열 두 곳(README 예시 · KISA 룰 테스트 픽스처)을 합성 문자열로
  바꿨다. **사람이 두 번 잡았던 것을 도구가 세 번째에 잡았다.**

### `dev-quick` 프로파일 채택 (Added) — 2026-08-03

- **개발 중 경량 점검용 `dev-quick` 을 체커 표준 프로파일로 추가**했다.
  `vibe_harness_claude` 가 작성해 하네스 저장소에서 운용하던 것을 **양 하네스 동의로
  그대로 채택**했다(출처는 정책 파일 안에 명기).
  - 채택 이유: 같은 정책 파일 사본이 체커와 하네스 양쪽에 있으면 **반드시 갈라지고,
    갈라져도 아무도 모른다.** 실제로 하네스 사본은 상대경로 `GVSKB_POLICIES_DIR` 탓에
    한 번도 적용된 적이 없었는데 그 사실이 드러나지 않았다. 단일 출처를 체커에 둔다.
  - 이제 `GVSKB_POLICIES_DIR` 없이도 동작하므로, 하네스는 사본 5개와 환경변수를
    지울 수 있다(기관 고유 정책이 생기기 전까지).
  - **채택 전 근거를 재검증했다** — 프로파일 주석이 "SQLi·명령/코드실행·경로조작·
    업로드는 이미 critical/high 라 `severity_min` 만으로 자동 포함된다"고 주장해
    `rules/` 와 대조했고, 해당 10개 룰 전부 사실이었다(critical 8 · high 2).
    `KISA-PY-SEC-13`(medium)처럼 등급이 낮은 비밀값·개인정보 룰이 있어
    `category_overrides` 로 강제하는 것도 맞았다. 도구의 주장을 그대로 옮기지 않는다.
  - 실측: 같은 코드에 `dev-quick` 5건(critical) vs `public-default-strict` 6건
    (critical 5 · medium 1) — 경량 점검이 실제로 가볍게 동작한다.

### CLI 경로도 프로파일 대체를 리포트에 남긴다 (Fixed) — 2026-08-03

- **CLI 가 스캔 전에 프로파일을 미리 치환해 `profile_fallback` 이 남지 않았다**
  (vibe_harness_claude 지적). 알 수 없는 프로파일을 만나면 `args.profile` 을 기본값으로
  바꾼 뒤 스캐너를 불러, 스캐너는 정상 프로파일을 받은 것으로 보였다.
  - 리포트가 거짓말을 한 것은 아니다(적용된 것을 그대로 적었다). 다만 **운영자가
    무엇을 요청했는지가 결재 붙임 문서에서 사라졌다** — 경고는 stderr 로 휘발된다.
    CI 스크립트의 `--profile` 오타는 매번 같은 리포트를 내고 아무도 눈치채지 못한다.
  - 스캐너가 대체·기록을 함께 처리하므로 CLI 의 선치환은 이제 불필요하고 해롭다.
    경고는 유지하되 치환만 제거했다 — CLI 와 MCP 가 같은 경로를 탄다.
  - 재현 명령에도 **운영자가 실제로 친 값**이 남는다. 그 명령을 다시 실행하면 코어가
    같은 대체를 하므로 결과도 같다.

### 하네스 연동 점검 — 프로파일 정직성 · 벤더 식별 검증 (Fixed) — 2026-08-03

하네스(vibe_harness_claude·codex)를 실제로 받아 연동 계약을 대조하다 발견.

- **적용되지 않은 프로파일을 적용된 것처럼 적었다** (Fixed). `load_profile()` 은 정책
  파일을 못 찾으면 빈 스펙을 돌려주는데, 호출자는 그것과 정상 스펙을 구분할 수 없어
  **요청한 이름을 그대로 리포트에 적었다.**
  - 실측: 하네스가 MCP 로 `scan_path(profile="dev-quick")` 을 부르는데, 그 정책 파일은
    하네스 저장소에만 있고 상대경로 `GVSKB_POLICIES_DIR` 로 지정돼 있어 **프로젝트
    폴더에서 뜨는 MCP 서버는 찾지 못한다**(실측: 하네스 루트 밖에서 프로파일 0개).
    아무 필터도 걸리지 않았는데 보고서 머리표에는 `dev-quick` 으로 판정했다고 찍혔다.
  - **CLI 는 원래 정직했다** — 경고 후 기본값으로 바꿔 진행한다. 그 처리가 API·MCP
    경로에만 없었고, 하네스가 쓰는 쪽이 하필 그 경로다.
  - `ProfileSpec.resolved` 신설, `ScanReport.profile_fallback` 에
    `{requested, applied, reason, available}` 기록. `report.profile` 은 **실제 적용된**
    프로파일을 적고, 머리표에 "요청한 `X` 를 찾지 못해 대체" 를 함께 표시한다.
    MCP 반환 JSON 에도 실리므로 하네스가 적용 여부를 확인할 수 있다.
- **파일명으로 추정한 컴포넌트를 검증 없이 조회했다** (Fixed). 어제 넣은 벤더 번들
  식별이 파일명 기준 이름을 그대로 npm 에 물었다.
  - 실측: `chart.min.js`(Chart.js 3.9.1)를 npm `chart` 로 조회 → 그 패키지에 3.9.1 이
    없는데도 **`checked_clean`(이상 없음)** 이 나왔다. 실제 Chart.js 는 `chart.js` 이고
    npm `chart` 는 전혀 다른 패키지(최신 0.1.2)다 — **조용한 초록불이다.**
  - 배너에서 라이브러리 이름을 뽑아 후보에 넣고(`/*! Chart.js v3.9.1 */` → `chart.js`),
    **그 버전이 실재하는 후보만** 채택한다. 레지스트리가 이름을 고르게 하는 셈이라
    검증이 곧 판별이 된다(실측: `chart.min.js` → `chart.js@3.9.1` 확정).
  - 어느 후보도 맞지 않으면 '이상 없음'이 아니라 **판정 불가**로 남긴다 — 엉뚱한
    패키지의 결과를 그 파일의 결과로 말하지 않는다.
  - 오프라인·통신 실패로 확인하지 못한 경우는 조회는 하되 `requires_review` 로 올리고
    "다른 라이브러리일 수 있습니다('안전' 아님)" 를 붙인다. 확인하지 못한 것을
    확인한 것처럼 두지 않는다.
  - 회귀: `xlsx 0.18.5`·`jquery 3.4.0` 은 그대로 취약 2건씩 검출.

### 상용툴 전수 대조 — 조용한 초록불 2종 · 계수 결함 2종 (Fixed·Added) — 2026-08-03

코드 스패로우(공공기관용) 보고서 **367쪽 전문**과 실측 공공 Flask 프로젝트를 전수 대조하며 발견.
상세 근거·변이 검사는 내부 검증 기록으로 보관합니다.

- **결론 박스가 의존성 감사 결과를 통째로 무시했다** (`97e6ef7`, Fixed).
  `dependency_audit` 은 리포트의 부가 필드일 뿐 `summary` 에 합류하지 않았고,
  `_deploy_status()` 는 소스 발견만 봤다. 깨끗한 소스 1개 + `pillow==12.2.0` 으로
  재현: 결론은 **"배포 승인 가능"(초록)** 인데 같은 문서의 의존성 절은 **"차단 권고"** 였다.
  - 그 프로젝트는 소스 차단 2건 덕에 우연히 빨간불이 떴을 뿐, **소스가 깨끗하고 패키지만
    위험한 프로젝트는 전부 초록불**을 받았다. 담당자가 가장 먼저 읽는 곳이다.
  - `_dep_risk()`·`_dep_verdict_clause()` 신설. 차단 패키지→빨강, 취약·미존재·판정
    불가→주황, 전부 이상 없음→초록 유지(과잉 교정 방지).
- **`.min.` 제외 규칙이 진짜 취약 컴포넌트를 가렸다** (`97e6ef7`, Fixed).
  `static/` 에 직접 떨궈진 `*.min.js` 는 빌드 출력이 아니라 **벤더링된 서드파티
  라이브러리**인데 파일명만 보고 '빌드 산출물'로 제외했다. 실측 프로젝트에는
  `package.json`·락파일·`node_modules` 가 하나도 없어 매니페스트 SCA 도 설치본 npm
  경로도 발동하지 않는 **이중 사각지대**였다.
  - 실측: `static/xlsx.full.min.js` = `xlsx 0.18.5` → CVE-2023-30533(Prototype
    Pollution, HIGH)·CVE-2024-22363(ReDoS, HIGH). 게다가
    `templates/event_new.html:1015` 가 사용자가 고른 엑셀을 `XLSX.read()` 로 파싱해
    advisory 가 명시한 취약 경로에 **실제로 도달한다.**
  - 라운드2 백로그가 "wheel·venv·**min.js**" 를 한 줄로 잡았다가 앞 둘만 연결하고
    닫혔다 — **복합 항목을 한 줄로 닫으면 남은 절반이 기록에서 사라진다.**
  - `tools/vendor_bundle.py` 신설. 소스 룰 검사는 계속 제외해 원래 의도(미니파이드
    오탐 방지)를 유지하고, 컴포넌트 취약점 검사로만 넘긴다.
  - 버전 식별은 **이름 토큰이 인접한 `.version=` 대입만** 채택한다.
    `xlsx.full.min.js` 에는 `.version=` 이 3개(0.18.5·1.2.0·1.2.1) 있고 뒤 둘은
    번들된 하위 라이브러리(SSF·CFB)다. 근거가 없으면 **추측하지 않고** `version=None`
    → '판정 불가'로 남긴다(스패로우는 같은 파일에 `Cuckoo` 9개 버전을 지어냈다).
  - MCP `scan_vendor_bundles` 도구 + 서버 지침 2-1 추가. CLI 만 배선하면 IDE 사용자
    에게 수정이 닿지 않는다 — **안 쓰이는 보안 도구는 보안 효과가 0이다.**
- **동일 컴포넌트를 매니페스트·설치본에서 두 번 셌다** (`fb7bb09`, Fixed).
  "취약 패키지 4건" 의 내역이 pillow(매니페스트) + pillow(설치본) + pip + xlsx 로,
  고유 취약 컴포넌트는 3종이었다. 조치 단위(업그레이드)와 알림 단위가 어긋나면
  경고 피로가 생기고 그 사이로 진짜 위험이 묻힌다.
  - **스패로우가 같은 오류를 3배로 저질러 pillow 하나에 106건(272건 중 39%)** 을 쓴
    것과 종류가 같다. 자기 도구에 관대한 기록은 개선을 멈추므로 같은 기준으로 고친다.
  - `_dep_merged_components()` 신설 — `(생태계, PEP 503 정규화 이름, 버전)` 병합.
    매니페스트별 표 N개를 **컴포넌트 표 1개**로 바꾸고 출처를 한 행에 나열.
    `취약 패키지 N건` 은 고유 컴포넌트 수, `검사 N건` 은 수행한 검사 횟수로 구분한다.
- **파일명에 `.min.` 이 없는 미니파이드 `.js` 가 여전히 조용히 빠졌다**
  (`fb7bb09`, Fixed). `vendor/lib.js` 처럼 라이브러리를 그대로 받아 둔 경우가 흔하다.
  - 미니파이드 `.js` 계열도 벤더 식별을 시도하되 **벤더라는 근거의 세기로 경보 수위를
    가른다**: 파일명 `.min.`(`detected_by="name"`)은 명시적 신호라 버전 미상이면
    '판정 불가'로 올리고, 내용만 미니파이드인 것(`detected_by="content"`)은 프로젝트
    자체 번들일 수 있어 제외 목록에만 남긴다.
  - **과잉 교정을 잡은 지점**: 최초 구현은 자체 번들 `app.js` 까지 판정 불가로 올렸다.
    그러면 자체 번들을 둔 프로젝트는 영구히 초록불을 못 받고, 그 피로가 진짜 경고까지
    묻는다.
- **상용툴 오탐 15건도 같은 기준으로 기록** — Python 6건(`redirect(url_for(...))` 를
  URL 자동접속으로, `pw = ''` 를 하드코드 비밀번호로, `import random` 선언줄을
  난수 결함으로) + `qrcode.min.js` 한 파일에 `Cuckoo` 9개 버전 GPL 위반 9건.
- **기관 도메인 개인키를 상용툴이 놓쳤다** — `.pem`·`개인키`·`ssl/`·`secret_key` 를
  367쪽 전문에서 검색해 **발견 0건** 확인. 기관 도메인 인증서 개인키(`*_key.pem`)·`.secret_key`·
  주석에 남은 기본 관리자 계정은 우리만 잡았다 — 공공 특화 룰의 실증.
- 재스캔: 발견 16 · 차단 2 **유지**(손실 없음), 취약 패키지 3→3종(xlsx 추가 ·
  중복 계상 제거), 판정 불가 0→1(qrcode — 추측 대신 정직). 테스트 797 → **817**.

### 기관 레지스트리 연동 — 20번 문서 대응 (Added·Fixed) — 2026-08-03

- **매니페스트 제약 연산자를 버리고 있었다** (Fixed). 파서 정규식이 `>=`·`~=`·`^` 를
  버리고 **경계값을 구체 버전처럼** 다뤘다. `requests>=2.28` 인 프로젝트에 실제로는
  2.31.0 이 깔려 있어도 2.28 기준으로 판정했고, 2.28 의 취약점이 2.30 에서 고쳐졌다면
  그대로 오탐이었다. 더 나쁜 것은 **그 오탐이 레지스트리에 "2.28 에 대한 관측 사실"로
  제출**된다는 점이었다 — 우리가 보지 않은 것을 사실로 넘기고 있었다.
  - `version_exact` 신설. 경계값 검사 자체는 남긴다("이 제약이 취약한 버전을 허용한다"도
    실제 신호다). 다만 그것이 **관측이 아니라 가정**임을 값에 실어 제출 필터(§5-D)가
    거른다.
- **`--include-installed` 의 npm 경로가 통째로 깨져 있었다** (Fixed). 설치 목록을
  `express==4.17.0`(requirements 형식)으로 만들고 npm(JSON)으로 파싱하려 해
  `verdict="unparsed"`·`parsed_count=0` 이 나왔다. **node_modules 에서 수집한 전이
  의존성이 한 건도 검사되지 않았다** — `--include-installed` 의 존재 이유가 전이
  의존성인데 npm 쪽은 비어 있었다.
  - `unparsed` 로 검토 대상에는 올라갔으니 완전한 침묵은 아니었으나, 화면에는
    "형식·ecosystem을 확인하세요"라고만 나와 **도구 자신의 결함이 사용자 입력
    문제처럼 보였다.**
- **설치본 중 직접 의존성을 `manifest` 로 구분** (Added, 레지스트리 요청 §3).
  이름은 매니페스트에, 정확한 버전은 설치 흔적에 있다 — 둘을 합쳐야 "직접 의존성 +
  실제 버전"이 되고, **그 판단은 둘 다 읽는 이쪽에서만 할 수 있다.**
  - `source_scope` 판정을 **감사 단위 → 항목 단위**로. 설치본 인벤토리 안에 직접·전이가
    섞여 있어 감사 단위로는 나눌 수 없다.
  - 락파일에 가려 검사에서 건너뛴 매니페스트도 **이름 목록으로는 읽는다** — 락파일은
    직접·전이를 구분하지 않고 평면으로 담기 때문이다.
  - 같은 패키지가 두 경로로 잡히면 한 번만 보낸다. 남기는 쪽은 `manifest` — 강등되면
    심사 큐에서 사라진다.
- **`kev_checked` 신설** (Added, 레지스트리 요청 §4). `in_kev=false` 를 '악용 목록에
  없음'으로 읽어도 되는지를 값으로 남긴다.
  - 레지스트리는 `cache_sources_used` 가 비었는지로 이를 추론하려 했으나 **그 규칙은
    악성 피드만 있고 KEV 캐시가 없는 경우를 놓친다** — 목록은 비어 있지 않은데 KEV
    대조는 되지 않은 상태이고, 망분리에서 번들을 부분 반입하면 흔하다.
- **항목 단위 실패를 'ok' 로 두지 않는다** (Fixed). 레지스트리가 배치 부분 실패를
  허용하도록 고친 뒤 실패는 HTTP 상태가 아니라 **항목 단위**로 온다. 우리 클라이언트는
  HTTP 상태만 봐서, 답을 받지 못한 항목이 `registry_status="ok"` 로 표시됐다.
  - 아는 판정(APPROVED·REJECTED·UNDER_REVIEW·UNKNOWN)이 아니면 `item_failed`.
    모르는 값을 통과로 읽으면 **상대가 상태를 하나 늘릴 때마다 조용한 초록불이
    하나씩 생긴다.**
- **`installed` 보류 해제** — 레지스트리가 enum 을 반영했음을 확인(회신 §2). 보류
  장치 자체는 남겨 뒀다(같은 상황이 다시 생길 자리).
- 문서 정정: 내부 구조 문서의 판정 신선도 규칙을 **통과·차단 비대칭**으로. 통과
  방향에는 신선도를 요구하고 차단 방향에는 요구하지 않는다 — 그러지 않으면 합의
  §4-5 가 TTL 을 7일로 늘린 이유가 1시간 단위로 되살아난다. **코드는 처음부터
  비대칭이었고 문서가 틀렸다**(r5 §4 와 같은 패턴).

### 보고서 재현 절차 (Fixed) — 2026-08-03

- **재현 명령이 판정을 바꾸는 옵션 둘을 빠뜨리고 있었다.** 보고서는 이 명령 위에
  "같은 결과를 다시 만들거나 다른 환경에서 검증하려면"이라고 적는다 — 재현을
  명시적으로 **주장**하는 문장인데, `--include-installed`(전이 의존성 포함)와
  `--env`(쿨다운 기준 3·7·14일)가 명령에 실리지 않았다.
  - 두 누락이 **같은 방향으로 움직인다: 발견이 줄어든다.** 결재 문서를 검증하러
    재실행한 사람이 더 깨끗한 결과를 받고 "해소됐다"고 읽게 된다. 도구가 스스로
    만들어 내는 조용한 초록불이다.
  - 종료 코드만 바꾸는 `--fail-on` 과 부산물만 만드는 `--registry-bundle` 은 넣지
    않았다 — 명령만 길어지고 재현되는 판정은 같다.
- **실행환경 등급이 보고서 어디에도 표기되지 않았다.** 등급은 쿨다운 기준일을
  정해 판정을 바꾸는데(같은 패키지가 E1 통과 · E2 `cooldown_hold`), 읽는 사람이
  어느 기준으로 나온 판정인지 알 방법이 없었다. Markdown·HTML 양쪽 '재현 절차'에
  `실행환경 등급: E2 (내부서버 공용, 쿨다운 기준 14일)` 한 줄을 추가했다.
  - `--env` 를 주지 않은 실행도 **적용된 기본 등급**을 적는다. '지정하지 않음'과
    '적용되지 않음'은 다르다 — 지정하지 않아도 기준일은 적용됐다.
  - 의존성 검사를 하지 않은 실행에는 적지 않는다. 판정에 관여하지 않은 값을
    적으면 읽는 사람이 근거로 오해한다.

### 기관 레지스트리 연동 — R7 대응 4건 (Added·Fixed) — 2026-08-02

- **`caller` 클라이언트 검증** (Fixed). 규칙(`<출처>:<모드>[:부서코드]`, 합의 §3)을
  제안한 것은 우리인데 **우리 쪽에 검증이 없었다.** `caller` 는 레지스트리 봉투로만
  나가는 값이 아니라 **우리 감사로그에도 그대로 들어간다** — 사번·PC명·이메일이
  섞이면 공공기관 감사 기록 자체가 개인정보 파일이 된다.
  - `audit.normalize_caller()` 신설. 형식 위반은 `unknown:invalid` 로 대체하고
    **빈 값으로 지우지 않는다** — 지우면 '신고하지 않음'과 구분되지 않아 위반
    사실이 기록에서 사라진다.
  - 부서코드만 어긋나면 **그 슬롯만** 버리고 `<출처>:<모드>` 는 살린다. 값 하나
    때문에 AUTO/MANUAL 감사 귀속까지 잃는 것은 과잉 교정이다.
  - **거부한 값을 경고에 싣지 않는다** — 검증이 유출 경로가 되면 안 된다. 같은
    위반은 프로세스당 한 번만 알린다(락파일 800건이면 경고도 800줄이 된다).
  - 검증 지점은 값이 **들어오는 곳**(MCP 도구)과 나가는 곳(감사 싱크·봉투) 양쪽.
    한 곳만 막으면 `result["caller"]` 로 우회된다.
- **422(스키마 거부)를 연결 실패와 분리** (Fixed). `lookup_batch` 가 모든 실패를
  `except Exception` 으로 뭉개어, 재시도가 무의미한 실패가 "레지스트리에 연결하지
  못했습니다"로 보고됐다. 담당자는 방화벽·VPN을 뒤지지만 고칠 곳은 스키마다.
  - `registry_status` 에 `rejected` 추가(400·404·422). 5xx·타임아웃은 조치가
    같으므로 `unreachable` 로 함께 둔다 — 종류를 더 쪼개면 배너만 늘고 할 일은 같다.
  - **부분 실패에서 받은 판정을 버리지 않는다.** 청크 하나가 거부됐다고 전체를
    상태 표시로 덮으면, 부분 실패 자체가 차단 우회 수단이 된다(200건짜리 청크
    하나만 깨뜨리면 나머지 600건의 기관 차단이 함께 사라진다).
  - 이건 가상의 상황이 아니다: `source_scope` 에 `installed` 를 추가하기로 했는데
    상대 enum 에 아직 없으면 배치가 통째로 422 로 돌아온다.
- **낡은 차단(`stale`) 표시** (Fixed). `_cache_get` 이 붙여 둔 `stale` 표식을
  `apply_registry_decision` 이 읽지 않아, 8일째 확인 못 한 차단과 방금 받은 차단의
  안내가 같았다. **판정은 그대로 차단**이고(조회 실패로 차단이 풀리면 레지스트리
  도달을 방해하는 것만으로 우회가 된다, 합의 §4-5) 달라진 것은 안내다 — 후자에는
  '해제됐는지 확인'이 남아 있다. `registry_stale` 필드 신설.
- **반입 번들 생성** (Added) — `--registry-bundle <파일>` + 리포트 저장 시
  `.registry-bundle.json` 사이드카, 둘 다 `.sha256` 동봉. 망분리 환경에서 관측이
  레지스트리로 넘어가는 유일한 경로다.
  - 리포트 전체를 올리면 `findings[].evidence`(코드 조각)와 `scanned_files`(파일
    경로)가 레지스트리를 거친다 — 합의 §3 이 **절대 전송 금지**로 정한 것이고,
    서버가 파싱 범위를 좁혀도 업로드 시점에 이미 지나간 뒤다.
  - `result` 는 **허용목록**으로 담는다(`PackageCheckResult` 스키마 필드만).
    제외 목록은 새 필드가 생길 때마다 뒤처지지만 허용목록은 자동으로 막는다.
  - `installed` 유래는 담지 않는다 — 레지스트리 `source_scope` enum 반영 확인
    전까지 제출하지 않겠다고 약속했고, 번들은 지연된 제출이다. **다만 몇 건을
    보류했는지 센다**(`excluded`): 조용히 빠지면 받는 쪽은 우리가 그 패키지를
    쓰지 않는 것으로 읽는다.
  - 화면 출력만 한 실행(`--stdout`·`--format json` 무저장)은 파일을 남기지
    않는다 — 반출 심사 대상이 담당자 모르게 디스크에 생기면 안 된다.
  - `.sha256` 을 **바이트로** 쓴다. `write_text` 는 Windows 에서 CRLF 로 바꾸고,
    그러면 파일명 끝에 CR 이 붙어 받는 쪽 `sha256sum -c` 가 실패한다(실측).
    검증하라고 동봉한 파일이 검증을 막으면 없느니만 못하다.

### 의존성 검사 — 락파일 지원 (Added) — 2026-08-02

- **전이 의존성을 검사 범위에 넣음** (`ccfe3b1`). 상용 SCA 대비 최대 격차였다.
  매니페스트에는 직접 의존성만 적히는데 실무 취약점은 대부분 전이 의존성에 있다.
  이전에는 락파일을 정직하게 거절했고(`verdict="unparsed"`), 정직했지만 검사가
  안 되는 건 마찬가지였다.
  - 지원: `package-lock.json`(v1 중첩 트리·v2/v3 평면 경로) · `poetry.lock` ·
    `uv.lock` · `pnpm-lock.yaml`(v6 슬래시·v9 `@` 표기) · `yarn.lock`(클래식·berry YAML)
  - 파싱에서 실제로 걸린 함정 둘 — 테스트가 잡았다: pnpm 스코프 패키지가
    `/@babel/core@7.24.0` → 이름 `@babel`·버전 `core@7.24.0` 으로 쪼개졌고,
    yarn berry 는 헤더 주석 없는 YAML 이라 형식 인식 자체가 실패했다.
- **규모 재설계**: `limit` 을 형식별 기본값으로 분리(매니페스트 20 · 락파일 500,
  상한 2000). MCP `limit` 기본을 `None` 으로 바꿔 "형식에 맞춰 자동"이 되게 했다 —
  사용자가 락파일이 다른 숫자를 필요로 한다는 걸 알아야 할 이유가 없다.
- **순차 → 동시 실행**(기본 8, `GVSKB_CHECK_CONCURRENCY`). 순차면 800건이 곧
  800회 왕복이다.
- **잘린 검사를 드러냄**: `truncated_count` + `requires_review` 승격. 일부만
  검사하고 전부 검사한 것처럼 보이면 그게 조용한 초록불이다.
- 같은 디렉터리에 매니페스트와 락파일이 둘 다 있으면 **락파일만** 검사한다
  (상위 집합이므로 중복 조회를 피한다). 이름이 같아도 버전이 다르면 별도 항목으로
  둔다(npm 중첩 설치) — 취약한 쪽이 하나라도 있으면 위험은 실재한다.

### 위협 인텔 캐시 (Fixed) — 2026-08-02

- **온라인 경로가 KEV 캐시에 의존하면서 그 사실을 숨기던 문제** (`c1d5ecf`).
  '인텔 캐시 낡음 표시'를 만들려다 표시 문제가 아니라 **탐지 결함**임을 발견했다.
  온라인 모드에서도 CISA KEV 교차 대조는 로컬 캐시를 쓰는데, 그 의존을 결과에
  전혀 싣지 않아 `cache_sources_used`·`cache_freshness`·`cache_stale_sources` 가
  모두 비어 있었다. 그래서 캐시가 없거나 6개월 낡아도 `in_kev=false` 가 나오고
  **'악용 없음'과 구분되지 않았다.** `_kev_cve_hits` 주석은 "'악용 없음'이 아니라
  '대조 못 함'이다"라고 정확히 적어 뒀는데 그 구분이 호출자에게 전달되지 않았다 —
  **주석에만 있는 안전장치는 안전장치가 아니다.**
- `_kev_cache_state()` 신설 — `ok`·`stale`·`missing` 구분. 취약점이 있을 때만
  캐시를 열어 비용을 늘리지 않는다.
- `_aggregate_intel_cache()` — 패키지별 상태를 매니페스트 1건의 시스템 상태로 집계.
- **보고서 배너**(md·html): 캐시 열화를 **한 줄만** 표시. 조치가 "번들 반입 한 번"인
  시스템 문제를 패키지 수백 건에 깃발로 꽂으면 담당자가 무시하게 되고, 그러면 그
  사이의 진짜 위험도 함께 묻힌다. 취약점 0건이면 `not_used` — 열화가 아닌 것을
  열화로 알리면 배너 자체가 무시된다.

### 기관 레지스트리 연동 (Added) — 2026-08-02

연동은 `GVSKB_REGISTRY_URL` 환경변수 **옵트인**이며 미설정 시 기존 동작과 100%
동일하다(연동 합의 원칙 1 — 단방향 의존).

- **verdict 2종 추가** (`673db76`): `registry_approved` · `registry_rejected`.
  사다리에서 `registry_rejected` 는 `not_found` 보다 위(기관의 명시적 차단이 더 강한
  신호), `registry_approved` 는 `checked_clean` 보다 위(사람이 확인한 것이 자동
  판정보다 강함), 그러나 **`malicious` 는 최상위**.
- **승인 3분기 규칙** — 기관 승인이 로컬 악성 탐지를 덮지 못하게 한다. 승인은
  시점의 판단이고 위협 정보는 그 뒤에도 갱신되므로, 승인된 버전이 나중에 악성으로
  등재되면 만료일까지(보통 3개월) 초록불이 유지된다. 협상에서 상대 원안이 정확히
  이 결함을 갖고 있었고 그걸 지적한 쪽이 우리이므로, 우리 구현이 같은 결함을
  갖는 일은 없어야 한다.
  - 캐시 신선 + 미적중 → `registry_approved` / `checked=True` / review=False
  - 캐시 신선 + 적중 → `malicious`·KEV 가 이김
  - 캐시 낡음 → `checked=False` + 배너 (30일 초과 또는 **캐시 없음**이면 review=True)
- **함께 고친 잠재 결함**: `blocked` 집계가 `verdict_severity == "high"` 만 봐서
  `critical` 이 누락됐다. 사다리 최상단인 `registry_rejected` 가 차단 게이트를
  못 지날 뻔했다. CLI 종료 코드도 `registry_rejected` 를 차단으로 다룬다.
- **클라이언트**(`24d4906`) — 조회(`check-bulk` 200건, 자체 분석과 **병렬**),
  제출(`submit-batch`, 봉투 형식), 캐시, fail-open.
  - **`REJECTED` 캐시는 만료 시 삭제가 아니라 '낡은 차단'으로 강등**. 지워 버리면
    장애가 TTL 을 넘긴 순간 차단이 조용히 풀리고, 레지스트리 도달을 방해하는
    것만으로 차단을 우회할 수 있게 된다.
  - 제출 필터에 `registry_approved`·`registry_rejected` 추가 — **우리가 준 답이
    되돌아가는 것**이라 관측이 아니다.
  - `GVSKB_MODE=offline` 에서는 `GVSKB_REGISTRY_ALLOW_OFFLINE` 별도 옵트인을
    요구한다. "외부로 나가지 않는다"는 사용자와의 약속이라 레지스트리가
    내부망이어도 조용히 바꿀 수 없다.
  - 도달 실패는 `registry_status` 로 표시하되 **`requires_review` 를 올리지 않는다** —
    조치가 '레지스트리 복구' 한 건인데 패키지 수백 건에 깃발을 꽂으면 경고 피로가 된다.

### 라이선스 변경 (Changed) — 2026-08-01

- **MIT → PolyForm Noncommercial License 1.0.0** (`31a32df`). 공공기관·교육기관·
  연구기관·공공안전 기관의 사용은 재원과 무관하게 허용됩니다(라이선스가
  "government institution"을 명시). 막히는 것은 자기 명의 제품으로의 상업적
  판매·번들입니다.
  - **2026-08-01 이전 MIT 배포본에는 소급되지 않습니다.** 해당 버전을 MIT 조건으로
    받은 이용자의 권리는 그대로 유지됩니다.
  - PyPI 분류자를 `License :: Other/Proprietary License`로 변경(비OSI 라이선스라
    대응하는 OSI 분류자가 없음). GitHub 표시는 "Other".
- **수급사업자 사용 추가 허가 부기** (`28db33e`). PolyForm NC 원문은 **기관 자신의
  사용**만 허용해, 공공사업을 수행하는 민간 용역사의 사용이 덮이지 않았습니다.
  공공 정보화사업 상당수가 용역사를 통해 수행되므로 도입 경로 하나가 통째로
  막히는 문제였습니다(연동 상대가 검토 중 발견).
  - 허용: 허용 조직의 계약·공공조달을 **수행하기 위해**, **계약 기간 동안**,
    용역사·하수급사업자의 사용·수정·배포. 발주기관 납품 포함.
  - 불허: 자기 명의 판매·구독·번들, 계약 종료 후 사용(기록 보존 제외).
  - `Required Notice:` 행 추가 — PolyForm 규정상 "약관 사본 **또는 URL**"만 주면
    되는데, **URL만 전달하면 이 추가 허가가 따라가지 않습니다.** LICENSE 파일 전체
    전달을 명시했습니다.
  - 법무 검토를 받지 않은 초안입니다.

### 시크릿 탐지·마스킹 (Fixed) — 2026-08-01

- **`sk_` 계열 키가 마스킹되지 않던 문제** (`8f17885`). `redact_evidence`가
  `sk-`(하이픈)만 가려서 `sk_ggtrust_…`·`sk_live_…` 형식 키가 보고서·오류
  메시지에 **그대로 출력**됐습니다. 탐지 누락은 못 찾는 것이지만 마스킹 누락은
  도구가 직접 유출하는 것입니다. `sk([-_])…`로 확장했습니다.
  - 마스킹은 탐지 룰보다 **의도적으로 느슨하게** 잡습니다. 과잉 마스킹의 손해는
    증거가 덜 읽히는 것뿐이고, 누락의 손해는 유출입니다.
- **변수명 없는 자리의 탐지 누락**. 기존 룰은 변수명(`api_key`·`token`…)에
  기대거나 `sk-`를 봤습니다. 값을 위치 인자로 바로 넘기면
  (`RegistryClient(url, "sk_…")`) 둘 다 빗나갔습니다.
  `sk_<벤더>_<본문>` 형태를 추가했습니다(Stripe `sk_live_`·`sk_test_` 포함).
  - `sk_`만으로 잡지 않습니다 — 하이픈과 달리 언더스코어는 식별자에 흔해
    `sk_model_pipeline_transformer` 같은 정상 코드가 오탐이 됩니다. 벤더 구절과
    "본문에 숫자·대문자 최소 1개"를 요구해 배제했습니다.
- **비상업 라이선스가 `unknown`으로 떨어지던 문제**. PolyForm 계열·Elastic-2.0이
  허용목록에도 검토목록에도 없었습니다. `license_review_required`에 등재했습니다
  (허용목록이 아닌 이유: 그 목록의 정의가 "공공 활용 제약 적음"인데 비상업
  라이선스에는 제약이 실재하고, 이는 이 도구 자신의 라이선스이기도 합니다).
  SPDX 식별자(하이픈)와 서술형(공백)이 함께 쓰여 목록 접두 일치만으로는 서술형이
  빠져나가, 계열 접두 분기를 추가했습니다.

### 패키지 판정 감사 기록 (Added) — 2026-08-01

- **`check_package`·`scan_dependencies`·`scan_installed_packages`에 감사 기록이
  아예 없던 공백을 메움** (`134a7d6`). `record_scan`이 `ScanReport`를 전제로 해서
  스캔 계열에서만 돌았습니다. 공공기관 감사에서 "언제 무엇을 허용·차단했는가"는
  스캔 결과와 별개의 질문인데, 허용 기록이 없으면 답할 수 없습니다.
- `AuditEvent`에 `package_check`·`package_check_batch` 이벤트와
  `package`·`verdict`·`checked` 필드 추가. 패키지명·버전은 해시하지 않고 purl
  형태로 남깁니다 — 비밀이 아니고, 이 이벤트의 존재 이유가 그것이며, 레지스트리·
  SBOM과 대조할 수 있어야 합니다.
- 기록 단위를 규모로 나눕니다: **단건은 판정이 무엇이든 1건**(사람이 직접 물어본
  허용 판정도 감사 대상), **대량은 집계 1건 + 조치가 필요한 판정만** 개별 기록.
  락파일까지 들어오면 한 번에 수백~수천 건이라, 정상 통과를 전부 남기면 감사로그가
  통과 기록으로 뒤덮여 봐야 할 줄이 묻힙니다.
- `caller` 설명에 개인 식별자 금지를 명시했습니다 — 감사에 필요한 것은 "무엇이
  호출했는가"이지 "누가"가 아닙니다.

### 외부 연결 인벤토리 (Fixed) — 2026-08-01

- **문서 문맥이 카탈로그의 구체적 경고를 덮어쓰던 결함** (`1d3d119`).
  `_file_context == "doc-or-installer"`일 때 `data_summary`를 통짜 문구로
  **교체**해, 카탈로그가 아는 호스트의 경고까지 사라졌습니다.
  실측: 설치 가이드의 `Invoke-RestMethod https://api.ipify.org`(관리자에게 실행을
  안내하는 명령)이 "설치·안내 문서의 다운로드 주소(운영 중 전송 아님)"로
  표시됐습니다. 다운로드 주소도 아니고 전송이 없지도 않습니다 — 실행하면 서버
  공인 IP가 제3자에게 나갑니다. **'문서라서 안전하다'로 위험을 덮은 것**입니다.
  → 문맥은 덧붙이고, 아는 게 없을 때만 통짜 문구를 씁니다.
- 미분류 호스트 카탈로그 보강 — 설치 자재 배포처(`nginx.org`·`nssm.cc`·
  `slproweb.com`·`python.org`·`github.com`)와 google.com 계열. 실측 프로젝트 기준
  미분류 17건 → 0건. google 계열은 경로마다 성격이 달라(`/maps`=좌표,
  `/recaptcha`=방문자 IP) 용도를 단정하지 않고 확인 지점을 지목합니다.

### CI · 인텔 파이프라인 (Fixed) — 2026-07-31 ~ 08-01

- **KEV 룰 PR이 24일간 병합되지 못하던 문제 해결** (`33f1a8f`, `10611f8`).
  `GITHUB_TOKEN`이 만든 PR에도 `pull_request` 이벤트 런은 생성되지만 전부
  `action_required`(사람 승인 대기)에서 멈춥니다. 그 결과 head 커밋의
  `statusCheckRollup`이 `null`이 되어 룰셋은 필수 체크가 "보고되지 않았다"고
  판단하고 BLOCKED을 유지합니다. **이름·SHA·앱ID가 전부 일치하는 성공 체크를
  `workflow_dispatch`로 따로 만들어 붙여도 이 게이트는 통과하지 못합니다** —
  그 우회책이 24일간 무력했던 이유입니다.
  → PR을 PAT(`INTEL_PR_TOKEN`)로 생성해 정상 경로를 탑니다. 자동 병합도 같은
    토큰으로 걸어야 main push가 CI를 트리거합니다.
  → 우회책 제거에 따라 `actions: write` 권한도 반납(최소 권한).
  → 교훈을 `tests/test_workflows.py`에 실행 가능한 가드로 고정했습니다.
- **PR 토큰 점검을 상시 실행** (`8a5ab3e`). 점검이 "새 룰이 있을 때"에만 돌아서
  **토큰이 만료돼도 다음에 새 KEV가 나오는 날까지 아무도 모르고, 그날 처음
  실패**하는 구조였습니다(실측: 2026-07-31 정기 실행에서 토큰 관련 단계가 전부
  skipped). PAT는 만료가 있으므로 언젠가 반드시 오는 상황입니다.
  → 매 실행 점검하고, 존재 여부만이 아니라 저장소 조회 1회로 **실제 사용
    가능한지**까지 확인합니다. 상태를 `missing`·`invalid`·`ok`로 나눠 요약과
    경고에 남깁니다.

### gg-trusted-registry 연동 설계 (Added) — 2026-08-02

- 경기도 신뢰 패키지 레지스트리와의 연동 설계 협상 종료. 관계·경계·API 계약·결정
  연혁을 내부 합의 문서 한 건으로 정리했습니다.
- 구현은 아직 착수 전입니다. 연동은 `GVSKB_REGISTRY_URL` 환경변수 옵트인이며,
  **미설정 시 기존 동작과 100% 동일**합니다.

### 폐쇄망(망분리) 영향 표시 (Added)
- **외부 리소스(CDN) 인벤토리** — `<script src>`·`<link href>`·`@import`·
  `url()`·ESM `import`로 로딩되는 외부 정적 리소스(jsDelivr·unpkg·cdnjs·
  Google Fonts·Tailwind CDN 등)를 `kind="resource"`로 탐지. 주석·문서 링크나
  `<a href>` 이동 링크는 잡지 않음(로딩 문맥 게이팅), 로컬·내부망 경로 제외.
- **폐쇄망 영향 필드**(`airgap_impact`) — 리소스는 `breaks`(인터넷 없이 로딩
  실패 → 화면·기능 파손), 외부 API·전송 SDK는 `egress`(차단되거나 정책 위반
  전송 소지)로 분류. 리포트 외부 연결 섹션에 **"폐쇄망 배포 시 확인" 콜아웃 +
  전용 표**(위치·운영주체·영향)로 표시하고, 유지 시 SRI(integrity) 적용 권고.
- 같은 줄의 호스트가 api로 중복 계상되지 않도록 리소스 문맥 줄은 API 추출에서
  제외. 테스트 478 → 487.

### 인텔 초안 룰 수명주기 자동화 (Added)
- **최근 등재 필터** — KEV→proposed 승격이 기본 **최근 90일 등재분만** 대상
  (`GVSKB_KEV_PROMOTE_SINCE_DAYS`, 0=전체 백필). 2002년부터의 카탈로그 전체
  (1,600+개)가 초안으로 쏟아지던 문제 해소. 등재일 미확인 항목은 보수적으로 제외.
- **만료 초안 자동 폐기** — `update-intel --promote`가 review_due(생성+90일)가
  지나도록 승인되지 않은 proposed 초안을 자동 삭제. 사람이 승격한
  `status: approved` 룰은 절대 건드리지 않음.
- **초안 PR 자동 병합** — 초안 카드는 지식층 전용(집행은 status 게이트가 차단)
  이므로 CI(테스트 478개) 통과 시 자동 병합. 사람의 결정은 "스캐너 집행 승격"과
  월간 다이제스트 검토로 이동 — 1인 운영의 PR 수동 병합 부담 제거.
- **월간 다이제스트 이슈** — 매월 1일 자동 생성(`intel-digest` 워크플로):
  대기 초안 수·신규·폐기 예정·**우선 검토 후보**(랜섬웨어 악용 critical)를
  요약. 유지관리자는 이슈 하나만 읽고 승격 여부만 판단.

## [0.2.1] - 2026-07-05

### 실시간 인텔 활용 구조 (Added)
- **일일 인텔 번들 자동 게시** — `update-intel` 워크플로가 매일 캐시를 수집한 뒤
  `intel-bundle export`로 검증 가능한 zip을 만들어 **`intel-latest` 릴리스에
  고정 URL로 게시**(+sha256 사이드카, 30일 아티팩트). 이전에는 CI가 받은 캐시가
  러너에서 휘발되어 "매일 갱신"의 산출물이 proposed 룰 PR뿐이었음 — 이제 망분리
  기관은 매일 아침 검증된 반입 번들을 내려받기만 하면 됨.
- **리포트 인텔 기준일 자동 표기** — 오프라인 스캔 시 캐시의 `fetched_at`을 읽어
  `intel_freshness`(피드별 날짜)를 채움. 보안팀이 "며칠 캐시 기준 판정인지"를
  리포트만으로 확인 가능(기존에는 필드·배너만 있고 채우는 코드가 없었음).
- **EPSS·NVD 점수 병기** — 매일 수집만 되고 판정에 쓰이지 않던 epss-recent·
  nvd-recent 캐시를 활용: KEV 등재 취약점 신호에 **EPSS 악용확률(0~1)·백분위**와
  **CVSS 3.1 점수·심각도**를 병기해 보안팀 우선순위 판단 근거 제공.
  테스트 467 → 471.

### 탐지 (Added)
- **LLM 프롬프트 인젝션 탐지**(`GOV-LLM-PROMPT-INJECTION-001`, OWASP LLM01 · 국정원
  AI 가이드 T08) — Python AST taint를 확장해, 문자열 결합(`+`)·f-string으로 **동적
  조립된 프롬프트 변수가 실제 LLM SDK 호출**(OpenAI `chat.completions.create`·
  Anthropic `messages.create`·Gemini `generate_content` 등)에 도달하면 발화합니다.
  SQL taint와 같은 스코프 추적을 재사용하며, **입력을 데이터로만 전달**하거나(역할
  분리 `messages`) 상수 프롬프트·비-LLM `.create()`(ORM)는 발화하지 않아 오탐 0을
  유지합니다. 독립 벤치마크 LLM 카테고리 **0/1 → 1/1**, 종합 recall **96.7% → 100%**
  (30/30), 오탐 0. 테스트 455 → 467.

## [0.2.0] - 2026-07-04

공공기관 실사용 워크플로("공무원 검사 → 보안팀 붙임 제출 → 보안성 검토 → 운영 배포")를
기준으로 탐지·리포트·오프라인 운영·감사를 전면 보강한 릴리스입니다.

### 탐지 (Added)
- **다줄 SQL 삽입 탐지** — Python AST 변수추적(mini-taint): "윗줄에서 쿼리 조립,
  아랫줄에서 `execute()`" 형태(AI 생성 코드의 지배적 패턴)를 탐지. 파라미터 바인딩은
  발화하지 않음(오탐 0 유지).
- **JS/TS 다줄 taint 스캐너**(`js-taint` 엔진) — 의존성 없이 동작. 다줄 조립 후
  `.query()/.execute()`·`eval()/Function()`·`innerHTML` 도달 탐지.
  `DOMPurify.sanitize` 정화·상수 재할당은 발화하지 않음.
- **런타임 룰 status 게이트** — `proposed`(자동 생성) 룰은 `GVSKB_ALLOW_PROPOSED=1`
  없이는 집행되지 않고, `deprecated`는 절대 집행되지 않음. 검색·조회에는 영향 없음.

### 리포트 (Added / Changed)
- **두 독자용 2층 재설계 (두괄식)** — ① 요약층(공무원, 항상 펼침): 문서 헤더 표 →
  **결론 박스(배포 승인=초록 / 미승인=빨강)** → 핵심 숫자 → 조치 가이드(3단계) →
  가장 먼저 할 일 → 검토 범위·한계. ② 상세층(보안팀, 기본 접힘): 발견을 **보안
  분야별**(개인정보·비밀값·주입·웹·암호화·설정·AI·코드안정성)로 묶어 펼치면 위치·
  왜 위험한지·대응 방법·근거를 표시. 모든 상세는 접힘 → **인쇄 시 자동 펼침**.
- **수정 프롬프트 복사 버튼**(인라인 스크립트, 외부 로딩 없음). 공문서 톤을 위해
  장식 이모지를 줄이고 승인/미승인은 색·굵은 글씨로만 구분.
- **검토 범위·한계 고지** 섹션 — "발견 0건 ≠ 안전"을 문서가 스스로 밝힘.
- **배포 판정**(배포 승인/미승인/보류) + 잔여 위험 문구 — 보안팀 승인 근거.
- **개인정보·비밀값 전용 요약** + 같은 줄 다중 룰 묶음("발견 N건 · 고유 위치 M곳").
- **의존성(패키지) 취약점 섹션** — `scan_dependencies`/`--check-deps` 결과 병합.
  판정 불가·파싱 불가는 '안전 아님'으로 명시.
- **외부 연결 인벤토리 보강** — 운영주체·국가(예: `OpenAI(미국)`), 호출 지점 수
  ("외 N곳"), AI 학습·보존 정책 확인 체크리스트. 국외이전 검토는 "누구에게,
  어느 나라로"가 특정되어야 하기 때문.
- **오프라인(망분리) 모드 배너** — 캐시 기준 검사임을 리포트에 정직하게 표기.
- **SARIF 2.1.0 출력**(`--format sarif`, MCP `render_report format="sarif"`) —
  GitHub code scanning·기관 보안도구 연동. 억제된 발견은 표준 `suppressions`로 표기.
- 리포트는 공문의 **'붙임' 문서** 전제 — 결재 헤더·서명란을 넣지 않음(결재는 상위 공문).

### 오프라인(망분리) 운영 (Added / Fixed)
- **인텔 캐시 무결성** — 로드 시 sha256 재검증, 변조·손상 캐시는 자동 무시.
- **신선도 정책** — 기본 30일(`GVSKB_INTEL_MAX_AGE_DAYS`) 초과 캐시의 '이상 없음'은
  `checked_stale`(검토 필요)로 승격. 악성 발견(양성)은 오래돼도 유효.
- **생태계 커버리지** — 캐시가 담은 생태계를 기록. PyPI 전용 캐시로 npm 패키지를
  '깨끗함' 판정하지 않음. KEV 단독 캐시는 '깨끗함'의 근거가 아님.
- **`gvskb intel-bundle export|import`** — 망분리 반입을 sha256 전수 검증되는
  단일 zip 절차로 공식화(검증 실패 시 전체 거부).
- `doctor`·MCP `server_status`에 인텔 캐시 존재·건수·경과일 진단 추가.
- `update-intel --from-cache`가 캐시 나이를 표시하고 신선도 초과 시 WARN.

### 게이트 정확성 (Fixed)
- `scan_dependencies`가 락파일(yarn.lock·poetry.lock 등)에 `verdict:"ok"`를 반환하던
  거짓 통과 제거 — 파싱 전에 감지해 `unparsed`(검토 필요)로 거절.
- 알려진 CVE가 있는 패키지가 `ok`로 통과하던 문제 — `review_required`로 승격.
- `gvskb check-package`가 판정 불가(오프라인 캐시 없음·API 실패)에 exit 0을 내던
  문제 — CI가 "검사 못 함"을 통과로 처리할 수 없도록 비-0 종료.
- 리포트 HTML `class="pill ..."` 속성 미이스케이프(신뢰 불가 리포트 JSON 렌더 시
  저장형 XSS) 봉쇄.
- **인쇄(PDF) 시 접힌 상세 누락** — 최신 Chromium/Edge의 `::details-content` 클리핑
  대응. 발견 상세·수정 프롬프트가 인쇄물에 온전히 포함됨.

### 감사·예외 (Added)
- **JSONL 감사로그**(`GVSKB_AUDIT_DIR` 옵트인) — scan/block/warn/approve_bypass/
  update_intel 이벤트를 월별 파일에 append. 원본 코드·개인정보는 저장하지 않고
  해시·마스킹 증거만 기록.
- **승인된 예외 파일**(`.gvskb-exceptions.yaml`) — 사유·승인자·만료일이 모두 있어야
  유효. 발견을 숨기지 않고 게이트만 통과시키며, 만료 시 자동으로 다시 차단.
  적용 내역은 리포트 '승인된 예외 내역'과 감사로그 `approve_bypass`로 남음.

### CI / 공급망 (Fixed)
- `update-intel` 워크플로 — 신규(untracked) proposed 룰을 감지하지 못해 PR이
  생성되지 않던 버그 수정(`git status --porcelain`), 외부 액션 커밋 SHA 핀.
- `gvskb scan --check-deps` — 코드+패키지 위험을 한 번에 검사해 리포트에 병합.
- pre-commit 예시 수정(`pass_filenames: false`), exit code 문서화(64 포함).

## [0.1.0] - 2026-06-13

- 최초 공개: 215개 룰(KISA·행안부·국정원·OWASP), regex+AST+semgrep 3엔진,
  FastMCP 서버 + `gvskb` CLI, 한국어 md/HTML 리포트, 망분리 오프라인 모드,
  독립 벤치마크(recall 96.7% / 오탐 0).
