name: update-intel

# Daily refresh of external security feeds (CISA KEV, OSV, NVD, EPSS) and
# promotion of new KEV entries into rules/intel-proposed/ as status=proposed.
# Network failures must not break this workflow — air-gapped operators run
# `gvskb update-intel --from-cache` instead.

on:
  schedule:
    # 18:00 UTC daily = 03:00 KST
    - cron: "0 18 * * *"
  workflow_dispatch:
    inputs:
      sources:
        description: "Which sources to refresh (--source value or 'all')"
        required: false
        default: "all"

permissions:
  contents: write
  pull-requests: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install gvskb
        run: |
          python -m pip install --upgrade pip
          python -m pip install ".[dev]"

      - name: Refresh intel cache + promote KEV proposed rules
        id: refresh
        continue-on-error: true
        env:
          NVD_API_KEY: ${{ secrets.NVD_API_KEY }}
          GVSKB_CACHE_DIR: ${{ github.workspace }}/.gvskb-cache
        run: |
          SRC="${{ inputs.sources || 'all' }}"
          if [ "$SRC" = "all" ]; then
            gvskb update-intel --all --promote --json | tee intel-result.json
          else
            gvskb update-intel --source "$SRC" --promote --json | tee intel-result.json
          fi

      # ------------------------------------------------------------------
      # 산출물 1: 검증 가능한 인텔 번들(고정 URL) — 캐시가 러너에서 휘발되지
      # 않고 사용자에게 도달하게 하는 단계. 망분리 기관은 외부망 PC에서 이
      # 번들을 받아 sha256 확인 → USB 반입 → `gvskb intel-bundle import` 하면
      # 매일 갱신된 캐시를 그대로 쓸 수 있다.
      # ------------------------------------------------------------------
      - name: Export verified intel bundle
        id: bundle
        if: steps.refresh.outcome == 'success'
        env:
          GVSKB_CACHE_DIR: ${{ github.workspace }}/.gvskb-cache
        run: |
          gvskb intel-bundle export gvskb-intel-bundle.zip
          sha256sum gvskb-intel-bundle.zip > gvskb-intel-bundle.zip.sha256
          cat gvskb-intel-bundle.zip.sha256

      - name: Upload bundle artifact (traceability)
        if: steps.bundle.outcome == 'success'
        uses: actions/upload-artifact@v4
        with:
          name: gvskb-intel-bundle-${{ github.run_id }}
          path: |
            gvskb-intel-bundle.zip
            gvskb-intel-bundle.zip.sha256
          retention-days: 30

      - name: Publish bundle to rolling release (intel-latest)
        if: steps.bundle.outcome == 'success'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # 고정 태그 intel-latest — 다운로드 URL이 매일 동일하게 유지된다:
          #   releases/download/intel-latest/gvskb-intel-bundle.zip
          # --latest=false: 코드 릴리스의 'Latest' 자리를 차지하지 않는다.
          if ! gh release view intel-latest >/dev/null 2>&1; then
            gh release create intel-latest \
              --title "일일 위협 인텔 번들 (자동 갱신)" \
              --notes "placeholder" --latest=false
          fi
          gh release upload intel-latest \
            gvskb-intel-bundle.zip gvskb-intel-bundle.zip.sha256 --clobber
          gh release edit intel-latest --notes "$(printf '%s\n' \
            "매일 03:00 KST 자동 갱신되는 위협 인텔 반입 번들입니다." \
            "" \
            "- 갱신 시각(UTC): $(date -u +%Y-%m-%dT%H:%M:%SZ)" \
            "- 소스: OSV malicious · CISA KEV · NVD recent · FIRST EPSS" \
            "- 검증: zip 내부 manifest.json(파일별 sha256) + 게시 sha256 사이드카" \
            "" \
            "망분리 반입: 외부망 PC에서 다운로드 → sha256 확인 → USB 이동 →" \
            "\`gvskb intel-bundle import gvskb-intel-bundle.zip\` → \`gvskb doctor --offline\`")"

      # ------------------------------------------------------------------
      # 산출물 2: KEV → proposed 룰 PR (사람 검토 게이트)
      # ------------------------------------------------------------------
      - name: Detect changes in proposed rules
        id: detect
        run: |
          # git diff 는 untracked(신규) 파일을 보지 못한다 — 새 proposed 룰은
          # 항상 신규 파일이므로 status --porcelain 으로 감지해야 PR이 생성된다.
          if [ -z "$(git status --porcelain rules/intel-proposed/ 2>/dev/null)" ]; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
          else
            echo "changed=true" >> "$GITHUB_OUTPUT"
            echo "## New proposed rules" > /tmp/pr_body.md
            git status --porcelain rules/intel-proposed/ | head -50 >> /tmp/pr_body.md
            echo >> /tmp/pr_body.md
            echo "> 자동 수명주기: 이 카드들은 지식층 전용(스캐너 집행 안 됨)이라 CI 통과 시 자동 병합됩니다." >> /tmp/pr_body.md
            echo "> 90일 내 status: approved 로 승격되지 않으면 다음 갱신에서 자동 폐기됩니다." >> /tmp/pr_body.md
            echo "> 우선 검토 후보는 매월 1일 다이제스트 이슈로 정리됩니다." >> /tmp/pr_body.md
            echo >> /tmp/pr_body.md
            echo "## update-intel output (JSON)" >> /tmp/pr_body.md
            echo '```json' >> /tmp/pr_body.md
            head -c 4000 intel-result.json >> /tmp/pr_body.md || true
            echo >> /tmp/pr_body.md
            echo '```' >> /tmp/pr_body.md
          fi

      # ------------------------------------------------------------------
      # PR 은 반드시 PAT 로 만든다 — GITHUB_TOKEN 으로 만들면 병합이 영구히 막힌다.
      #
      # 실측(PR #4, 2026-07-07 ~ 07-31, 24일 정체): GITHUB_TOKEN 이 만든 PR 에도
      # pull_request 이벤트 런은 **생성되지만** 전부 `action_required`(사람 승인
      # 대기)에서 멈춘다. 그 결과 PR head 커밋의 statusCheckRollup 이 null 이 되어
      # 룰셋은 필수 체크가 "보고되지 않았다"고 판단하고 BLOCKED 을 유지한다.
      # 이름·SHA·앱ID 가 전부 일치하는 성공 체크를 workflow_dispatch 로 따로
      # 만들어 붙여도 이 게이트는 통과하지 못한다(그 우회책이 24일간 무력했던
      # 이유). 승인 대기 런을 사람이 승인하자 즉시 병합됐다.
      #
      # → 시크릿 INTEL_PR_TOKEN (fine-grained PAT: contents write,
      #   pull-requests write) 로 PR 을 만들면 작성자가 실제 계정이 되어
      #   pull_request CI 가 승인 없이 정상 실행되고 자동 병합이 발동한다.
      #   자동 병합도 같은 토큰으로 걸어야 main 으로의 push 가 CI 를 트리거한다
      #   (GITHUB_TOKEN 으로 병합하면 main HEAD 에 CI 가 붙지 않는다).
      # ------------------------------------------------------------------
      - name: PR 생성 토큰 확인
        id: prtoken
        if: steps.detect.outputs.changed == 'true'
        env:
          PR_TOKEN: ${{ secrets.INTEL_PR_TOKEN }}
        run: |
          if [ -z "$PR_TOKEN" ]; then
            echo "available=false" >> "$GITHUB_OUTPUT"
            echo "::warning::INTEL_PR_TOKEN 시크릿이 없습니다 — 룰 PR 을 만들지 않습니다."
          else
            echo "available=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Create pull request
        id: cpr
        if: steps.detect.outputs.changed == 'true' && steps.prtoken.outputs.available == 'true'
        # SHA 핀 — 태그는 이동할 수 있으므로 공급망 안전을 위해 커밋으로 고정 (= v6)
        uses: peter-evans/create-pull-request@c5a7806660adbe173f04e3e038b0ccdcd758773c  # v6
        with:
          token: ${{ secrets.INTEL_PR_TOKEN }}
          branch: intel/auto-update
          base: main
          delete-branch: true
          add-paths: |
            rules/intel-proposed/
          commit-message: "[intel] refresh proposed rules from CISA KEV"
          title: "[intel] update CISA KEV proposed rules"
          body-path: /tmp/pr_body.md
          labels: |
            intel
            proposed-rules
            needs-review

      # 초안 카드는 지식층 전용(집행 게이트는 status: approved)이므로, 테스트가
      # 전부 통과하면 자동 병합한다 — 사람의 결정은 "스캐너 집행 승격"과 월간
      # 다이제스트 검토로 옮겨진다(1인 운영에서 PR 수동 병합 부담 제거).
      - name: Enable auto-merge (advisory cards only)
        if: steps.cpr.outputs.pull-request-number
        # 룰 PR 처리 실패가 **번들 배포 성공까지 빨간불로 덮으면 안 된다**.
        # 사용자가 매일 받는 것은 번들이고, 룰 카드 병합은 하루 늦어도 된다.
        # 대신 아래 status-gate 가 요약에 남겨 조용히 묻히지 않게 한다.
        continue-on-error: true
        id: automerge
        env:
          GH_TOKEN: ${{ secrets.INTEL_PR_TOKEN }}
        run: |
          gh pr merge --squash --auto "${{ steps.cpr.outputs.pull-request-number }}"

      # ------------------------------------------------------------------
      # 상태 게이트 — "무엇이 진짜 실패인가"를 분명히 한다.
      #
      # 이전 구조의 문제: 인텔 수집 스텝이 continue-on-error 인데 최종 검증이
      # 없어, **수집이 실패해도 잡이 초록불**로 끝났다("갱신되고 있다"고 믿는데
      # 실제로는 아닌 상태 — 보안 도구에서 가장 위험한 침묵). 반대로 룰 PR
      # 처리의 사소한 실패는 잡 전체를 빨간불로 만들어 경고 피로를 낳았다.
      #
      # 기준: 사용자가 매일 내려받는 **번들 배포 실패만 빨간불**. 룰 카드
      # 병합 실패는 요약에 경고로 남긴다.
      # ------------------------------------------------------------------
      - name: 상태 게이트 (번들 배포 성공 여부로 판정)
        if: always()
        env:
          REFRESH: ${{ steps.refresh.outcome }}
          BUNDLE: ${{ steps.bundle.outcome }}
          AUTOMERGE: ${{ steps.automerge.outcome }}
          PR_NUM: ${{ steps.cpr.outputs.pull-request-number }}
          CHANGED: ${{ steps.detect.outputs.changed }}
          PR_TOKEN_OK: ${{ steps.prtoken.outputs.available }}
        run: |
          echo "### 인텔 갱신 결과" >> "$GITHUB_STEP_SUMMARY"
          echo "- 인텔 수집: \`$REFRESH\`" >> "$GITHUB_STEP_SUMMARY"
          echo "- 번들 배포: \`${BUNDLE:-skipped}\`" >> "$GITHUB_STEP_SUMMARY"
          echo "- 룰 PR: \`${AUTOMERGE:-none}\` ${PR_NUM:+(#$PR_NUM)}" >> "$GITHUB_STEP_SUMMARY"

          if [ "$REFRESH" != "success" ]; then
            echo "::error::인텔 수집 실패 — 사용자가 받는 위협 데이터가 갱신되지 않았습니다."
            echo "> ❌ **인텔 수집 실패** — 사용자 캐시가 낡습니다." >> "$GITHUB_STEP_SUMMARY"
            exit 1
          fi
          if [ "$BUNDLE" != "success" ]; then
            echo "::error::번들 배포 실패 — intel-latest 릴리스가 갱신되지 않았습니다."
            echo "> ❌ **번들 배포 실패** — 사용자가 최신 번들을 받을 수 없습니다." >> "$GITHUB_STEP_SUMMARY"
            exit 1
          fi
          # 새 룰이 나왔는데 토큰이 없어 PR 자체를 못 만든 경우 — 조용히 넘어가면
          # "오늘은 새 KEV 가 없었다"와 구분되지 않는다. 반드시 흔적을 남긴다.
          if [ "$CHANGED" = "true" ] && [ "$PR_TOKEN_OK" != "true" ]; then
            echo "::warning::INTEL_PR_TOKEN 시크릿 미설정 — 새 proposed 룰이 있으나 PR 을 만들지 못했습니다."
            echo "> ⚠️ **INTEL_PR_TOKEN 미설정** — 새 룰 카드가 저장소에 반영되지 않습니다(번들은 정상)." >> "$GITHUB_STEP_SUMMARY"
          fi
          if [ -n "$PR_NUM" ] && [ "$AUTOMERGE" != "success" ]; then
            echo "::warning::룰 PR #$PR_NUM 자동 병합 실패 — 번들은 정상 배포됐습니다. PR 을 확인하세요."
            echo "> ⚠️ 룰 PR #$PR_NUM 처리 실패(번들은 정상). 수동 확인 필요." >> "$GITHUB_STEP_SUMMARY"
          fi
          echo "> ✅ 번들 배포 완료 — 사용자는 최신 위협 인텔을 받습니다." >> "$GITHUB_STEP_SUMMARY"
