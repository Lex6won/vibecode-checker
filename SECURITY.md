# 보안 정책 (Security Policy)

## 취약점 제보

vibecode-checker는 보안 도구이므로, 도구 **자체의 취약점**은
공개 이슈로 올리지 말고 **비공개로** 제보해 주세요.

- **이메일**: firstwon77@gmail.com (제목에 `[vibecode-checker security]` 표기)
- 또는 GitHub **Security Advisory** (저장소 → Security → Report a vulnerability)

제보 시 가능하면 다음을 포함해 주세요:

- 영향받는 버전 / 환경 (OS, Python 버전)
- 재현 절차 또는 PoC
- 예상되는 영향

## 대응 절차

1. 영업일 기준 수일 내 접수 확인을 드립니다.
2. 재현·영향도를 평가하고 수정 계획을 공유합니다.
3. 수정 배포 후, 제보자 동의 시 크레딧과 함께 공개합니다.

비공개 제보를 존중하며, 책임 있는 공개(coordinated disclosure)를 지향합니다.

## 적용 범위

- 이 정책은 **도구 자체의 보안 결함**(예: 룰 처리 중 임의 코드 실행,
  캐시 무결성 우회, 의도치 않은 정보 노출 등)에 적용됩니다.
- 룰의 **오탐(false positive)·미탐(false negative)** 은 취약점이 아니라
  품질 이슈입니다. 공개 [Issues](../../issues)로 제보해 주세요.

## MCP 서버 사용 시 주의 (로컬 파일 접근)

vibecode-checker를 MCP 서버로 실행하면, `scan_path` 도구는 **요청받은 경로의
로컬 파일을 읽어** 검사합니다. 이는 도구의 정상 기능이지만, 연결된 AI
클라이언트가 임의 경로(예: 홈 디렉터리, 비밀이 든 폴더)를 스캔하도록 요청할
수 있음을 의미합니다.

- **신뢰할 수 있는 클라이언트·환경에서만** MCP 서버를 연결하세요.
- 민감 정보가 있는 디렉터리는 스캔 대상으로 지정하지 마세요.
- 검사 자체는 로컬에서 수행되며, 소스코드는 외부로 전송되지 않습니다
  (외부 통신은 패키지 취약점 조회 시 공개 식별자에 한정 — README 참고).
- 망분리·고민감 환경에서는 `GVSKB_MODE=offline`로 외부 통신을 차단할 수 있습니다.

## 중요 면책

> vibecode-checker의 자동 점검은 **공식 보안적합성 검토를 대체하지 않습니다.**
> 1차 보안 린터로서, 명백한 실수를 걸러내고 학습을 돕는 보조 도구입니다.
> `critical`·`high` 항목은 보안 담당자의 검토를 권장하며,
> 기관별 보안 정책·개인정보 처리 기준을 함께 확인하세요.